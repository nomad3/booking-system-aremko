# ============================================================
# DEPENDENCIAS CON VERSIONES SEGURAS
# Actualizado: 2025-11-09
# Backup: booking_system_backup_20251109_115651.tar.gz
# ============================================================

# Core Django (VULNERABILIDADES CRÍTICAS CORREGIDAS)
Django>=4.2.30,<4.3.0  # Fixes: multiple CVEs incl. CVE-2025-64459 (SQLi via _connector), CVE-2025-57833, CVE-2026-33034 (DATA_UPLOAD_MAX_MEMORY_SIZE bypass), CVE-2026-3902 (ASGI header spoofing)
psycopg2-binary>=2.9.9  # PostgreSQL adapter

# Google APIs
google-api-python-client>=2.150.0  # Google Calendar API
google-auth>=2.35.0  # Google authentication
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# HTTP Clients (VULNERABILIDADES CORREGIDAS)
requests>=2.32.5,<2.33.0  # Py3.9 compat. Incluye fix CVE-2024-47081. CVE-2026-25645 requiere 2.33+ (Py3.10) — no aplica: no usamos extract_zipped_paths
httpx>=0.27.2  # Cliente HTTP asíncrono

# Django REST Framework
djangorestframework>=3.15.2  # Django REST Framework

# Production Server
gunicorn>=22.0.0  # Production WSGI server

# OpenAI
openai>=1.57.0  # OpenAI API client

# Database & Storage
dj-database-url>=2.2.0
whitenoise>=6.8.2  # Static files serving
django-cors-headers>=4.6.0  # CORS headers
google-cloud-storage>=2.18.0  # Google Cloud Storage (mantener por compatibilidad)
django-storages>=1.14.4  # Django storage backends

# Cloudinary
cloudinary>=1.41.0  # Cloudinary SDK
django-cloudinary-storage>=0.3.0  # Django integration for Cloudinary

# Excel & Data
xlwt==1.3.0  # Para exportación a Excel (XLS viejo)
openpyxl>=3.1.5  # Para importar/exportar Excel moderno (XLSX)

# Media & Images (VULNERABILIDADES CORREGIDAS)
Pillow>=11.3.0,<12.0.0  # Pinned to 11.x for Python 3.9 compat. CVE-2026-25990 (PSD loader OOB) unresolved — no riesgo real: no procesamos PSD

# Utils
phonenumbers>=8.13.48  # Phone number validation/formatting
django-solo>=2.3.0  # Singleton configuration models
beautifulsoup4>=4.12.3  # HTML parsing
python-dateutil>=2.9.0  # Date/time utilities (para frecuencias periódicas)
pytz>=2024.1  # Timezone support for reminder emails

# PDF Generation
WeasyPrint>=62.3  # PDF generation

# Email Service
django-anymail[sendgrid]>=10.3  # SendGrid integration
