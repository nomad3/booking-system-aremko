apiVersion: v1
kind: Service
metadata:
  name: {{ include "aremko-booking-system.fullname" . }}
  labels:
    {{- include "aremko-booking-system.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aremko-booking-system.selectorLabels" . | nindent 4 }} 