# HANDOFFS — Bitácora de encargos entre agentes (Django ↔ aremko-cli)

> Fuente de verdad del trabajo cruzado entre el **agente Django**
> (`~/dev/booking-system-aremko`) y el **agente aremko-cli**
> (`~/Documents/GitHub/aremko-cli.nosync`). Si se corta la sesión/energía,
> este archivo + `git log` reconstruyen en qué quedamos.

## Tabla de handoffs

| ID | Qué | Implementa | Estado | Última actualización |
|----|-----|-----------|--------|----------------------|
| H-001 | Antiduplicado 48 h + orden gracias→resumen + cron normalizar ciudades (bandeja Conexión-Masajes) | Django → luego aremko-cli (UI) | ✅ CERRADO — Django `a10d1c9` + UI aremko-cli `2f0f793` + cron-job.org diario 07:30. Validado por Jorge en prod 2026-06-10 (bandeja OK; sin casos de bloqueo activos aún — los escenarios de candado/forzar se comprobarán con el uso). Contrato: `RESPUESTA_ANTIDUPLICADO_MASAJES.md` | 2026-06-10 (agente Django) |
| H-002 | Auditar discrepancia leads Refugio (Pixel 14 vs BD 3) + endpoint `GET /api/refugio-leads/` que LISTE leads (no solo conteos) para CPL real + cierre Etapa 4b (cruce teléfono→reservas→ROAS) | Django + aremko-cli | 🔵 INTEGRADO — Django `9f6f078` + aremko-cli `5a9676c` (GetRefugioLeads + applyRefugioSalesAndROAS cruza teléfono form/WhatsApp → reservas; card muestra ingresos/ROAS + CPL formulario vs intención). Pendiente: deploy Render/Vercel + validación de Jorge en prod. Diagnóstico 14vs3 en RESPUESTA_H-002 | 2026-06-11 (agente aremko-cli) |
| H-003 | `GET /api/masaje/outbox/` tarda >10s → timeout en Conexión-Masajes (aremko-cli). Optimizar la vista (sospecha: `_serialize(include_preview=True)` renderiza N previews inline, o falta índice en `SeguimientoBienestarMasaje(estado, fecha_programada)`) | Django | ✅ CERRADO — Django `47d141c` (N+1 eliminado en `outbox_list`, ~1200→~12 queries, sin preview inline). Jorge confirmó bandeja OK en prod 2026-06-13. **aremko-cli verificó el contrato:** NO se consume `preview_html` inline (solo era campo opcional en `types.ts`, nunca leído); el modal "Ver" usa `<iframe src={previewUrl(id)}>` (`page.tsx:714`) → endpoint `/preview/<id>/` (text/html, sin cambios) → no le afecta. Ver `RESPUESTA_H-003` | 2026-06-13 (agente aremko-cli) |
| H-004 | Editar `Cliente.nombre` desde la bandeja de WhatsApp (los nombres auto-creados no coinciden con el real). Endpoint `POST /api/whatsapp/conversations/<phone>/editar-nombre/` (luna-key, body `{nombre}`, audita MovimientoCliente) | Django (endpoint) + aremko-cli (proxy Go + UI lápiz) | ✅ CERRADO — Django `37c92ef` + aremko-cli `5e88ffd`. `POST /api/whatsapp/conversations/<phone>/editar-nombre/` (luna-key, body `{nombre}`); valida no-vacío, audita MovimientoCliente, devuelve `{ok, cliente_id, cliente_nombre}`. Relleno auto sigue `if nombre and not cliente.nombre` (no pisa la corrección). aremko-cli: proxy Go + lápiz inline en el encabezado. **Jorge validó en prod 2026-06-13: "funcionando impecable".** Ver `RESPUESTA_H-004` | 2026-06-13 (agente aremko-cli) |
| H-005 | "Marcar como leído": que el badge "1" y el filtro "solo pendientes" se basen en `requiere_atencion` (que responder/marcar-atendido ya limpian) y no en timestamp (`last_in > last_out`), para poder cerrar una conversación sin respuesta. + botón ✓ en aremko-cli | Django (lógica lista) + aremko-cli (botón) | ✅ CERRADO — Django `eac641a` + aremko-cli `1da3b6e`. `conversations`: `_pendiente`=`req>0` y badge `sin_responder`=`req` (basado en `requiere_atencion`, no timestamp); reacciones (`type='reaction'`) ya no marcan pendiente. aremko-cli: botón "✓ Leído" en el encabezado (llama al `marcar-atendido` existente + refresca). **Jorge validó en prod 2026-06-13: "H-005 ok".** Ver `RESPUESTA_H-005` | 2026-06-13 (agente aremko-cli) |
| H-006 | Ordenar la bandeja con **pendientes primero** (antes del corte `[:limit]`): un blast de plantillas salientes empuja las pendientes hacia abajo y el `agg[:limit]` puede hacer que una conversación sin responder ni se devuelva | Django (orden en `conversations`) | ✅ CERRADO — Django `76550e1`: `conversations` ordena `(req>0, ultimo_ts)` desc ANTES de `[:limit]`; las pendientes quedan arriba y no se caen por el corte aunque haya blast de salientes. aremko-cli: nada (respeta el orden). **Jorge validó en prod 2026-06-13: "orden ok, pendientes primero".** Ver `RESPUESTA_H-006` | 2026-06-13 (agente aremko-cli) |
| H-007 | **Agente IA que contesta WhatsApp** (plan por fases): responde SOLO sobre servicios publicados + productos con stock>0; grounding con catálogo vivo; modelo desde env Render, tono/on-off desde formulario aremko-cli; human-in-the-loop primero (F1 borrador → F2 auto-info → F3 auto+botón). Proyecto "Agentes IA Aremko" | Django (núcleo agente + grounding + config) + aremko-cli (UI control + borradores) | ✅ CERRADO (Fase 1) — app nueva aislada `whatsapp_agent` (drift-safe, migración `0001_initial` sin dependencias). Genera borrador con grounding de catálogo vivo (servicios `publicado_web`+`activo`, productos `publicado_web`+stock>0) vía patrón DPV/OpenRouter; config singleton (on/off, modo, tono, modelo, link reserva) editable desde admin + endpoint `GET/POST /api/whatsapp/agente/config`; `sugerencia_agente` colgado de `GET /api/whatsapp/conversation/?phone=` (generación lazy, no toca el inbound); escalamiento (heurística pre-LLM + `[ESCALAR]` del modelo), pausa por conversación, anti-injection, fallback seguro, sanitización ≤1000 chars. aremko-cli (`12a9b6b`→`cc02791`): página "Agente IA" (on/off, modo, tono, link, modelo) + proxy config; en la conversación precarga el cajón con el borrador ("✨ sugerido por IA") o muestra banner "Derivar a persona" si escala; **auto-genera el borrador al llegar un entrante nuevo** (sin tocar Actualizar); Actualizar regenera sin blanquear el hilo. **Jorge validó en prod 2026-06-13:** borradores con grounding real (masajes con precios; "¿venden comida?" → pizzas/tablas/café/jugos de productos con stock), auto-generación y escalamiento OK. Bugs resueltos en la validación: (1) el proxy Go no reenviaba `&sugerencia=1` [aremko-cli `57f74dc`]; (2) la pausa-por-conversación se aplicaba en modo borrador [Django `df56895`]. Contrato: modelo en el form (no env); sugerencia lazy opt-in (`&sugerencia=1`). **Iteración futura:** Fases 2 (auto-info) y 3 (auto+botón); refinar prompt con reglas de reserva por servicio (solo relajación/descontracturante se reservan online; el resto deriva a WhatsApp). Ver `RESPUESTA_H-007` | 2026-06-13 (agente aremko-cli) |
| H-008 | Botón **"Mensaje de ausencia"**: toggle manual que, al activarse, auto-responde a cada entrante una frase fija ("solo atendemos por www.aremko.cl…"); al desactivarse vuelve el flujo normal (Deborah/borrador agente) | Django (auto-respuesta en inbound + config `ausencia_activa`/`ausencia_mensaje`) + aremko-cli (toggle + editor) | ✅ CERRADO 2026-06-13 (Jorge validó en prod: "mensaje de ausencia funcionando OK") — `whatsapp_agent` suma `ausencia_activa`/`ausencia_mensaje`/`ausencia_anti_spam_horas` a la config (en `agente/config`) + tabla `AusenciaEnviada` (anti-spam por teléfono). El inbound devuelve directiva `responder_ausencia: {mensaje}|null` (guard anti-spam por conversación cada N h; default 6); **quién envía = Go de aremko-cli** (`SendSessionMessage`). Precedencia: con ausencia activa, `sugerencia_agente`=null (no gasta LLM). `outbound` acepta `no_marcar_atendido:true` para que el saliente de ausencia NO saque la conversación de pendientes (Deborah las ve al volver). Migración aditiva (`0002`, 3 campos + 1 tabla). Deploy + `migrate whatsapp_agent` (0002) hechos; aremko-cli `e956f58` (UI toggle + Go envía la directiva y persiste con `no_marcar_atendido`). **Detalle menor pendiente (opcional):** la ausencia solo responde a entrantes de TEXTO; si un cliente manda solo un adjunto va por `/inbound-media` (sin directiva) — cubrir a futuro si se requiere. Ver `RESPUESTA_H-008` | 2026-06-13 (agente Django) |
| H-009 | **Agente IA Fase 2:** (a) base de "Conocimiento/Correcciones" (texto → prompt, autoridad máxima) para corregir respuestas (ej. "tinas son por persona 1-4", "no ofrecer Cacao"); (b) catálogo rico (unidad de precio, capacidad, incluye) + flag `ofrecible_agente`; (c) memoria del cliente (ficha en grounding); (d) history_window + resumen | Django (núcleo) + aremko-cli (UI conocimiento) | ✅ CERRADO parte (a) 2026-06-14 (Jorge validó en prod: "conocimiento funcionando") — campo `conocimiento` (TextField) en `WhatsAppAgentConfig`, inyectado al prompt como **bloque 0 de autoridad máxima** (prima sobre el catálogo), expuesto en `agente/config` (campo `conocimiento`); Django `cca3d1a` + migración `0003` + aremko-cli `8e14322` (textarea "📚 Conocimiento y correcciones"). 10/10 tests + check OK. **(b) ✅ CERRADO 2026-06-14** (Jorge validó: "$25.000 para 4 personas por 4 horas… ¿para qué día?") — **Decisión de Jorge: `publicado_web`(+stock) es la ÚNICA fuente de verdad de lo ofrecible; NO se agrega flag `ofrecible_agente`** (para no ofrecer algo, se despublica). El grounding inyecta capacidad (`capacidad_minima/maxima`; min==max→"para N personas") + duración formateada en horas (`formatear_duracion`: 240→"4 h") + `informacion_adicional`, reusando campos QUE YA EXISTEN en `Servicio` → **sin migración**. Regla en prompt: el dato estructurado del catálogo GANA sobre la prosa de la descripción; y NO empujar la web en cada mensaje. Comando `probar_agente_wa --catalogo [--filtro X]` para diagnóstico. Django `e26ca21`+`0ace5b9`. **Aprendizaje:** el agente daba "2 horas" porque la `descripcion_web` decía "2 horas" (prosa) aunque `duracion`=240; fix = formato claro + regla prompt + Jorge corrigió la prosa. **(c/d) pendientes.** Ver `RESPUESTA_H-009`. Incluye regla heredada H-007 (reserva online solo relajación/descontracturante) | 2026-06-14 (agente Django) |
| H-010 | **Motor de aprendizaje + evolución del agente:** (1) capturar el delta borrador-vs-enviado; (2) ⭐ el agente **extrae la regla** de cada corrección de Deborah y la **PROPONE**, un humano la **aprueba con un clic** → entra al Conocimiento (NO auto-escribe sin aprobación → evita sobre-generalizar, ej. "10% a este cliente" ≠ "10% a todos"); (3) tablero de evolución (serie semanal, % sin editar subiendo). Propósito: mejora progresiva, visible como tendencia (película, no foto) | Django (`AgenteFeedback`+`ReglaSugerida`+extracción LLM, endpoints `/agente/feedback`, `/reglas-sugeridas`(+aprobar/descartar), `/metricas`) + aremko-cli (reportar envío + sección "reglas sugeridas" + tablero) | 🟢 IMPLEMENTADO parte (1) (Django) 2026-06-14 — modelo `AgenteFeedback` + `POST /api/whatsapp/agente/feedback` {phone, wa_message_id, borrador, enviado, editado?} → {ok, feedback_id, editado}; `editado` se calcula si no viene; fire-and-forget (errores → ok:false HTTP 200, nunca 5xx). Migración `0004` (1 tabla). check + smoke + 10/10 tests OK. **Parte 1 🔵 INTEGRADO 2026-06-14**: Django desplegado + `migrate 0004` aplicado (ruta `/agente/feedback` viva) + aremko-cli `5135261` reporta tras envío con borrador (`!escalar`, omite `editado` → lo calcula Django, fire-and-forget, limpia la sugerencia tras reportar). Cierra al ver el primer delta real capturado. **(2) 🟢 IMPLEMENTADO (Django) 2026-06-14** — `SugerenciaAprendizaje` + clasificador LLM (`aprendizaje.clasificar`, temp 0, compara borrador vs enviado + catálogo + conocimiento → hecho_catalogo/regla/tono/puntual; solo crea sugerencia para los 2 accionables) + comando `procesar_aprendizaje` (procesa `AgenteFeedback.editado&!procesado`, no bloquea inbound, reintenta si LLM falla) + endpoints `GET sugerencias-aprendizaje` / `POST <id>/aprobar` (regla→append a `conocimiento`; hecho_catalogo→marca para aplicar a mano) / `POST <id>/descartar`. Migración `0006` (1 tabla, FK solo a AgenteFeedback, sin trampa cruzada). 15/15 tests + check + smoke OK. **Clasificador validado en prod 2026-06-14** (`probar_clasificador`): precio→hecho_catalogo (+ref_catalogo para Jorge); "Tui-Na no online"→regla (texto listo p/ Conocimiento); saludo personalizado→puntual (sin sugerencia). Deploy + `migrate 0006` aplicados. Falta: cron `procesar_aprendizaje` (o manual) + UI aremko-cli "🧠 El agente aprendió algo" (endpoints listos). **(3) métricas** `GET /agente/metricas?semanas=N` (de `AgenteFeedback`) pendiente. Ver `RESPUESTA_H-010` | 2026-06-14 (agente Django) |

| H-011 | **Disponibilidad real del agente** (a pedido directo de Jorge): que el agente conteste "¿tienen tinas el sábado para 4?" con horarios reales del calendario, en vez de mandar a la web. Flujo: capacidad (filtra servicios) + fecha → horarios libres. **Fase A:** una categoría/tipo a la vez. **Fase B (después):** tina+masaje coordinados (orden y dependencia de lo ya agendado). | Django (servicio de disponibilidad + tool del agente) + aremko-cli (nada en Fase A; se ve en el borrador) | 🟠 EN PROGRESO (Django, Fase A) 2026-06-14 — **Paso 1 ✅ + complementos ✅** validados en prod por Jorge. (a) `whatsapp_agent/availability.py` `disponibilidad(fecha, personas, tipo)` reúsa el motor existente (`extraer_slots_para_fecha` + `verificar_disponibilidad` + bloqueos); filtra `publicado_web`+`activo`+capacidad. (b) **Principal vs complemento:** `WhatsAppAgentConfig.servicios_complementarios` (M2M, en la app del agente, NO toca `Servicio` → sin outage/drift); grounding y disponibilidad EXCLUYEN complementos; comando `marcar_complementos` (pre-llenó 7: 4 decoraciones + Yates $0 + 2 tinas niño). Borrador validado: "tenemos tinas… ¿para cuántas personas y qué día?" sin ofrecer complementos ni empujar la web. Comandos `probar_disponibilidad` y `probar_agente_wa --catalogo`. ⚠️ La M2M cruzada hizo que `makemigrations` volcara el drift en un `ventas/0125` (BORRADO); `0005` re-apuntada a `ventas.0124`. **Paso 2 ✅ tool-calling** (`generate_with_tools` + tool `consultar_disponibilidad` + fecha de hoy inyectada al prompt). La tool calcula `precio_total` = base × personas (TODOS los servicios son por persona: tina/masaje/cabaña — confirmado por Jorge) para que el modelo no haga aritmética. **Fase A CERRADA, validada en prod 2026-06-14:** "Tina Calbuco hoy 14:30 para 4 — 4 horas por $100.000 total. ¿Te late?" (disponibilidad real + capacidad + duración + precio por persona correcto + sin complementos + sin empujar web). **Fase B pendiente:** tina+masaje coordinados (orden + dependencia de lo ya agendado). **Refinamiento 2026-06-14 (Django):** (a) preguntas de SOLO PRECIO sin fecha ahora dan el total (`disponibilidad` con fecha opcional → la tool devuelve `precio_total` aunque no haya fecha; el LLM no hace aritmética); (b) cabañas se expresan "por noche", no en horas (grounding + `duracion_texto`). Confirmado: catálogo de cabañas correcto (por persona) — las sugerencias `hecho_catalogo` de "cabañas a mitad" eran falsos positivos (Jorge las descarta). Ver `RESPUESTA_H-011` | 2026-06-14 (agente Django) |
| H-012 | **Automatizar la salida de la Bandeja WhatsApp con plantillas:** el flujo manual de Deborah se rompió con Cloud API (iniciar requiere plantilla aprobada). La segmentación de la Bandeja genera **envíos de plantilla pendientes** (motivo→plantilla aprobada+variables) → **aprobación de Deborah** (lote) → `run-template-campaign` envía → la plantilla abre la ventana 24h → la atiende el agente. Reusa `run-template-campaign`+`pending-template-sends`+plantillas `vac_*` | Django (segmentación→envíos+estado+endpoints) + aremko-cli (UI de aprobación + proxies) | 🟡 🟢 MECANISMO IMPLEMENTADO (Django) 2026-06-14 — gate de aprobación reusando el motor existente (cron diario P0-P6 + ScriptWhatsApp.meta_template + pending-template-sends + run-template-campaign). Nuevo estado `ContactoWhatsApp='aprobado'` (entre pendiente=por-aprobar y enviado; string en CharField → SIN migración). `pending-template-sends` ahora devuelve `aprobado` (nada se envía hasta aprobar; cron sin cambios). Endpoints: `GET /api/whatsapp/bandeja-envios`, `POST .../<id>/aprobar`, `.../<id>/descartar`, `.../aprobar-lote` ({ids}|{motivo}). Comando `scripts_para_mapeo` (lista simple→complejo p/ que Jorge calce cada `vac_*`). Admin ScriptWhatsApp ya editable. check+smoke OK. UI aremko-cli `f4eb73f` ("Envíos por aprobar" + Aprobar/Descartar/lote + "Enviar aprobados" vía endpoint Go abierto detrás del login). **Mapeo APLICADO 2026-06-14**: las plantillas Meta `vac_*` siguen el patrón del script_id (A.6→vac_a_6, etc.; excepción Leal/Campeón→vac_e_mesachica_<s|n|sc>) → comando `mapear_plantillas_vac --aplicar` mapeó los 34 scripts (meta_template_name + meta_variables_orden derivado del cuerpo). **✅ VALIDADO end-to-end 2026-06-14:** Jorge aprobó + "Enviar aprobados" → la plantilla (C.1 `vac_c_1`) se envió por Cloud API y quedó registrada en "Mensajes WhatsApp" (saliente 'sent'); el sistema muestra el aviso de ventana 24h. Flujo completo OK: segmentación→plantilla→aprobación→envío→bandeja; al responder el cliente, entra el agente IA. Pendiente menor: confirmar que `vac_b_2`/`vac_c_2`/`vac_c_3` (salvas 2/3) existan en Meta (no estaban en las capturas); arrancar con volumen bajo por el tier. Ver `RESPUESTA_H-012` | 2026-06-14 (agente Django) | aremko-cli) |
| H-013 | **Botón "Procesar aprendizaje" en la UI** (Jorge lo prefiere manual, no cron, para ir viendo qué corrige el agente y dónde): exponer `procesar_aprendizaje` como endpoint `POST /api/whatsapp/agente/procesar-aprendizaje` (lote acotado ~50, responde <60s) → el botón lo dispara, muestra resumen y refresca pendientes. + vista **Historial** (reusa `sugerencias-aprendizaje?estado=`) con problema/cambio/dónde | Django (endpoint) + aremko-cli (botón + resumen + historial) | 🟡 🟢 IMPLEMENTADO (Django) 2026-06-14 — `POST /api/whatsapp/agente/procesar-aprendizaje` (luna-key) llama `procesar_pendientes(50)` (lógica del comando refactorizada, idempotente, lote ~50) → `{ok, procesados, creadas, errores, resumen}`. 15/15 tests + check + smoke OK, sin migración. Falta: deploy + botón/Historial aremko-cli. aremko-cli sube el timeout del proxy a ~60s para esta llamada | 2026-06-14 (agente Django) |
| H-014 | **Excluir servicios complementarios al segmentar/redactar la campaña de la Bandeja** (pedido de Jorge 2026-06-15): en prod aparecieron mensajes que mencionan/segmentan por un servicio COMPLEMENTARIO (vistos: "Tina Hidromasaje Niño", "Tina Normal Niño") en vez del PRINCIPAL que contrató el cliente (ej. agregó 1-2 "niños" a su Tina Llaima → el mensaje debe ser por la Llaima). También excluir "Tina de Agua Fría Yates". La campaña (`ventas/management/commands/generar_bandeja_whatsapp_diaria.py` + taxonomía + render `ctx`) debe **determinar el servicio principal EXCLUYENDO** `WhatsAppAgentConfig.servicios_complementarios` (lista que YA existe de H-011: 4 decoraciones + Yates + 2 tinas niño; el grounding/disponibilidad ya la excluyen). Si tras excluir no hay principal calificable → no generar envío (sin-script). No usar scripts/plantillas basados en un complemento. Confirmar que los 3 nombrados estén en la M2M. | Django (segmentación + taxonomía + ctx; reusa M2M de `whatsapp_agent`, idealmente sin migración) + aremko-cli (nada, solo muestra/envía) | 🔵 🟢 IMPLEMENTADO (Django) 2026-06-15 — la campaña excluye complementos en 2 puntos, reusando la M2M `WhatsAppAgentConfig.servicios_complementarios` (sin migración): (1) `obtener_ultimo_servicio_nombre` (bandeja_whatsapp_service.py) → el `{ultimo_servicio}` del mensaje ahora es el PRINCIPAL (`.exclude(servicio_id__in=ids_complementarios())`); (2) `recalcular_taxonomia_clientes` excluye complementos del mix → `pct_tinas/masajes/cabañas` (y por ende `eje_estilo` + `servicio_recomendado`) limpios. `calcular_servicio_recomendado` ya devuelve frases genéricas (no nombra complementos). check + smoke (sin ciclo de imports) OK. La taxonomía se recalcula en su cron (05:30) antes de la bandeja (06:00) → la próxima corrida sale limpia. **Hallazgo en validación:** un mensaje mencionaba "Ambientación romántica R1" (complemento NO marcado — `marcar_complementos` solo captaba "Decoración…" por categoría exacta). Se amplió el heurístico (nombre icontains ambientaci/decoraci, aunque no estén publicadas) y Jorge re-corrió `marcar_complementos --aplicar` 2026-06-15 (lista de complementos ampliada). Deploy hecho. Falta: descartar los envíos de HOY ya generados con el bug + validar la próxima corrida del cron (debe salir sin complementos). Ver `RESPUESTA_H-014` | 2026-06-15 (agente Django) |

| H-015 | **Disponibilidad de PACKS multi-servicio para Luna** (pedido directo de Jorge): subir de "disponibilidad de 1 servicio" (H-011) a componer ITINERARIOS. Roadmap: **Nivel 1** tina+masaje (mismo día) → **Nivel 2** cabaña+tina → **Nivel 3** cabaña+tina+masaje. Cada nivel se construye/valida antes del siguiente. | Django (composición en el agente) + aremko-cli (solo muestra el borrador) | ✅ CERRADO Nivel 1 (Django) 2026-06-15 — diseño en `docs/BRIEF_H-015_disponibilidad_packs_agente.md`. **Criterio clave Nivel 1 (Jorge):** la masajista viene de otra ciudad → el masaje nuevo se agenda **lo más cerca posible de un masaje ya reservado ese día** (minimizar hueco muerto; a veces antes, a veces después de la tina). No solapar tina/masaje. Precio: tina+masaje por persona − `PackDescuento`. **Nivel 1 VALIDADO en prod 2026-06-15** (`whatsapp_agent/packs.py` `disponibilidad_pack_tina_masaje` + tool `consultar_disponibilidad_pack` + comando `probar_pack`). Capacidades cerradas: (1) ofrece **2 alternativas** — tina CON hidromasaje (gama mayor) y SIN hidromasaje (más económica) — para que el cliente elija sin pregunta extra; (2) **descuento de pack** aplica de verdad: el carrito usa el masaje del propio pack (**Masaje Relajación o Descontracturante, id 53**) en vez de uno cualquiera → el motor lo cuenta (antes "Drenaje Linfático" daba "Total masajes:0" y $0 de descuento); (3) muestra precio normal + con pack (dom-jue); (4) **upsell determinístico**: si ninguna opción trae descuento (fin de semana), el tool devuelve `nota_upsell` y Luna ofrece cotizar un día entre semana (NO se confía la condición al modelo — Haiku se la saltaba); (5) **clustering validado contra la matriz real** (masaje pegado al bloque de masajes ya agendados); (6) **no ofrece horas pasadas** + margen de viaje de masajista (~1h) si no hay masajistas en sitio hoy; (7) solo se agenda el masaje **Relajación/Descontracturante** (los demás → derivar a persona); (8) explicita los horarios libres por servicio. Commits `a1f8e83`→`f5dd0e7`, sin migración. **Aprendizaje reforzado:** lo determinístico (disponibilidad, descuento, upsell) lo calcula el CÓDIGO; el modelo solo conversa — cambiar de modelo (deepseek etc.) no arregla cálculos. **✅ CERRADO Nivel 2 (cabaña+tina) 2026-06-15, VALIDADO en prod** (`whatsapp_agent/packs.py` `disponibilidad_pack_cabana_tina` + tool `consultar_disponibilidad_pack_cabana` + comando `probar_pack_cabana`): cabañas SIEMPRE 2 pax (Luna lo explicita); lista las cabañas libres esa noche; **tina en el horario MÁS TARDE disponible ≥16:00** (regla fundamental de Jorge, con o sin hidromasaje); **check-in 16:00 / check-out 11:00** del día siguiente; descuento **$30.000 dom-jue** vía pack 9 (validado por TIPO `es_pack_alojamiento_tina`, no por servicio específico → no hubo el problema de routing de Nivel 1); **desayuno por cabaña $20.000 para dos** (~10:00 día siguiente, mapeado por nombre: Cabaña Torre→Desayuno Torre, despublicados) que Luna ofrece SOLO si el cliente pregunta (nunca "$10.000 por persona"); upsell determinístico fin de semana/martes. Validado: Arrayan/Tepa $140.000→$110.000, Torre $150.000→$120.000. 1 noche por ahora. Commits `159cdb4` (+ refuerzo "para 2"), sin migración. **Nivel 3 (cabaña+tina+masaje) pendiente** (definir dónde cae el masaje: por la tarde tras la tina, o la mañana siguiente; aplica la regla de cercanía a masajistas de Nivel 1). Ver brief | 2026-06-15 (agente Django) |

| H-016 | **Persistencia de Instagram DM en la bandeja (omnicanal, Fase 2)** (pedido de Jorge 2026-06-16): sumar **Instagram DM** al inbox de WhatsApp. Fase 0 (Meta) y Fase 1 (webhook Go `/api/v1/instagram/webhook` recibe+loguea DMs) ✅ hechas por aremko-cli. Falta el cimiento: **guardar** los DMs de IG y exponerlos en la bandeja igual que un WhatsApp. Clave: la identidad NO es teléfono sino **IGSID** → conversación keyeada por `(canal, external_id)`, `Cliente` **opcional** (un DM puede no matchear ningún cliente). IG es REACTIVO (ventana 24h, sin plantillas/campañas → NO replicar H-012/H-014). Alcance H-016 = inbound (guardar) + reads channel-aware (lista+hilo+marcar-atendido con campo `canal`); outbound/responder = H-017, adjuntos = Fase 5. Precedente en el repo: `destino_puerto_varas.LeadConversation(channel, external_id)`. Constraint AR-034: `ventas.WhatsAppMessage` está congelado → app aislada drift-safe recomendada. Contrato inbound + reads detallado en `docs/BRIEF_H-016_instagram_dm_persistencia.md`. aremko-cli enganchará Go (POST inbound + resolver IGSID→@username) + front (ícono+canal) según el contrato que defina Django. | Django (modelo + inbound + reads channel-aware) + aremko-cli (Go dispatch + front, después) | 🔵 INTEGRADO 2026-06-16 (Django ✅ + aremko-cli ✅, ambos en prod — falta validación visual de Jorge). App aislada drift-safe `inbox_omnicanal` (`ChannelMessage`, `0001_initial` SIN tocar `ventas`; `migrate inbox_omnicanal` aplicado en Render; `makemigrations --check` limpio). Commit `548bd5b`. **CONTRATO FINAL (cableado y verificado, 401 sin key):** (1) inbound `POST /api/instagram/inbound` header `X-API-Key: LUNA_API_KEY`, body `{ig_message_id, from_igsid, to_igsid, text, timestamp, contact_name, is_echo}` — idempotente por `ig_message_id`; `is_echo:true`→saliente; conversación = IGSID del cliente (el que NO es la cuenta `17841400756478364`); responde `{ok, message_id, canal, external_id, direction, requiere_atencion, duplicate}`. (2) Lista unificada `GET /api/inbox/conversations/?limit=&solo_pendientes=&canal=` → `conversations[]` con `canal, external_id, phone(WA|null), cliente_id, cliente_nombre, contact_name, ultimo_mensaje, ultimo_direction, ultimo_timestamp, sin_responder, requiere_atencion, total_mensajes`; pendientes-primero cruzando WhatsApp+Instagram. (3) Hilo `GET /api/inbox/conversation/?canal=instagram&external_id=<igsid>` (WA: `?canal=whatsapp&external_id=<phone>` o `?phone=`) → `messages[]` con `external_message_id, canal, direction, body, type, status, timestamp` (+`sugerencia_agente` solo WA con `?sugerencia=1`). (4) `POST /api/inbox/conversations/<canal>/<external_id>/marcar-atendido/`. Conviven con `/api/whatsapp/*` legacy. Validado con `probar_inbox --simular` (agregado/detalle/hilo OK, rollback) + 401 en ambos endpoints. **aremko-cli HECHO (commit `c6b51eb`):** `handleInstagramEvent`→POST inbound (idempotente; is_echo→saliente; normaliza timestamp ms→s); proxy Go `/api/v1/inbox/{conversations,conversation,conversations/<canal>/<external_id>/marcar-atendido}`; front migrado a bandeja unificada con ícono por canal (WhatsApp responde como siempre; **Instagram solo-lectura**, responder = H-017). **Validado end-to-end en prod 2026-06-16:** POST webhook IG sintético en formato `messaging[]` (el de DMs reales, no el `changes[]` del simulador) → persistido con `canal:instagram` keyeado por IGSID + `phone:null` → visible en `GET /api/inbox/conversations` + `marcar-atendido` OK (requiere_atencion→false). **H-017 (responder IG) HECHO por aremko-cli (commit `f37a59b`) SIN endpoint outbound nuevo:** el Go envía el DM vía `graph.instagram.com` y el saliente se persiste vía el webhook ECO de Meta (`is_echo:true`→`/api/instagram/inbound`). Django suma **clear-on-echo (commit `62b95e0`):** un eco saca la conversación de pendientes (limpia `requiere_atencion` de sus entrantes, paridad con el outbound de WhatsApp); el inbound devuelve `pendientes_limpiados`; helper compartido con marcar-atendido. **Registro de prueba `918273645091827` BORRADO** (2 filas). Validado en prod con `probar_inbox --simular` (clear-on-echo: req 1→0). Outbound IG validado contra Meta (token OK) por aremko-cli; falta DM real con cuenta tester + `INSTAGRAM_ACCESS_TOKEN` en Render para `@username`/H-017 e2e. **Validación VISUAL de Jorge OK en prod 2026-06-16:** bandeja unificada mostrando WhatsApp (💬) e Instagram (📷) mezclados (100 convos), @usuarios resueltos (no IGSID crudo) incluso con último mensaje = eco "Tú:" (H-018 echo-proof confirmado con datos reales), badges de pendientes por canal, respuestas "Tú:" persistidas vía eco. **H-016/H-017/H-018 CERRADOS.** Único pendiente futuro: `INSTAGRAM_ACCESS_TOKEN` en Render para H-017 e2e con cuenta tester (al arrancar esa parte). Ver también H-018. | 2026-06-16 (agente Django) |

| H-018 | **Mostrar el nombre del cliente en conversaciones de Instagram** (pedido de Jorge 2026-06-16): en la bandeja, varias convos de IG muestran el IGSID crudo en vez del nombre. aremko-cli YA resuelve el nombre (display name / @usuario vía Graph API) y lo manda en `contact_name` en cada inbound no-eco, pero parece guardarse **solo al crear** → las convos viejas (cuando `contact_name` iba vacío) no se actualizan. **Pedido (alcance "solo A", SIN backfill):** en el inbound de Instagram (`inbox_omnicanal`), cuando llega `contact_name` no vacío, **ACTUALIZAR** el nombre del contacto/conversación (no solo set-once). Así se llenan solos cuando el cliente vuelve a escribir. No pisar con vacío; aplica al contacto del cliente (IGSID ≠ `17841400756478364`). Detalle en `docs/BRIEF_H-018_instagram_actualizar_nombre.md`. | Django (fix en read de la lista) + aremko-cli (nada nuevo: ya manda contact_name, commit `a8eb2cc` prefiere display name) | ✅ VALIDADO EN PROD 2026-06-16 (commit `5084e61`) — **root cause real distinto al hipotetizado:** `contact_name` SÍ se guarda en cada mensaje (no es set-once); el bug estaba en el READ de la lista (`_detalle_instagram`), que tomaba el `contact_name` del mensaje MÁS RECIENTE — que suele ser un ECO saliente sin nombre → mostraba vacío → caía al IGSID. **Fix:** la lista usa el último `contact_name` NO vacío de la conversación (el hilo ya lo hacía). Echo-proof y cubre cambios de nombre; se llena solo cuando el cliente vuelve a escribir, sin backfill ni write nuevo. Validado con `probar_inbox --simular`: eco reciente SIN nombre → la lista igual muestra `@cliente_prueba`. aremko-cli no cambió nada (ya manda `contact_name`). | 2026-06-16 (agente Django) |

| H-019 | **Borrador de IA (agente Luna) para conversaciones de Instagram** (pedido de Jorge 2026-06-16): el agente H-007 da borradores "✨ sugerido por IA" en WhatsApp pero NO en Instagram (el contrato H-016 lo dejó "solo en WA"). Pedido: que `GET /api/inbox/conversation/?canal=instagram&external_id=<igsid>&sugerencia=1` devuelva `sugerencia_agente` con el MISMO shape que WA (`{texto, escalar, motivo, modo, modelo, error, generada_at, responde_a}`), generado por el mismo `whatsapp_agent` con el historial de la conversación IG. Reusar grounding/config/escalamiento/anti-injection; lazy (solo con `&sugerencia=1`, no en el auto-refresco); modo borrador (no auto-responder). Matiz: IG no tiene teléfono → sin ficha del cliente por teléfono (grounded en catálogo igual). aremko-cli reenvía `&sugerencia=1` por el proxy del inbox y muestra el borrador en `ConversacionInstagram` (lo hace en este ciclo). Detalle en `docs/BRIEF_H-019_agente_ia_instagram.md`. | Django (agente para IG en `inbox/conversation`) + aremko-cli (proxy + UI, en curso) | ✅ INTEGRADO Y VALIDADO EN PROD 2026-06-16 (commit Django `d767cc4` + aremko-cli `8e842c7`) — `inbox/conversation` (canal=instagram, `&sugerencia=1`) devuelve `sugerencia_agente` con el mismo shape WA reusando `whatsapp_agent._producir_borrador` (grounding/escalamiento, agnóstico de teléfono) + historial de `ChannelMessage`. Lazy, gate por `config.activo`, solo si hay entrante IG pendiente; sin caché ni persistencia (v1). **Validación visual de Jorge:** en la conversación IG `@martin_aguilerat` aparece el banner "✨ Borrador sugerido por IA — revísalo antes de enviar" con borrador grounded ("¿Buscas tina, masaje, cabaña o algo para comer?"). `probar_inbox --sugerencia <igsid>` para diagnosticar. v1 sin saludo adaptativo IG ni token-tracking (optimizaciones futuras). | 2026-06-16 (agente Django) |
| H-020 | **Adjuntos de Instagram (Fase 5) — igual que WhatsApp** (pedido de Jorge 2026-06-16): que las fotos/audios/videos de un DM de IG se guarden y se vean en la bandeja, replicando el flujo de media de WhatsApp (download del media temporal de Meta → Cloudinary → mostrar en el hilo). | Django (modelo + inbound-media + media en reads) ✅ + aremko-cli (Go: download+upload, en su lado) | 🔵 INTEGRADO 2026-06-16 (Django ✅ + aremko-cli ✅, ambos en prod — falta validar un adjunto real con Jorge). **aremko-cli HECHO (commit `4d30e7a`):** `instagram.DownloadMedia` baja los bytes del CDN temporal de IG (tope 16 MB, sin token: la URL es pre-firmada); `handleInstagramEvent` rutea los DMs con adjunto a `handleInstagramMedia`, que sube cada uno vía `POST /api/instagram/inbound-media` (multipart, espejo de `/api/whatsapp/inbound-media`); el texto del mensaje va como `caption` del 1er adjunto; `is_echo`→saliente; múltiples adjuntos → `ig_message_id` con sufijo `#i` para no chocar idempotencia. Front: `MensajeInbox` suma `media_url/mime_type/filename` y `ConversacionInstagram` renderiza foto/video/audio/documento (mismo patrón que WA). Outbound de media IG = fase posterior. Detalle Django abajo. `ChannelMessage` suma `media_file` (Cloudinary RAW, `upload_to='instagram/'`), `mime_type`, `original_filename` (migración `0002`, patrón SeparateDatabaseAndState + `ADD COLUMN IF NOT EXISTS`; columnas pre-agregadas → deploy sin gap; `migrate inbox_omnicanal` aplicado; `--check` limpio). **CONTRATO `POST /api/instagram/inbound-media`** (multipart/form-data, header `X-API-Key: LUNA_API_KEY`): campos `ig_message_id` (req), `from_igsid` (req), `to_igsid`, `is_echo`, `type` (image/video/audio/voice/share/story_mention/sticker/document; default document), `caption`, `mime_type`, `filename`, `contact_name`, `timestamp`, y `file` (los bytes, req). Idempotente por `ig_message_id`; `is_echo:true`→saliente + limpia pendientes; conversación = IGSID del cliente; tope 16 MB (`WHATSAPP_MEDIA_MAX_BYTES`); nombre UUID; mismo storage RAW que WA. Responde `{ok, message_id, canal, external_id, direction, requiere_atencion, pendientes_limpiados, media_url, duplicate?}`. **Reads:** el hilo (`/api/inbox/conversation/`) ya devuelve por mensaje `media_url`, `mime_type`, `filename`; la lista muestra "📷 Foto"/"🎤 Nota de voz"/etc. cuando el DM es solo adjunto. Validado: `--check` limpio, `migrate` OK, reads sanos (39 msgs), 401 en el endpoint. **aremko-cli ahora:** en `handleInstagramEvent`, si el DM trae adjunto, bajar los bytes del media temporal de IG (con el token) y POSTear acá multipart (igual que hace con `/api/whatsapp/inbound-media`); el front ya muestra media en el hilo (reusa el patrón WA). Outbound de media IG = fase posterior. | 2026-06-16 (agente Django) |

| H-021 | **Módulo de Métricas / "Tablero de Evolución"** (pedido de Jorge 2026-06-16): tablero que mide la EVOLUCIÓN (series semanales) de WhatsApp+Instagram, Conexión-Masajes, aprendizaje del agente y Envíos por aprobar, con foco comercial (¿la bandeja vende?) y de gestión. MVP = 4 endpoints de agregación read-only sobre data EXISTENTE: (1) `/api/metrics/campanas` funnel + **COSTO de plantillas** (enviados × tarifa marketing CLP configurable) + ROI neto/ROAS + ingreso atribuido (reserva_atribuida 30d); (2) `/api/metrics/agente` curva % borradores sin editar (de `AgenteFeedback`) + escalados + aprendizajes + tiempo ahorrado estim.; (3) `/api/metrics/canales` volumen por canal + 1ª respuesta mediana + backlog; (4) `/api/metrics/masajes` cobertura+respuesta (`SeguimientoBienestarMasaje`). Tarifa de plantilla configurable (env `WHATSAPP_TEMPLATE_COST_CLP` o config); si no, costo=null. Fase 2 (otro H): atribución inbound conversación→reserva + costo real de Meta vía API facturación. Contrato detallado en `docs/BRIEF_H-021_modulo_metricas.md`. aremko-cli arma la página "Métricas/Evolución" (proxy Go `/api/v1/metrics/*` + charts, patrón Campañas Meta), construyéndola contra este contrato. | Django (4 endpoints agregación) + aremko-cli (página + proxy + charts) | 🔵 INTEGRADO 2026-06-16 (Django ✅ + aremko-cli ✅, ambos en prod — falta validación visual de Jorge). **aremko-cli HECHO (commit `9794de7`):** proxy Go `GET /api/v1/metrics/{tipo}` → Django; nueva sección "Métricas / Evolución" (`permissions.ts` key `evolucion` + Sidebar + página `perfiles/evolucion`) que llama los 4 endpoints en paralelo y grafica con recharts: cinta de KPIs, funnel + ROI de campañas (muestra "configura tarifa" si `tarifa_plantilla_clp` null → costo/roi en null), curva del agente (% sin editar + % escalado), volumen WhatsApp vs Instagram, cobertura Conexión-Masajes (tasa de respuesta = "no disponible"). Tolerante a null. **Pendiente:** Jorge setea la tarifa en el admin para activar costo/ROI + validación visual. 4 GET read-only, `X-API-Key: LUNA_API_KEY`, `weeks` (default 12, máx 52), series ISO ("YYYY-Www"). **Decisiones vs brief:** (a) **tarifa = campo `tarifa_plantilla_clp` en `WhatsAppAgentConfig`** (editable en ADMIN, sección "Costos / Métricas"), NO env; migración `0007` SeparateDatabaseAndState sin downtime; 0=sin configurar → `costo_estimado`/`roi`/`roas`/`series.costo` en `null`. (b) **4 endpoints separados**. (c) ⚠️ `masajes.tasa_respuesta_pct: null` + `nota`: `SeguimientoBienestarMasaje` NO registra la respuesta del cliente (solo envío) → campo nuevo en Fase 2; `cobertura_pct`=enviados/programados. **Shapes (validados con data real):** `campanas`: resumen `{generados,aprobados,enviados,respondieron,reservaron,ingreso_atribuido,costo_estimado,roi_neto,roas}` + `tarifa_plantilla_clp` + series `{semana,enviados,costo,respondieron,reservaron,ingreso}`. `agente`: resumen `{pct_sin_editar,delta_pts_8sem,aprendizajes_aprobados,tiempo_ahorrado_min_estim}` + series `{semana,total,sin_editar,pct_sin_editar,escalados,pct_escalado}`. `canales`: resumen `{backlog_actual,primera_respuesta_mediana_min}` + series `{semana,whatsapp,instagram,primera_respuesta_mediana_min}`. `masajes`: resumen `{cobertura_pct,tasa_respuesta_pct(null)}` + `nota` + series `{semana,programados,enviados,cobertura_pct}`. Validado: 401 sin key, `--check` limpio, `migrate 0007` OK, los 4 devuelven data real (campañas 635→370 enviados/$138k atribuido; agente 51% sin editar; canales 1ª resp 1 min; masajes 54% cobertura). **Pendiente operativo:** Jorge setea la tarifa en el admin para activar costos. Fase 2 (otro H): atribución inbound conv→reserva + costo real Meta. | 2026-06-16 (agente Django) |

| H-022 | **Métricas: drill-down de reservas atribuidas + funnel por semana** (pedido de Jorge 2026-06-16, sobre el tablero H-021): en el bloque Campañas, poder LISTAR las reservas atribuidas (cliente, nº reserva, servicios con cantidad y valor, fecha, total) para dar transparencia al "$138.000 / 3 reservas"; y que el selector de tiempo permita ver una **semana puntual**. (1) Nuevo `GET /api/metrics/campanas/reservas?weeks=&semana=` → detalle de cada `ContactoWhatsApp.reserva_atribuida` (la VentaReserva con sus ReservaServicio/Producto). (2) Agregar `generados`+`aprobados` a cada fila de `campanas.series` para armar el funnel de una semana desde la serie. Sin migración (lectura). aremko-cli: proxy Go + selector de semana + tabla de reservas. Detalle en `docs/BRIEF_H-022_metricas_drilldown_reservas.md`. | Django (endpoint reservas + 2 campos en serie) + aremko-cli (selector semana + tabla) | 🟦 SOLICITADO 2026-06-16 | 2026-06-16 (agente aremko-cli) |

| H-023 | **Persistencia de Facebook Messenger en la bandeja (omnicanal)** (pedido de Jorge 2026-06-16): sumar Messenger igual que Instagram. Fase 0 (Meta) + Fase 1 (webhook Go `/api/v1/messenger/webhook`, mismo formato Messenger Platform que IG, object `page`) ✅ hechas por aremko-cli. Identidad = **PSID**, Página de Aremko `555157687911449`, canal REACTIVO (marketing pagado diferido). Pedido: (1) `POST /api/messenger/inbound` espejo de `/api/instagram/inbound` con `canal='messenger'` (body `{fb_message_id, from_psid, to_page_id, text, timestamp, contact_name, is_echo}`, idempotente, conversación = PSID del cliente, is_echo→saliente+clear-on-echo); (2) confirmar que los reads `/api/inbox/*` ya soportan `canal='messenger'` (modelo channel-agnostic) — lista unificada + hilo `?canal=messenger&external_id=<psid>` + marcar-atendido. Sin migración si `canal` es string libre (si es choices, agregar 'messenger'). Mirror de H-016. aremko-cli: conectar `handleMessengerEvent`→POST inbound + front ícono de canal. Detalle en `docs/BRIEF_H-023_messenger_persistencia.md`. | Django (inbound messenger + reads) + aremko-cli (Go dispatch + front) | 🟩 INTEGRADO 2026-06-17 — Django: `POST /api/messenger/inbound` vivo + reads `?canal=messenger` OK; aremko-cli: `handleMessengerEvent`→PostMessengerInbound (commit 4f8715c) + bandeja con canal messenger (ícono azul + hilo solo-lectura, commit eb9d8f4). Pendiente fase posterior: responder + nombres (Graph API Page token) + adjuntos, espejo de IG H-017/H-018/H-020. **AMPLIADO 2026-06-18:** responder Messenger ✅ VALIDADO end-to-end (aremko-cli `2b679ab`, `POST /api/v1/messenger/reply` + compositor; saliente vía eco, sin endpoint outbound django); nombre del PSID confirmado bloqueado por App Review (Meta 400 code 100 subcode 33, no es bug). | 2026-06-17 (agente aremko-cli) |
| H-024 | **Adjuntos entrantes de Messenger (foto/video/audio/documento)** (pedido de Jorge 2026-06-18): mirror de H-020 (IG adjuntos). Pide endpoint **`POST /api/messenger/inbound-media`** espejo de `/api/instagram/inbound-media` con `canal='messenger'` — multipart `{fb_message_id, from_psid, to_page_id, is_echo, type(image|video|audio|file), timestamp, contact_name, caption, mime_type, file≤16MB}`, idempotente por `fb_message_id`, conversación = PSID del cliente (misma resolución del inbound texto Messenger H-023), is_echo→saliente, guarda media_file(Cloudinary RAW)/mime_type/original_filename, body=caption. aremko-cli (Go `messenger.DownloadMedia`+`PostMessengerInboundMedia`+`handleMessengerMedia`, commit `8fd3d14`) + front (sin cambios, `ConversacionInstagram` ya renderiza media genérica) **YA HECHO y desplegado**; hoy loguea `error subiendo adjunto a Django` (404) hasta que exista el endpoint. Detalle en `docs/BRIEF_H-024_messenger_adjuntos.md`. | Django (endpoint inbound-media messenger) | 🟩 INTEGRADO 2026-06-18 — endpoint `/api/messenger/inbound-media` creado; validado con foto real (se renderiza en el hilo). | 2026-06-18 (agente aremko-cli) |
| H-025 | **Biblioteca de medios en la bandeja** (pedido de Jorge 2026-06-18): que el botón adjuntar abra una galería con las fotos del catálogo del sitio (tinas/cabañas/masajes) para enviarlas rápido desde el celular, sin buscar en el teléfono. Pide endpoint read-only **`GET /api/inbox/media-library`** (X-API-Key) que devuelva los Servicios `publicado_web=True` de tipo tina/cabana/masaje agrupados, con sus fotos (`imagen`/`imagen_2`/`imagen_3` no vacías) + video (`video`/`video_url`). Shape en `docs/BRIEF_H-025_biblioteca_medios_bandeja.md`. aremko-cli: proxy + envío (descarga la URL y reenvía por el canal, sin endpoint outbound nuevo) + galería en el front (miniaturas agrupadas + buscador, los 3 canales). | Django (endpoint media-library) | 🟩 INTEGRADO 2026-06-18 — `GET /api/inbox/media-library` vivo con datos reales; galería validada por Jorge ("se ve fantástico"). | 2026-06-18 (agente aremko-cli) |
| H-026 | **Borrador de IA (Luna) en Messenger** (pedido de Jorge 2026-06-18): que el agente sugiera respuesta en Messenger, espejo de H-019 (Instagram). Pide en `inbox_omnicanal/views.py`: `_historial_messenger` + `_contexto_saludo_messenger` + `_sugerencia_messenger` (idénticos a los `_*_instagram` pero `canal='messenger'`) y agregar `sugerencia_agente` (lazy, opt-in `&sugerencia=1`) a la rama `canal=='messenger'` de `conversation()`. Sin migración. Detalle en `docs/BRIEF_H-026_borrador_ia_messenger.md`. Front aremko-cli ya pide `&sugerencia=1` en Messenger + banner azul, tolerante a null. | Django (sugerencia messenger) | 🟩 INTEGRADO 2026-06-18 — Django agregó `_sugerencia_messenger`; Jorge validó el borrador en Messenger. (aremko-cli sumó fix de alto del textarea `8a1ec61`.) | 2026-06-18 (agente aremko-cli) |
| H-027 | **Disponibilidad de alojamiento multi-noche para Luna** (pedido de Jorge 2026-06-18, diseñado con aremko-cli). Continuación de H-011. Que el agente responda estadías de varias noches: (1) desambiguar nº de noches ANTES de consultar (ej. "23 y 24" → ¿1 o 2 noches?); (2) tool `consultar_disponibilidad_alojamiento(llegada, salida, personas)` que devuelve cabañas libres TODAS las noches (misma cabaña, intersección por noche), `{nombre, total_por_noche, noches, total_estadia}` SIN exponer precio por persona; (3) precio = `precio_por_persona × personas × noches` (cálculo por persona SE MANTIENE) pero Luna SOLO muestra total por noche + total estadía, NUNCA por persona; (4) máx 2 pax; (5) martes cerrado NO aplica a cabañas. Alcance: solo Luna (chat). Sin migración esperada. Preguntas para Django: cómo se almacena la ocupación de cabaña por noche (ReservaServicio por noche vs rango). Detalle en `docs/BRIEF_H-027_disponibilidad_alojamiento_multinoche.md`. | Django (tool + lógica disponibilidad multi-noche) | 🟩 INTEGRADO 2026-06-18 — tool con `noches` (cálculo de salida en código, fix del bug "2 noches→1"); validado por Jorge (25→30 jul = 5 noches $450k). Refinamiento: tope de 2 opciones en código (la regla blanda en prompt no bastó con Haiku) — SOLICITADO. | 2026-06-18 (agente aremko-cli) |
| H-028 | **Luna crea reservas (v1: SOLO TINAS, con aprobación de Deborah)** (pedido de Jorge 2026-06-18, "pantalones largos"). El salto de copiloto→acción que ESCRIBE en el sistema. Flujo: identificar cliente por teléfono (WhatsApp ya lo tiene; IG/Messenger Luna lo pide) → `verificar_cliente(telefono)` (existe→solo pide mail/ciudad si faltan; nuevo→nombre/mail/ciudad/RUT) → Luna arma resumen y pide confirmación al cliente → botón "Crear reserva" en la bandeja, Deborah aprueba → Django crea (re-verifica disponibilidad, idempotente, reusa el camino web ⚠️AR-034) → Django genera el resumen-de-pago (botón existente) y Luna lo trae a la conversación. Arquitectura propuesta: tool `preparar_reserva(...)` guarda una `propuesta` estructurada (drift-safe) + `crear_reserva(propuesta_id)` que dispara el botón. Preguntas para Django: cómo se crea hoy una reserva de tina; qué genera el botón de resumen-de-pago y cómo exponerlo; dónde guardar la propuesta; validación RUT. Detalle en `docs/BRIEF_H-028_luna_crea_reservas.md`. | Django (verificar_cliente + preparar/crear reserva + resumen-pago) + aremko-cli (botón + postear resumen) | ✅ CERRADO 2026-06-18 — **BUG 1 (external_id placeholder) + BUG 2 (propuesta_reserva missing) RESUELTOS** (Django commit `21637b4`). BUG 1: `_tool_executor` se movió dentro de `_producir_borrador` como closure que CAPTURA el phone real del cliente → `preparar_reserva` guarda PropuestaReserva con el external_id correcto (antes era siempre `+56912345678`). BUG 2: `/api/whatsapp/conversation/` ahora devuelve `propuesta_reserva` (antes solo lo devolvían IG/Messenger) → aremko-cli ve el botón "Crear reserva" en WhatsApp. **E2E H-028 desbloqueado:** Luna → Deborah → cliente. Validación: aremko-cli ejecutó test e2e y confirmó que el banner aparece tras `preparar_reserva`. | 2026-06-18 (agente Django) |

| H-029 | **Flujo de carrito de reservas de Luna** (pedido de Jorge 2026-06-18, diseñado con aremko-cli; evoluciona H-028). Principio: una conversación = un carrito = una reserva; servicios se acumulan; un total/pago/resumen. **FASE 1:** guion recolección datos + `verificar_cliente` tool. **FASE 2:** `CarritoReserva` (tabla) + servicios+productos + descuentos dinámicos PackDescuentoService + 5 tools (agregar_servicio, agregar_producto, ver_carrito, quitar_item, checkout_carrito) + cross-sell sutil + checkout→preparar_reserva con TODOS los items = 1 reserva. **FASE 3:** cron 23:00 libera pendientes impagas EXCEPTO grace 21-23h + flag `eximir_liberacion`. **FASE 4:** coordinación combos. Luna NO toca pago (Deborah registra a mano). **TESTING:** management command `limpiar_conversacion --phone +56958655810` + API endpoint `POST /api/luna/admin/limpiar-conversacion/` para borrar historial antes de cada test (evita sesgos). Detalle en `docs/BRIEF_H-029_flujo_carrito_reservas.md`. | Django (carrito + tools + prompt + cron + combos) + aremko-cli (UI carrito; menú Admin para limpiar) | 🟩 FASE 1+2 IMPLEMENTADAS (Django) 2026-06-19 — Commits `3f2ed50` (FASE 1) + `187ab3a` (FASE 2 modelo) + `c5286e2` (FASE 2 checkout) + `d8765b5` (testing limpiar_conversacion). **FASE 2 E2E completo:** agregar servicio → agregar producto → checkout → recolectar datos (FASE 1) → preparar_reserva UNA vez con TODOS items → descuentos PackDescuentoService aplicados automáticamente. **Testing ready:** management command `python manage.py limpiar_conversacion --phone +56958655810` + API endpoint `POST /api/luna/admin/limpiar-conversacion/` (X-API-Key required). **✅ E2E VALIDADO POR JORGE 2026-06-19** (captura WhatsApp): personas (pregunta antes de ofrecer) → carrito (Tina Llaima 2p $60k) → cross-sell sutil (respeta "eso no más") → cliente existente pide solo RUT faltante → `confirmar_reserva_carrito` crea PropuestaReserva → banner "Crear reserva" aparece → borrador confirma con el `mensaje` de la tool (no alucina). **Fixes de la jornada tras el primer test:** `d777373` (confirmar_reserva_carrito colapsa checkout→verificar→preparar en 1 tool determinística; antes Luna alucinaba "registrado" sin crear propuesta + construir_payload_desde_carrito estaba muerta) + `799f121` (catálogo/disponibilidad SOLO tipos principales tina/masaje/cabana; desayuno ya no se ofrece) + `46ee924` (Luna pregunta cuántas personas antes de ofrecer, no asume 1; capacidad filtra cabañas/tinas >2p). Modelo de Luna confirmado: `google/gemini-2.5-flash` (no Haiku). **✅✅ CICLO COMPLETO E2E VALIDADO 2026-06-19 (reservas 6058+6060):** Luna propone → banner → Deborah aprueba → `crear_reserva` (201) → resumen limpio carga en el cajón editable → Deborah envía al WhatsApp del cliente (outbound CONFIRMADO entrega a clientes, sin bloqueo App Review). **Fixes de la jornada (orden):** `d777373` (confirmar_reserva_carrito colapsa 3 tools→1 determinística) + `799f121` (solo tipos principales, no desayuno) + `26ba20b` (variedad de tipos + limpiar borra PropuestaReserva) + `6ee2d42` (RUT tolera sin guión) + `e59a915` (servicio por nombre: "llaima"→Llaima, no Puntiagudo) + `e267cef`/`3df4f94` (personas primera pregunta, no re-preguntar si ya la dio) + `6979acf` (fecha texto literal → 21 no 28; verificar_cliente usa external_id, no pide teléfono en WA) + `f9f148b` (mensaje suavizado: "preparando" no "registrada") + `c859bf2` (TTL PropuestaReserva 1h→24h: Deborah aprueba sin que caduque el cajón) + `778eb6f` (no asumir 1 persona: handlers devuelven falta_personas; filtrar notas internas "[Luna]/[Propuesta]/Aprobada por Deborah" del resumen al cliente). Bug clave del cajón vacío: era cliente Go de aremko-cli tratando 201 como error + TTL 1h. **Pendiente menor:** productos del carrito aún NO van a la PropuestaReserva (solo servicios); revisar `ConfiguracionResumen` (banco/mail/foto) en admin; reserva 6052 quedó con fecha vieja (pre-fix). **Próximo:** FASE 3 (cron 23:00 liberación impagas) + FASE 4 (combos tina+masaje/cabaña). | 2026-06-19 (agente Django) |
| H-030 | **Adjuntos tipo `share` de Instagram NO llegan a la bandeja** (reportado por Jorge 2026-06-19). Cuando un cliente COMPARTE una publicación/anuncio de Aremko por DM (lo más común al consultar; ej. "Natalia" → "Ese de tinaja, masaje y cabaña, la publicación que envié"), el adjunto no aparece en la bandeja; solo llega el texto. **Diagnóstico (confirmado con datos de prod):** las fotos/videos DIRECTOS entrantes SÍ llegan (validado: imagen entrante de Deborah 2026-06-19 23:35 guardada como `image`); pero NUNCA ha llegado un `share` (conteo BD: solo text/image/video, 0 share). El endpoint Django receptor `POST /api/instagram/inbound-media` ya está listo y soporta `share` en `_MEDIA_TYPES` (inbox_omnicanal/views.py:322). **El gap está en el webhook de aremko-cli** (componente que recibe los DM de Meta): procesa `image`/`video` directos pero no el attachment tipo `share`. **Pedido a aremko-cli:** inspeccionar el payload real de un DM `share` de Meta → descargar la preview/imagen del post compartido (con el token) → POST a `/api/instagram/inbound-media` con `type=share`. ⚠️ Nota técnica: los shares de IG a veces NO traen URL de media descargable estándar (pueden traer link del post + preview, o formato distinto) — definir el manejo según el payload real. **Contrato endpoint** (multipart, header `X-API-Key`): `ig_message_id` (req), `from_igsid` (req), `to_igsid`, `file` (req, bytes), `type=share`, `caption`, `mime_type`, `filename`, `contact_name`, `timestamp`. Django: receptor listo, nada que tocar. | aremko-cli (webhook entrante IG: manejar `type=share`) | 🟧 EN PROGRESO (aremko-cli) 2026-06-19 — diagnóstico Django completo y confirmado por Jorge (captura conversación Natalia + desglose BD por msg_type/direction: todas las media son `out`, 0 `share`; imagen directa entrante validada en vivo). · **ITERACIÓN 1 desplegada (aremko-cli):** el share ya NO se pierde — el payload del adjunto se captura crudo y se loguea (`[instagram] adjunto tipo=share payload_crudo=...`), y si no hay media descargable se registra el mensaje como texto "[el cliente compartió una publicación]" para que aparezca en la bandeja. **ITERACIÓN 2 desplegada:** payload real capturado — el share llega como tipo `ig_story` con la media en `story_media_url` (no en `url`). Ahora se extrae esa URL, se descarga y se sube normalizando el tipo por mime (image/video) para que Django la acepte y la bandeja la muestre como foto/video. 🟦 INTEGRADO (aremko-cli desplegado; django ya estaba listo) — esperando validación de Jorge: compartir una historia/publicación de Aremko por DM y ver que aparezca la imagen/video (ya no solo el texto). | 2026-06-19 (aremko-cli) |
| H-031 | **Producto insignia "Noche de ritual junto al río" en el sistema** (Plan Río PR-01, pedido de Jorge 2026-06-19 vía aremko-cli). El ritual 3-en-1 (cabaña+tina+masaje nocturno+desayuno, **$240.000**, 2 pax) como experiencia agendable + flujos de pre-llegada. **Fase 1:** Luna ofrece/arma el ritual como bundle de un clic (upsell al pedir cabaña/tina); definir mecanismo (combo/servicio compuesto/carrito con ítems fijos — reusar lo de H-029). Requiere masaje nocturno agendable en horario nocturno. **Fase 2:** pre-llegada 24h antes (Luna manda mensaje "5 sentidos/circuito térmico" + captura foto de la pareja, preferencia de masaje, preferencia de desayuno → visibles para staff). Gift card del ritual = PR-03 (handoff aparte). Detalle + texto del mensaje en `docs/BRIEF_H-031_producto_insignia_ritual.md`. | Django (pack agendable + pre-llegada Luna) + aremko-cli (UI si aplica) | 🟧 FASE 1 DISEÑADA Y SOLICITADA a django 2026-06-22 (aremko-cli leyó el código real: `packs.py`/`availability.py`/`agent.py`/carrito). **Diseño concreto en el brief — ~80% reusa lo existente:** (1) **`disponibilidad_ritual(fecha)`** nueva en `packs.py` = extiende `disponibilidad_pack_cabana_tina` (cabaña+tina ya coordinados) con la 3ª pata vía `disponibilidad(f,2,'masaje')`; arma 1 itinerario si las 3 patas calzan esa noche. **Masaje futuro YA es agendable** (`_es_masaje_agendable` habilita Relajación; la traba `masaje_en_sitio` solo aplica a HOY). (2) **Capacidad 5/noche SIN modelo nuevo:** configurar el servicio masaje nocturno con 5 slots/noche → la disponibilidad topa en 5 sola. (3) **`confirmar_ritual(fecha)`** = 1 tool determinística que carga los 4 ítems en sus slots → `confirmar_reserva_carrito` (H-029) → PropuestaReserva → banner "Crear reserva" (reusa entero el cierre de un clic). **Decisiones Jorge 2026-06-22:** secuencia = la función elige el combo tina+masaje que CALCE (no forzar tina-más-tarde); precio = **$240.000 PLANO todos los días** (saltar el descuento dom-jue del pack). **Config a confirmar Django (no código):** que el masaje nocturno tenga slots nocturnos + cap 5/noche + precio $80k/2pax. Fase 2 (pre-llegada) NO bloquea Fase 1. · **🟧 CORRECCIÓN Jorge 2026-06-23 (vía Django) — diseño MÁS SIMPLE, descarta el servicio nuevo:** NO crear "masaje nocturno" ni capacidad-5; reusar TODO. Reglas: (a) **cabaña** = asignar una que NO sea "Torre"; solo si no queda otra → Torre (Torre +$10k). (b) **masaje** = reusar un slot EXISTENTE de masaje para 2 (el sistema ya agenda bloques de 2 masajes, ≥2 masajistas/día) filtrado a ≥16:00; nada de slots nuevos. (c) **precio $240k plano:** si toca Torre (+$10k), `confirmar_ritual` agrega la línea existente "Descuento de servicios" (precio_base=-$1) con cantidad=10000 → -$10k → vuelve a $240k. **Django LO TOMA** y construye `disponibilidad_ritual`+`confirmar_ritual` con estas reglas (extiende `disponibilidad_pack_cabana_tina`); arrancará con OK de Jorge (acaban de hacer wrapup). **Nota de aremko-cli para el cierre:** el banner/crear_reserva no cambia (acepta 2xx, idempotente); PERO el resumen al CLIENTE debería mostrar "Noche de ritual junto al río $240.000" como 1 unidad, ocultando el desglose (Torre + línea negativa de descuento) — Django decide. | 2026-06-23 (aremko-cli) |
| H-032 | **Máquina de reseñas: pedido de reseña Google vía Luna (post-visita)** (Plan Río PR-02, pedido de Jorge 2026-06-19 vía aremko-cli). AMPLIFICA lo existente (EncuestaSatisfaccion NPS D+1 + funnel web "solo contentos" `califica_para_review_publico` + recovery `requiere_followup` + ReviewSnapshot) moviendo el pedido a Luna/WhatsApp. Outcome: post-visita, Luna pide reseña Google SOLO a contentos con link directo; detractores → recovery (bandeja Deborah). Gating robusto recomendado (micro-NPS numérico o sentimiento con default seguro a recovery — ojo modelo liviano). Link: `https://maps.app.goo.gl/zSoVC4kFjS87Lfez7`. Mantener funnel web en paralelo; NO incentivar (Google). Responder reseñas + automatizar ReviewSnapshot = futuro. Detalle + mensajes en `docs/BRIEF_H-032_maquina_resenas_luna.md`. | Django (trigger + Luna flujo + gating + recovery) + aremko-cli (si requiere UI) | 🟥 SOLICITADO a django 2026-06-19. No urgente (después de H-030/H-031). | 2026-06-19 (aremko-cli) |
| H-033 | **Luna: disponibilidad muestra hora INEXISTENTE (20:00) + más de 2 opciones** (reportado por Jorge 2026-06-19, captura WhatsApp). Cliente: "tina para mañana a las 8pm" → Luna listó **6 tinas** y dijo "disponibilidad a las 20:00 hrs", pero NO existe slot de tina a las 20:00 (horarios reales: 19:00, 19:30, 21:30, 22:00). **Diagnóstico aremko-cli (leyendo código):** (1) **Tope de 2 NO aplicó** — `whatsapp_agent/availability.py::disponibilidad()` tiene `if tipo: servicios = servicios[:2]`, y como salieron solo tinas el `tipo='tina'` sí filtró el qs, pero igual salieron 6 → verificar qué versión está desplegada en Render / por qué el cap no dispara en prod. (2) **Hora 20:00 inexistente** — la tool devuelve `slots_libres` reales (vía `extraer_slots_para_fecha`); Luna dijo "20:00" = la hora que pidió el cliente, NO un slot real → grounding: el prompt debe forzar mostrar SOLO los horarios de `slots_libres` y, si la hora pedida no está, decirlo y ofrecer las cercanas; verificar también que `slots_disponibles` de las tinas no traiga un 20:00 mal. (3) **Fecha:** "mañana" (20/6) pero Luna dijo "viernes 21 de junio" (el 21 es domingo) → verificar resolución de fecha + nombre de día. | Django (availability + prompt grounding + verificar deploy) | 🟩 FIX APLICADO (Django) 2026-06-20 — **causa raíz confirmada en el código:** el bloque "# 7 PRECIOS Y DISPONIBILIDAD" del prompt tenía instrucciones CONTRADICTORIAS con los fixes de ayer (quedó sin tocar): (a) le decía a Luna "resuelve dates a YYYY-MM-DD / 'mañana' → calcula a partir de hoy" → Gemini calculaba la fecha y erraba ("viernes 21"); (b) "SIEMPRE lista TODAS las opciones" → empujaba a listar las 6 tinas del catálogo. Fix (1 commit, solo prompt.py): el bloque ahora (1) exige pasar el TEXTO de fecha tal cual y usar el `dia_semana` que devuelve la herramienta (alineado con 6979acf); (2) ofrece SOLO lo que devuelve la tool, prohíbe listar/inventar del catálogo; (3) REGLA DURA: nunca afirmar servicios/horarios/fechas sin llamar la herramienta; (4) si la hora pedida no está en `slots_libres`, no confirmarla y ofrecer las cercanas (ataca el "20:00"). El tope de 2 y resolver_fecha (código) ya estaban OK desde ayer; el bug era el prompt empujando a NO usar la tool. **SUB-BUG DESTAPADO por el fix (commit `d247d65`→`<nuevo>`):** al forzar a Luna a pasar el TEXTO LITERAL de la fecha, "hoy"/"mañana" NO los resolvía `resolver_fecha` (solo manejaba nombres de día y números) → caía en el fallback de error → "no pude encontrar disponibilidad para hoy" aunque SÍ había tinas (caso real Jorge 2026-06-20: "tina para 2 hoy a las 8 de la noche" → Luna dijo no hay). Además el "8" de "8 de la noche" lo tomaba como día 8 del mes. **Fix:** `resolver_fecha` ahora maneja "hoy"/"mañana"/"pasado mañana" (PATRÓN 1.5), DESPUÉS de los nombres de día (para que "sábado por la mañana" caiga en sábado) y ANTES del patrón numérico (para que "hoy a las 8" no tome el 8). Verificado con 9 casos. **✅ CERRADO Y VALIDADO POR JORGE 2026-06-20** (captura WhatsApp): "tina para 2 hoy a las 8 de la noche" → Luna respondió "Para hoy sábado 20 de junio, tengo disponibles las tinas Llaima y Puntiagudo para 2 personas, $60.000... A las 20:00 no tengo, pero sí 19:00 o 21:30 para Llaima, y 21:30 para Puntiagudo". Fecha correcta + 2 tinas (no 6) + horarios reales + maneja la hora pedida inexistente. Commits live: `d247d65` (prompt) + `eae5f26` (resolver_fecha hoy/mañana). | 2026-06-20 (agente Django) |
| H-034 | **Medir la landing del Ritual del Río en la pestaña "Web" de Informes** (brief de Jorge 2026-06-21). Landing oculta `aremko.cl/ritual-del-rio/` (producto insignia $240k; tráfico de pauta Meta/Google con UTM; puede tener 0 tráfico al inicio → el tracking tolera 0). CTA único = botón WhatsApp `wa.me/56957902525` (aparece 2 veces) = conversión/lead. GA4 `G-T3K4CTD3HJ`. **Reparto:** **aremko-cli** — segmento "Ritual del Río" en la pestaña Web filtrando por `pagePath=/ritual-del-rio/` (vistas/usuarios/sesiones + engagement + UTM source/medium/**campaign**); ya existe `/ga4/page-metrics?path=` (falta sumar dimensión `sessionCampaignName`) + tarjeta en el front. **Django (lo que ofreció instrumentar):** evento GA4 `click_whatsapp_ritual` en los 2 botones de WhatsApp de la landing + **UTM passthrough** al link wa.me (para atribuir el lead a la campaña). | aremko-cli (segmento Web + UTM campaña + leer el evento) + Django (evento GA4 `click_whatsapp_ritual` + UTM passthrough) | 🟦 aremko-cli LISTO (desplegado 2026-06-21): la landing `/ritual-del-rio/` está en el selector de Informes→pestaña Web (vistas/usuarios/sesiones + engagement + fuente/medio + nueva columna **UTM Campaña**, tolera 0 tráfico). **🟩 INTEGRADO 2026-06-22** — Django desplegó y verificó en prod el evento `click_whatsapp_ritual` en los 2 botones (commits `f2f08c0`→`afe0149`), con UTM passthrough como params `ritual_campaign/source/medium` (opción B, para no chocar con nombres reservados de GA4) acotado a `send_to G-T3K4CTD3HJ`. Verificado en la landing live. Falta solo validación de Jorge con tráfico real. | 2026-06-21 (aremko-cli) |
| H-035 | **Envío de fotos WebP por WhatsApp falla con #546** (reportado por Jorge 2026-06-22). Al adjuntar una foto de cabaña (catálogo / biblioteca de medios = WebP de Cloudinary) para enviar por WhatsApp → Cloud API `(#546) The type of file you're trying to attach isn't allowed`. Causa: WhatsApp solo acepta `image/jpeg` e `image/png` como imagen; el webhook saliente pasaba el mime del archivo tal cual a UploadMedia (webp → rechazo). Diagnóstico Django + fix aremko-cli. | aremko-cli (transcode Go) | ✅ IMPLEMENTADO (aremko-cli, desplegado 2026-06-22): `NormalizeImageForSend` (`internal/whatsapp/transcode.go`) detecta el tipo real por magic bytes si el header viene vacío, transcodifica WebP/GIF/BMP → JPEG antes de `UploadMedia`, y si no se puede decodificar (HEIC de iPhone) devuelve un error claro al operador ("convierte la foto a JPG") en vez del críptico #546. Cubre el envío y el registro del saliente en Django (el hilo guarda el JPEG). Dependencia agregada: `golang.org/x/image` (decoders webp/bmp). **✅ CERRADO Y VALIDADO EN PROD por Jorge 2026-06-22.** CAUSA RAÍZ REAL (no era el formato): el catálogo entrega JPEG al servidor; el #546 lo gatillaba el NOMBRE de archivo con puntos (`WhatsApp_Image_..._21.07.11_btlwof`) → WhatsApp lee `.11_btlwof` como "extensión" inválida y rechaza, aunque el Content-Type sea image/jpeg. Fix final `withImageExt` (commit `d74906b`): garantiza filename terminado en `.jpg`/`.png` según mime (recorta sufijos no-imagen, sin puntos internos). Verificado en vivo (misma foto con nombre limpio = OK). Las defensas previas (transcode WebP→JPEG + f_jpg en Cloudinary) quedan como bonus. | 2026-06-22 (aremko-cli) |
| H-036 | **Campaña de Google Ads para "Noche de ritual junto al río"** (pedido de Jorge 2026-06-22). Crear la campaña de Google Ads del producto insignia replicando el patrón/estructura de la campaña **Refugio Aremko** (la que ya armó aremko-cli) + método Sabri (Google Ads = CAPTURA de demanda: keywords que la gente YA busca, copy directo). **Contexto:** $240k/2pax, 3-en-1 (cabaña+tina+masaje nocturno+desayuno) junto al río; landing PÚBLICA e indexable `aremko.cl/ritual-del-rio/` (hero+3 escenas+reseñas reales+oferta+garantía); USP KEYSTONE = único en Pto Varas que junta río+noche+sistema; audiencia sur (Osorno/Pto Montt/Pto Varas). **Conversión ya lista (H-034):** evento GA4 `click_whatsapp_ritual` + UTM passthrough; CTA único → WhatsApp `wa.me/56957902525` → **importar esa conversión de GA4 a Google Ads** para optimizar hacia leads + mandar UTM en los anuncios. Keywords sugeridas: "spa puerto varas", "cabaña tina caliente puerto varas", "escapada romántica los lagos", "noche romántica puerto varas", "tinajas calientes puerto varas"; negativas trabajo/empleo/gratis. RSA con oferta+USP+CTA. **Reparto:** aremko-cli arma y carga la campaña completa; Django: landing+tracking listos (nada que tocar salvo pedido). Detalle en `docs/BRIEF_H-036_google_ads_ritual.md`. | aremko-cli (campaña Google Ads completa) | 🚀 LANZADA 2026-06-22 (aremko-cli + Jorge paso a paso en el panel; integración Google Ads read-only). Campaña "Ritual del Río – Search" LIVE, cuenta Aremko Spa boutique 539-975-0827 (login ecolonco1@gmail.com): Búsqueda (Display fuera), geo Osorno/Pto Montt/Pto Varas, español, 12 keywords concordancia de frase, **Maximizar CLICS** (no conversiones — importar el evento requiere vincular GA4↔Ads, se hace después; medición del lead NO se pierde: GA4 + Informes→Web H-034 con UTM), CLP 5.000/día, UTM utm_campaign=ritual_del_rio → /ritual-del-rio/, RSA 15 títulos+4 descripciones. Pendiente lado Jorge/operación (no sistema): extensiones, quitar extensión de llamada con número raro, **verificación identidad anunciante (plazo 4-jul-2026)**, a futuro vincular GA4↔Ads + cambiar a Maximizar conversiones. | 2026-06-22 (aremko-cli + Jorge) |
| H-037 | **Luna Interna · Fase 0 (piloto con Jorge)** (pedido de Jorge 2026-06-22; plan `docs/PLAN_LUNA_INTERNA.md`). Primer paso de la IA al servicio de la gestión operativa: Jorge escribe "empezando el día" → Luna lo reconoce por su número, lo saluda por turno y le entrega su briefing (pagos que vencen del tablero `costos_web` + saldos bajos + comandas/tareas pendientes). REACTIVO (él inicia → ventana 24h abierta; el envío proactivo 11:00 = Fase 0.5). **Base ya construida (Django):** app aislada `personal_operativo` con modelo `PersonalOperativo` = la WHITELIST (telefono E.164 único = llave, usuario FK→`usuario.proveedor` para masajistas, rol, turno, **responde_auto** = interruptor de autonomía, activo); admin SOLO superusuarios. **Falta Django:** detección de modo interno si el número está en la whitelist + intent "empezando el día" + tool `mi_briefing` (determinístico) + marcar la respuesta como `responde_auto` para auto-envío. **Falta aremko-cli:** respetar ese flag → enviar la respuesta de Luna AUTOMÁTICAMENTE (sin el cajón de aprobación de Deborah) para números staff; clientes sin cambios. A definir: flag en la respuesta (propuesto) vs endpoint de consulta. Detalle en `docs/BRIEF_H-037_luna_interna_fase0.md`. | Django (whitelist ✅ + modo interno + mi_briefing + flag) + aremko-cli (auto-envío staff) | ✅ CERRADO Y VALIDADO POR JORGE 2026-06-22 (captura WhatsApp): Jorge escribió "empezando el día" al WhatsApp de Aremko y le llegó su briefing SOLO a su celular (sin cajón, sin aprobación) — "Buenas tardes, Jorge 👋 ... 💳 Pagos próximos (30 días): Cloudinary 13-07 · 29 USD · ****8892 ... 📋 Comandas pendientes: 14". **Django (commits `9e146c2` flag en read + `45da186` directivo `responder_briefing` en la respuesta del inbound, sin costo LLM + `167bb8b` ventana pagos 30 días):** whitelist `PersonalOperativo` + admin superuser + `buscar_personal`/`construir_briefing` + `_es_inicio_turno` + comando `preview_briefing`. **aremko-cli:** webhook server-side lee `responder_briefing` y envía solo. Idempotencia vía PostWhatsAppOutbound (último msg→saliente). Pendiente menor: vincular usuario de Jorge para ver tareas; Fase 0.5 = cron proactivo 11:00. **Próximo: Fase 1** (identidad/roles completa) y Fase 2 (entrega de labores por rol, masajistas primero). | 2026-06-22 (Django + aremko-cli) |

| H-053 | **Ventas reales de programas en el reporte diario de ads** (pedido de Jorge 2026-06-30). Los reportes diarios Meta/Google (aremko-cli) hoy muestran métricas de plataforma (conversaciones, clics, CPC); Jorge quiere ver el **RETORNO REAL**: ventas de cada programa vs gasto en ads (ROI). Mapeo a los buckets **mutuamente excluyentes** de `bookings_family_combinations` (`ventas/api_aremko_cli.py` ~2113): **Pausa** = `tinas_masajes` (SOLO tina+masaje), **Ritual** = `cabanas_tinas_masajes` (cabaña+tina+masaje; incluye Refugio 2 noches por ahora). Cada VentaReserva cae en EXACTAMENTE un bucket → sin doble conteo (confirmado por Jorge: tina+masaje-solo NO se cuenta dentro de cabaña+tina+masaje). **Necesito de Django:** endpoint por **RANGO DE FECHAS** (el actual es mensual; el reporte usa ventana de ~7 días) que devuelva `count_reservas` + `revenue` por bucket. Contrato sugerido `GET /api/aremko-cli/program-sales/?start&end` — detalle en `docs/BRIEF_H-053_ventas_reales_programas_reporte.md`. **aremko-cli (yo):** consumo eso → línea "Ventas reales (período): N reservas · $ingresos · ROI" en los bloques Ritual/Pausa (Meta+Google). ROI directional (ventas totales ÷ gasto ads). | Django (endpoint por rango) + aremko-cli (consumo en reporte) | ✅ CERRADO E INTEGRADO (vivo en prod ambos lados 2026-06-30) — Django: endpoint vivo `GET /ventas/api/aremko-cli/bookings/family-combinations-range/?date_start=YYYY-MM-DD&date_stop=YYYY-MM-DD` (params obligatorios; reusa `_classify_family_combo`; excluye cancelado; agrupa por `fecha_creacion`; revenue=total de la reserva; TODOS los buckets presentes con 0). Shape `{date_start,date_stop,combinations{<combo>:{count_reservas,revenue}},total{...}}` — calza con `CombinationStats` del cliente Go. **aremko-cli:** `GetFamilyCombinationsRange` + `attachProgramRealSales` enganchan las ventas reales (misma ventana 30d) en los bloques 🌙 Ritual y 🍃 Pausa del reporte Meta; commit `cf24b1e` vivo (auto-deploy). Verificado end-to-end (jun: Pausa 21/$2.807.000, Ritual 21/$5.404.997, total 164/$17.911.989). **Decisión de presentación:** se muestran unidades + ingresos ABSOLUTOS del programa (TODOS los canales) al lado del gasto; NO se calcula "ROI ÷ gasto ads" porque la mayoría de esas ventas no vienen de ads (el ratio daría ~20-36× y engañaría). **Pendiente opcional:** (1) ventas reales también en el reporte de Google Ads (hoy Ritual/Pausa ahí son filas de tabla, no bloques); (2) separar Refugio (2 noches) de Ritual por nº de noches si Jorge lo pide (nuevo H-0xx). | 2026-07-01 (aremko-cli + Django) |
| H-053b | **Separar Ritual (1 noche) de Refugio (2 noches) en las ventas por programa** (Jorge 2026-07-01: los 3 programas SIN intersección — cada reserva en un solo programa). Django parte el bucket `cabanas_tinas_masajes` del endpoint range por **nº de noches = fechas DISTINTAS de cabaña** (Refugio agenda la misma cabaña 2 fechas; Ritual 1). | Django (split, listo) + aremko-cli (leer keys nuevas) | ✅ CERRADO E INTEGRADO (aremko-cli vivo `55b3b68`) — en `GET /ventas/api/aremko-cli/bookings/family-combinations-range/` hay 3 keys nuevas: `cabanas_tinas_masajes_1n` (**Ritual**), `cabanas_tinas_masajes_2n` (**Refugio**), `cabanas_tinas_masajes_otras` (3+ noches). El combinado `cabanas_tinas_masajes` **SE MANTIENE** (compat = suma de las 3) para NO romper el consumo vivo `cf24b1e`. **aremko-cli LISTO (`55b3b68`, vivo):** el reporte Meta muestra 3 tablas SEMANALES (8 sem) separadas — 🌙 Ritual lee `_1n`, 🌿 Refugio lee `_2n` (métrica Leads, es campaña de formulario), 🍃 Pausa `tinas_masajes`; usa SOLO los splits (no el combinado) → sin doble conteo. Verificado end-to-end (8 sem: Ritual 29 · Refugio 5 · Pausa 48 res). Commits Django `7e95595`+`ddb0384`; aremko-cli `55b3b68`. | 2026-07-01 (aremko-cli + Django) |
| H-057 | **Endpoint de lectura de snapshots SEO (GA4/GSC) para el loop de mejora continua** (Jorge 2026-07-02: arrancando 3 loops de mejora continua — Meta Ads y Google Ads ya viven en aremko-cli; SEO va en sesión dedicada de Django, solo aremko.cl por ahora). Descubrí que YA existe el pipeline semanal (`snapshot_weekly_traffic` lunes 9am → `GA4Snapshot`/`SearchConsoleSnapshot`; `generate_weekly_marketing_brief` lunes 10am → email) — el loop queda SEPARADO de ese brief (decisión de Jorge), reusando los mismos snapshots pero con bitácora y comparación semana a semana, enfocado en acciones SEO concretas. **Problema puntual:** los 2 endpoints existentes generan datos y piden `X-API-KEY` (`AUTOMATION_API_KEY`) — el loop corre en sesión local sin ese secreto disponible, y no queremos ponerlo en un archivo de prompt. Solo necesito LEER lo que ya se guardó, no generar nada nuevo. Contrato propuesto: `GET /ventas/api/aremko-cli/seo-snapshots/?weeks=8` (público, sin auth, mismo criterio que `family-combinations-range` de H-053) devolviendo los últimos N snapshots GA4+GSC. Detalle completo en `docs/BRIEF_H-057_endpoint_lectura_snapshots_seo.md`. | Django (endpoint nuevo) | 🔵 INTEGRADO — `GET /ventas/api/aremko-cli/seo-snapshots/?weeks=8` construido (`ventas/api_aremko_cli.py:seo_snapshots`), público sin auth, últimos N `GA4Snapshot`+`SearchConsoleSnapshot` (más antiguo primero; `top_queries`/`top_pages` extraídos de `datos`). **Pendiente:** verificación con datos reales (comando `diagnosticar_seo_snapshots`) antes de avisar shape final a aremko-cli. **⚠️ Verificado por aremko-cli (2026-07-02):** el endpoint responde bien (200, shape correcto) y `ga4` trae 8/8 semanas con números reales (sessions/conversions/whatsapp_clicks todos poblados, ej. semana 2026-06-15: 1541 sesiones, 89 conversiones). Pero **`gsc` trae CERO en las 8 semanas** (`clicks:0, impressions:0, position:0.0, top_queries:[], top_pages:[]` en TODAS, no solo alguna). Revisé `seo_snapshots` en `api_aremko_cli.py`: lee fielmente `s.clicks/impressions/ctr/position` (campos materializados de `SearchConsoleSnapshot`) — mi endpoint no tiene bug, el problema está antes, en cómo `snapshot_weekly_traffic` guarda esos campos (probable: `gsc_reporter.py` no está recibiendo datos reales de la API de GSC, o hay un mismatch entre lo que devuelve y cómo se mapea a esos 4 campos del modelo — noté además que `top_queries` se lee como `datos.get('top_queries',{}).get('last_7d',[])` mientras `top_pages` se lee como `datos.get('top_pages',[])`, formas distintas, por si ayuda a ubicar el origen). **No voy a armar el loop de SEO sobre este dato** — sería una falsa alarma de "SEO colapsado". Avisame cuando esté corregido y confirmado con números reales de Search Console. | 2026-07-02 (agente Django) |
| | **✅ RESUELTO (2026-07-02, agente Django):** causa raíz confirmada — NO era bug de código. La cuenta de servicio (`aremko-reader@aremko-e51ae.iam.gserviceaccount.com`, la misma que usa GA4 con éxito) nunca había sido agregada como usuario en la propiedad de Search Console — es un permiso que Google exige por separado para cada producto, aunque la cuenta ya tuviera acceso a GA4. Confirmado con `HttpError 403 "User does not have sufficient permission for site"` en vivo. Jorge agregó la cuenta en Search Console → Usuarios y permisos (permiso Completo). Verificado end-to-end: (1) llamada en vivo a `gsc_reporter.get_full_snapshot()` → `errors: []`, `overview.last_7d = {clicks: 242, impressions: 2526, ctr: 9.58, position: 6.37}`; (2) corrí `python manage.py snapshot_weekly_traffic --only gsc --generado-por management_command` para no esperar al cron del lunes → `SearchConsoleSnapshot` id=9 con esos mismos números reales; (3) `GET /ventas/api/aremko-cli/seo-snapshots/?weeks=8` ya devuelve la semana 2026-07-02 con clicks=242, 25 `top_queries` reales (ej. "aremko" 75 clics, "aremko aguas calientes puerto varas" 33 clics), 21 `top_pages`. Las 7 semanas anteriores siguen en cero (snapshots viejos, de antes del permiso) — se van completando solas cada lunes desde ahora. **Listo para que aremko-cli arme el loop de SEO.** | 2026-07-02 (agente Django) |
| H-059 | **Botón "buscar otro horario" en la bandeja para navegar alternativas de Pausa (tina+masaje)** (Jorge 2026-07-03: reportó con capturas reales que Luna le dijo a un cliente que 2 opciones de Pausa eran "las últimas disponibles" cuando en realidad, para esa fecha, había 6 combos válidos — verificado con diagnóstico real contra otra fecha donde sí era cierto y una donde no). **Causa raíz confirmada:** `_componer_opcion()` en `whatsapp_agent/packs.py` recorría las tinas del grupo y en cuanto encontraba la PRIMERA tina+horario que calzaba con un masaje, hacía `return` inmediato — nunca seguía buscando alternativas más tardías, aunque existieran (mismo tipo de gap que `elegir_tina_mas_tarde` ya resolvía para cabaña+tina, nunca replicado para tina+masaje). En vez de solo arreglar el prompt, Jorge propuso algo mejor: un botón nuevo en la bandeja (al lado de "Adjuntar") que, cada clic, busca la SIGUIENTE alternativa (cada vez más tarde) y la pone en el borrador — así Deborah navega las combinaciones con un clic y decide cuál mandar, sin depender de que el LLM interprete bien un "¿más tarde?" ambiguo. Jorge confirmó explícitamente: implementar AMBAS cosas (el botón supervisado + el fix automático del prompt). **Decisión de diseño (investigada con 2 agentes Explore, Django+aremko-cli):** hoy `SugerenciaAgenteWhatsApp` NO guarda qué tool se llamó ni con qué argumentos, y `PropuestaReserva` (que sí trae fecha/hora/personas) recién existe DESPUÉS de que Luna arma el carrito — no en la etapa de "borrador informativo" de las capturas de Jorge. Por eso el endpoint nuevo recibe `fecha`+`personas` EXPLÍCITOS (no los adivina) y devuelve la lista COMPLETA ordenada cronológicamente; aremko-cli mantiene el índice de "en cuál alternativa vamos" en estado de React. Cero migraciones, cero cambios al tool-calling de Luna. | Django (fix + endpoint + tool, listo) + aremko-cli (botón, listo) | ✅ CERRADO E INTEGRADO (Django, commit `3030e78`, vivo en prod) — **(1)** `whatsapp_agent/packs.py`: `_candidatos_opcion` junta TODOS los candidatos válidos de un grupo (con/sin hidromasaje), ordenados por hora de tina ascendente; `_componer_opcion` queda como wrapper de compatibilidad (`candidatos[0]`, cero cambio de comportamiento para quien ya la usaba). `disponibilidad_pack_tina_masaje` suma 2 parámetros: `todas=True` (agrega `alternativas`: TODAS las combinaciones del día, ambos grupos mezclados) y `mas_tarde=True` (cada `opciones` trae la ÚLTIMA de su grupo en vez de la primera). **(2) Endpoint para el botón:** `GET /api/luna/pausa/alternativas/?fecha=YYYY-MM-DD&personas=N` (auth `X-API-Key`/`LUNA_API_KEY`, mismo patrón que el resto de `/api/luna/*` en `ventas/views/luna_api_views.py` + `api/urls.py`) → `{fecha, personas, alternativas:[{etiqueta, tina, hora_tina, hora_masaje, precio_total, precio_con_descuento, hay_descuento, texto_sugerido}]}` — `texto_sugerido` ya viene formateado listo para pegar en el cajón (usa `grounding.formatear_precio`), `alternativas` vacía (200, no error) si no hay ninguna combinación ese día. **(3) Fix automático (Parte C):** la tool `consultar_disponibilidad_pack` (agent.py) acepta `mas_tarde:boolean`; regla nueva en `prompt.py`: si el cliente ya vio opciones y pregunta "¿más tarde?", Luna vuelve a llamar la tool con `mas_tarde=true` en vez de repetir la respuesta o inventar que no hay más; si el resultado trae el MISMO horario, debe decir que esa es la única alternativa (no reofrecerla como si fuera nueva). **Tests:** caso real reproducido con datos sintéticos exactos del domingo 2026-07-05 (Llaima 11:30/14:00/16:30 + Hornopiren 12:00/14:30/17:00, masaje 14:15/16:45/19:15 — 6 combos, antes solo salían 2), 27/27 OK. **Verificado en prod:** deploy Live confirmado + `www.aremko.cl` 200; corrí el diagnóstico (`diagnosticar_pausa_mas_tarde 2026-07-05 --personas 2`, con las secciones `todas=True`/`mas_tarde=True` agregadas) como *one-off job* vía `render jobs create` (terminó `succeeded`, sin excepción, contra datos reales) — **pero no pude leer el stdout del job** (gap del CLI `render logs` para recursos tipo job, no encontré la forma de filtrarlo tras varios intentos) ni hacer un curl directo con el `LUNA_API_KEY` real (el clasificador de permisos bloqueó, correctamente, mi intento de sacarlo desde el Keychain — no es un canal establecido para eso). Si querés el detalle línea a línea antes de construir el botón, o confirmar con un curl real, avisame y lo corro con vos mirando, o pasame el `LUNA_API_KEY` para un curl puntual. **Para aremko-cli (el botón, no implementado acá):** nuevo botón en `ConversacionWhatsApp.tsx` junto a "Adjuntar" (mismo estilo). Primer clic: pedir/confirmar `fecha`+`personas` (no hay forma de adivinarlos con confianza). Clics siguientes: mismo fecha/personas, índice en estado de React que avanza +1 (arranca en 0). Llamar el endpoint de arriba vía el mismo patrón proxy Go que `send-media` (Go agrega `X-API-Key`), tomar `alternativas[indice].texto_sugerido` y reemplazar el contenido del textarea del borrador. Si `indice >= len(alternativas)`: loop a 0 o deshabilitar (decisión de UX de aremko-cli). **✅ aremko-cli hecho y verificado 2026-07-03 (commits `d34b3ab`+`ce059af`):** proxy Go (`GetPausaAlternativas` en `bookings/client.go`, mismo patrón `X-API-Key` que `GetRefugioLeads`) + `GET /api/v1/whatsapp/pausa-alternativas` — **probado en vivo contra el endpoint real de Django** (2026-07-05/2 personas → 6 alternativas reales, resuelve exactamente el bug original). Botón (ícono Waves) junto a "Adjuntar" en `ConversacionWhatsApp.tsx`: 1er clic abre modal pidiendo fecha+personas; confirmar carga la 1ª alternativa en el borrador; clics siguientes avanzan el índice y dan la vuelta al llegar al final (decisión de UX: loop, no deshabilitar). Indicador de estado (fecha·personas·índice/total) + link "cambiar fecha". Estado se descarta al cambiar de conversación. Verificado: `tsc --noEmit` limpio + `next build` exitoso; desplegado (Vercel auto-deploy). | 2026-07-03 (Django + aremko-cli) |
| H-061 | **Generalizar el botón "buscar alternativas de horario" (H-059, hoy solo Pausa) a los otros 5 casos** (Jorge 2026-07-06: mostró en la bandeja lo cómodo que es el botón de Pausa — fecha+personas → alternativas de horario reales, sin depender de que Luna interprete bien un "¿más tarde?" — y pidió lo mismo para: **Ritual** (cabaña+tina+masaje+desayuno, 1 noche), **Refugio** (misma cabaña, 2 noches), **Noche de Aguas Calientes** (cabaña+tina, sin masaje), **Tina sola** (N personas, sin masaje ni cabaña) y **Masaje solo** (1 persona=1 masaje, 2 personas=2 masajes — confirmado explícitamente por Jorge que "personas" alcanza como campo, no hace falta "cantidad de masajes" separado). UX deseada: el botón abre un selector de "qué buscar" (los 6 tipos, Pausa incluida) ANTES de fecha+personas; el resto del flujo (navegar alternativas con clics, insertar `texto_sugerido` en el borrador) es idéntico al de H-059. **No conozco el código de disponibilidad de Django lo suficiente para proponer el contrato de API** — a diferencia de Pausa (1 sola combinación tina+masaje mismo día), Ritual/Refugio ya tienen sus 4 horarios fijos de masaje documentados (`docs/PENDIENTES.md`/memoria: 15:30/18:00/20:30/21:45 para Ritual/Refugio, río 15:30 para Pausa) y Refugio además exige la MISMA cabaña libre 2 noches seguidas — bastante más complejo que Tina sola/Masaje solo/Noche de Aguas Calientes, que deberían ser disponibilidad de 1-2 servicios sin combinar horarios. Sugiero (a criterio tuyo, con más contexto del código real): (a) evaluar si conviene fasear — los 3 simples primero, Ritual/Refugio después — o construir los 5 de una — y (b) definir vos el contrato de API (¿un endpoint con `?tipo=ritual\|refugio\|noche_aguas_calientes\|tina_sola\|masaje_solo&fecha=...&personas=...` generalizando `GET /api/luna/pausa/alternativas/`, o uno por tipo? mismo criterio de auth/shape que H-059 me sirve). Los buckets de family-combinations ya usados en reportes (`tinas_masajes`=Pausa, `cabanas_tinas_masajes_1n`=Ritual, `cabanas_tinas_masajes_2n`=Refugio, `cabanas_tinas_1n`=Noche de Aguas Calientes) pueden servir de referencia de nomenclatura, aunque son buckets analíticos, no necesariamente el mismo modelo que la disponibilidad en vivo. | Django (Fase 1 LISTA en prod) + aremko-cli (pendiente: proxy Go + modal) | 🟡 **FASE 1 EN PROD (2026-07-13, commit 000d6b5)** — CONTRATO CERRADO para aremko-cli: **`GET /api/luna/experiencias/alternativas/?tipo=<tipo>&fecha=<YYYY-MM-DD>&personas=<n>`**, auth **`X-API-Key`** (idéntico a H-059), personas default 2. `tipo` en Fase 1 = `pausa \| tina_sola \| masaje_solo \| noche_aguas_calientes` (`ritual`/`refugio` responden 400 "Fase 2" hasta que se construyan). Respuesta 200: `{"tipo","fecha","personas","nombre_experiencia","alternativas":[{"titulo","precio_total","precio_con_descuento","hay_descuento","texto_sugerido","itinerario":[{"servicio","hora"}]}]}`. El modal solo necesita `titulo`+precio para pintar el botón e insertar `texto_sugerido` (mismo flujo que H-059). `alternativas:[]` (200, no error) si ese día no hay nada; 400 si falta tipo/fecha o tipo inválido/Fase 2. Tope 12 alternativas por respuesta. `/api/luna/pausa/alternativas/` (H-059) sigue viva igual (el modal puede migrar a `tipo=pausa` o dejarla). Notas: masaje N personas = N masajes mismo horario (precio ×personas, excluye los 4 slots del Programa); Noche usa el itinerario cabaña+tina (tina a la hora más tarde ≥16:00 por regla, desayuno incluido). **PENDIENTE aremko-cli**: proxy Go al endpoint + selector de tipo en el modal (los 4 de Fase 1). **FASE 2 EN PROD (2026-07-13, commit 79c6b4d)**: `ritual` y `refugio` ya activos en el mismo endpoint (precio fijo, la alternativa varía el slot de masaje del Programa 15:30/18:00/20:30/21:45 que esté libre; Refugio 2 noches misma cabaña + tina cada noche + masaje 1ª noche). 38/38 tests + diagnóstico real sin errores. **PENDIENTE aremko-cli**: habilitar `ritual` y `refugio` en el selector del modal (estaban como "próximamente"; el contrato NO cambió — mismo endpoint/shape, solo sacar el disabled). | Django (por definir/construir) + aremko-cli (pendiente, depende del contrato) | 🟡 FASE 1 EN PROD — Django listo (2026-07-13, agente Django/booking): 35/35 tests (5 nuevos de shape/precio), `manage.py check` limpio, y corrida real del diagnóstico `diagnostico_alternativas` sin errores contra datos de prod. Verificación por equivalencia: reusa las MISMAS primitivas ya en producción (`disponibilidad()`, `disponibilidad_pack_cabana_tina()`, y para pausa la propia función de H-059) — solo reshape. La integración final la valida aremko-cli con la key real al construir el proxy. Falta proxy Go + modal en aremko-cli (Fase 1) y Ritual/Refugio en Django (Fase 2). **✅ aremko-cli Fase 1 hecho y verificado (2026-07-06/13, commit `b91f6f5`):** proxy Go `GetExperienciaAlternativas` en `bookings/client.go` (mismo patrón `X-API-Key` que H-059) + `GET /api/v1/whatsapp/experiencia-alternativas`; ruta vieja `/whatsapp/pausa-alternativas` reemplazada (sin uso externo, seguro renombrar). Modal en `ConversacionWhatsApp.tsx` generalizado: selector de "Experiencia" antes de fecha/personas con los 6 tipos — Pausa/Noche de Aguas Calientes/Tina sola/Masaje solo habilitados, Ritual/Refugio deshabilitados con "(próximamente)" hasta la Fase 2 de Django (evita gastar un request en un 400 esperado). Resto del flujo idéntico a H-059 (clic siguiente = próxima alternativa, loop al final, `texto_sugerido` al borrador). Verificado: `go build`+`gofmt`+`tsc --noEmit`+`next build` limpios; desplegado (Render+Vercel auto-deploy). **Jorge probó los 4 tipos de Fase 1 en prod: "sí están disponible y funcionan perfecto".** Queda pendiente solo Ritual/Refugio del lado Django (Fase 2) — cuando estén, aremko-cli solo necesita habilitar esas 2 opciones en `EXPERIENCIA_TIPOS` (`types.ts`), sin tocar nada más. **✅ aremko-cli Fase 2 cerrada (2026-07-13, commit `c66f6b9`):** `ritual`/`refugio` habilitados en `EXPERIENCIA_TIPOS` (`types.ts`) — cero cambios de contrato, como anticipado. Agregado además `personasFijas` en el catálogo: el modal ahora fija y deshabilita el campo "Personas" en 2 cuando el tipo es Ritual/Refugio (antes hubiera dejado editable un campo que el endpoint ignora). Verificado `tsc --noEmit`+`next build` limpios; desplegado (Vercel auto-deploy). **H-061 CERRADO — los 6 tipos completos en producción.** | 2026-07-06 (agente aremko-cli) |
| H-060 | **Luna agrega servicios/productos a una reserva YA CREADA (Gate de Deborah)** (Jorge 2026-07-03: cliente aprueba cotización → se crea la `VentaReserva` → más tarde/al otro día el mismo cliente quiere sumar un servicio o producto a esa reserva, quizás ya pagada; hoy solo se hace a mano en el admin). Jorge confirmó explícitamente (pregunta directa) que el cambio debe pasar por SU aprobación antes de aplicarse, igual que las reservas nuevas — mismo "Gate de Deborah" (`preparar_reserva`/`confirmar_reserva_carrito`/`confirmar_ritual`/`confirmar_refugio`: Luna SIEMPRE arma una propuesta, nunca escribe directo sobre una reserva real). **Hallazgo:** ya existía un endpoint `agregar_servicios_reserva` (`ventas/views/luna_api_views.py`) construido en el refactor de carrito (H-029) pero NUNCA conectado a ninguna tool de Luna — solo lo llamaba un script de test. Además tenía **2 bugs reales**: (1) nunca llamaba `actualizar_saldo()` → una reserva `'pagado'` se quedaba pegada en `'pagado'` aunque se le agregara algo nuevo sin pagar (justo el escenario que describe Jorge); (2) el total recalculado solo sumaba servicios, perdiendo los productos ya existentes en la reserva. Detalle completo en `docs/BRIEF_H-060_agregar_a_reserva_existente.md`. | Django (completo) + aremko-cli (handoff pendiente, ver abajo) | 🔵 INTEGRADO (Django, deployado y migrado en prod) — **(1)** `whatsapp_agent/models.py`: campo nuevo `PropuestaReserva.reserva_existente_id` (si está seteado, la propuesta AGREGA a esa reserva en vez de crear una nueva; NO se reutiliza `reserva_id`, que es un campo de salida). Migración `0010` normal (app aislada, sin el tratamiento de migración a mano que sí requieren `ventas`/`control_gestion` por AR-034). **(2)** `ventas/services/reserva_addition_service.py` (nuevo): `agregar_items_a_reserva()` — fuente única con los 2 bugs corregidos, reusada tanto por el endpoint HTTP existente (ahora también soporta `productos`, antes solo `servicios`) como por la aprobación de una adición. **(3)** `crear_reserva` (luna_api_views.py) branch-ea a `_aprobar_adicion_a_reserva_existente` cuando la propuesta trae `reserva_existente_id` — mismo guard `select_for_update()` de concurrencia que ya protege la creación de reservas nuevas (caso real 6169/6170); al aprobar, `reserva_id` queda igual a `reserva_existente_id` (no null) para que la idempotencia funcione sin tocar ese código. **(4)** Nuevo endpoint de descubrimiento `GET /api/luna/reservas/buscar/?telefono=...` (`buscar_reservas_cliente`) — reservas elegibles (no canceladas, fecha vigente o con 1 día de gracia) vía `whatsapp_agent/reservas_existentes.py` (lógica pura, testeada sin DB). **(5)** 3 tools nuevas de Luna: `buscar_reservas_cliente`, `agregar_servicio_a_reserva_existente`, `agregar_producto_a_reserva_existente` (dos tools separadas por tipo, no combinadas — mismo criterio que el carrito). **(6)** `prompt.py`: sección "3c. AGREGAR A UNA RESERVA YA HECHA". **Tests:** 30/30 OK (3 nuevos de elegibilidad/resumen/orden). **Para aremko-cli (no implementado acá):** el serializer `_propuesta_reserva` (`inbox_omnicanal/views.py`, DENTRO de este repo Django, no en el de aremko-cli) ya expone `reserva_existente_id` — falta que el cajón lo lea para mostrar "esto se agrega a la Reserva RES-X" en vez de "esto crea una reserva nueva" (mismo botón "Crear reserva", sin contrato nuevo), y que lea `agregado: true` en la respuesta de aprobación para el mensaje de éxito correcto. | 2026-07-03 (agente Django) |
| H-054 | **Mismo split también en el endpoint MENSUAL** (aremko-cli pidió consistencia entre `family-combinations` y `family-combinations-range`; propuso los mismos nombres de clave que H-053b, coordinación cruzada). | Django | ✅ CERRADO — `GET /ventas/api/aremko-cli/bookings/family-combinations/` ahora trae, **por mes**, junto al combinado (que se mantiene): `cabanas_tinas_masajes_1n`/`_2n`/`_otras`. Mismo criterio (fechas distintas de cabaña); sub-buckets NO se suman a `total` ni a `summary.share_by_combination`/`trend` (alcance mínimo, se extiende si se necesita). Verificado jun 2026: idéntico al endpoint range (18/2/1, combinado 21, total mes 164 sin doble conteo). Commit `e61f984`. **Resuelto (aremko-cli):** la decisión 2 vs 2+ se maneja en el CONSUMO (Django no cambia nada). Hoy Refugio = `_2n` (exactamente 2 noches = definición del programa); `_otras` (3+ noches) queda FUERA de los 3 programas (no es Refugio ni Ritual). Si Jorge prefiere 2+, aremko-cli suma `_otras` de su lado sin tocar Django. | 2026-07-01 (aremko-cli + Django) |
| H-056 | **Briefing de Luna Interna: pagos solo por perfil + Agenda del día para todos** (Jorge 2026-07-01, tras sumar a Ernesto como 2do receptor de "empezando el día" — H-037/[[project_aremko_luna_interna]]). Ernesto recibió la sección de Pagos próximos/Saldos bajos (`costos_web`) que no debería ver. Criterio final de Jorge: **Pagos/Saldos → SOLO Jorge y Alda** (mismo perfil que Jorge, la crea él); el resto ve su info "según su perfil" (ya lo hace bien "Tus tareas de hoy"/"Comandas", sin cambios ahí). **Nueva sección "Agenda del día"** (primera tina, primer masaje, cuántas/cuáles cabañas hoy) → para TODOS sin excepción, es solo lectura de `ReservaServicio` de hoy (sin migración). Detalle + pseudocódigo exacto en `docs/BRIEF_H-056_briefing_por_perfil_y_agenda_dia.md`. | Django (`construir_briefing` + campo nuevo + admin) | ⏳ PENDIENTE — brief listo (aremko-cli); esperando que Django implemente el campo `recibe_info_financiera` + la sección de agenda, y avise para que Jorge active el campo en su fila (y la de Alda) tras el deploy. | 2026-07-01 (aremko-cli) |
| H-055 | **4to programa "Noche de Aguas Calientes" (cabaña 1 noche + tina, SIN masaje) en las estadísticas** (Jorge 2026-07-01: lanzamiento de producto — landing propia + Luna ya lo presenta con nombre; falta el 4to bloque semanal del reporte). Mismo criterio de "sin intersección" que Ritual/Refugio/Pausa. | Django (split, ambos endpoints) + aremko-cli (sumar 4to bloque semanal) | 🔵 DJANGO LISTO (falta consumo aremko-cli) — mismo patrón exacto de H-053b/H-054, ahora sobre el bucket `cabanas_tinas` (cabaña+tina SIN masaje, que antes mezclaba cualquier nº de noches): `GET .../bookings/family-combinations-range/` y `GET .../bookings/family-combinations/` traen 2 keys nuevas junto al combinado (que se mantiene, = suma de las 2): `cabanas_tinas_1n` (**Noche de Aguas Calientes**, cabaña 1 noche + tina) y `cabanas_tinas_otras` (cabaña+tina sin masaje, otro nº de noches — residual, no se pierde ni se infla nada). **Falta aremko-cli:** sumar un 4to bloque semanal (⛲ Noche de Aguas Calientes) al reporte Meta, junto a 🌙 Ritual (`cabanas_tinas_masajes_1n`), 🌿 Refugio (`cabanas_tinas_masajes_2n`), 🍃 Pausa (`tinas_masajes`) — leer `cabanas_tinas_1n` del endpoint range, usar SOLO el split (no el combinado) para no duplicar conteo, mismo patrón que los otros 3. | 2026-07-01 (Django) |
| H-052 | **🐛 carrito_vacio: Luna cotiza/pide datos sin agregar los servicios** (detectado por Jorge, conversación real cliente Sofia, tina+masaje grupo de 3, 2026-06-30). Luna ofreció un precio GENÉRICO por persona (sin hora), saltó a "¿te envío la cotización?" → datos → `confirmar_reserva_carrito` con el carrito VACÍO → escaló "carrito_vacio" DESPUÉS de pedir los datos. Causa: ofrecer un precio ≠ agregar; Luna no llamó `agregar_servicio_carrito` (necesita HORA, que nunca eligió). Misma familia que H-045 (flujo ofrecer-vs-agregar poco confiable). Backstop OK (confirmar corta con carrito_vacio, no crea reserva fantasma; agent.py:1085). **100% Django (prompt).** | Django | ✅ HECHO y vivo en prod: REGLA DURA en prompt.py — cada servicio aceptado DEBE entrar al carrito (con hora específica) ANTES de pedir datos u ofrecer la cotización; prohibido confirmar con carrito vacío. **Pendiente verificar (no era el bug central):** (1) disponibilidad cargada para fechas lejanas (sept, 3 meses); (2) flujo tina+masaje GRUPO (3p) que ofrezca slots. | 2026-06-30 (agente Django) |
| H-051 | **🐛 "Solo cabaña" igual incluía la tina** (detectado por Jorge 2026-06-30, post H-050). Al pedir SOLO cabaña, Luna igual ofrecía cabaña+tina ($160k) en vez de cabaña sola ($110k). El router (`router_disponibilidad`) SÍ tiene rama "cabaña sola" (`disponibilidad('cabana')`), pero el prompt decía "ofrecé la tina como parte del plan" para cualquier cabaña → Luna metía la tina por defecto. **100% Django (prompt).** | Django | ✅ HECHO y vivo en prod: excepción en prompt.py — si el cliente pide "solo cabaña"/"sin tina"/"no tinas", NO incluir la tina; presentar solo `cabana.precio_total` ($110k, incluye desayuno) y ofrecer la tina como upsell OPCIONAL aparte. | 2026-06-30 (agente Django) |
| H-050 | **🐛 Cabaña+tina cotizaba $180.000 en vez de $160.000 (desayuno duplicado)** (detectado por Jorge, conversación real 2026-06-30). Causa REAL (no era H-049): desde los programas Ritual/Refugio, el **desayuno dejó de ser un adicional** y quedó INCLUIDO en el precio de la cabaña (cabañas $110k / Torre $120k plano, todo el año). Pero `disponibilidad_pack_cabana_tina` (whatsapp_agent/packs.py) seguía **sumándolo aparte**: cabaña $110k + tina $50k + desayuno $20k = $180k. Jorge borró la instrucción del prompt y el problema siguió → la duplicación estaba en el CÓDIGO, no en el admin. (Pista clave: cabaña SOLA daba $110k correcto; cabaña+tina $180k.) **100% Django.** | Django | ✅ HECHO y vivo en prod: el desayuno ya viene en `cab_total` → `precio = cabaña + tina` (sin sumar desayuno); cabaña sola = `cab_total`. Se sigue MENCIONANDO como incluido. Ritual/Refugio ya lo trataban bien (no tocados). cabaña+tina = $110k+$50k = $160k. **Pendiente aparte:** "solo cabaña" Luna igual agrega la tina (routing/prompt). | 2026-06-30 (agente Django) |
| H-049 | **🐛 Luna cotizó distinto el mismo pack en dos fechas iguales** (detectado por Jorge en conversación real con cliente 2026-06-30). Cabaña+tina para 2 un sábado = $160.000 (sáb 4 jul, correcto), pero otro sábado dio **$180.000** (sáb 11 jul, mal). Causa: el precio de cabaña/tina/desayuno es **independiente de la fecha** (no hay temporada; verificado en `whatsapp_agent/packs.py`) y el descuento dom-jue da lo mismo para ambos sábados → la herramienta devuelve $160k para los dos. Luna (Gemini) **no volvió a llamar la herramienta para la fecha nueva**, reusó la oferta anterior e inventó el $180k (la pista: omitió la nota "precio normal, pack dom-jue" que la herramienta SIEMPRE devuelve un sábado). Patrón conocido [[feedback_haiku_determinismo]]. **100% Django (prompt).** | Django | ✅ HECHO y vivo en prod: REGLA DURA en prompt.py #7 — llamada FRESCA a la herramienta por CADA fecha antes de dar precio; prohibido arrastrar/ajustar/estimar precios de una fecha anterior; explicitado que NO hay precios por temporada. Backstop determinístico: la cotización (cajón) siempre muestra el precio real del catálogo. | 2026-06-30 (agente Django) |
| H-048 | **Mostrar el texto conversacional de Luna junto al cajón de cotización** (pedido de Jorge 2026-06-29, opción B). Al crear la cotización aparece el cajón "Cotización lista" pero Luna NO presenta texto conversacional → cajón mudo. Jorge quiere que se vea un mensaje listo para enviar al cliente (saludo + link), no solo el cajón. **Frontend puro** — el backend YA provee `sugerencia_agente` (no se suprime con propuesta, confirmado en `generar_sugerencia`) + `propuesta_reserva.url_cotizacion`; aremko-cli hoy muestra el cajón pero no el texto. | aremko-cli (front) | ✅ CERRADO 2026-06-29 — Django sin cambios. **aremko-cli integrado** (commit `db24754`, pusheado; Vercel auto-deploy): en `CotizacionCajon.tsx`, al aparecer una `propuesta_reserva` con `url_cotizacion` se **autocarga** el mensaje conversacional (saludo + link, el mismo que arma "Poner cotización en el mensaje") en el campo de respuesta — UNA vez por propuesta (ref por `propuesta_id`): no re-carga en cada poll ni pisa lo que Deborah escriba; el botón queda para recargar. Ya no hay cajón mudo. Front puro (passthrough, sin tocar Go). tsc 0 + next build OK. **Pendiente:** prueba viva de Jorge. | 2026-06-29 (Django + aremko-cli) |
| H-047 | **🐛 Luna se queda SIN borrador tras ejecutar tools (Pausa)** (detectado por Jorge en prueba viva 2026-06-29). Al elegir la Pausa, Luna ejecutó las 2 tools (tina+masaje → el carrito quedó perfecto con descuento, visible en el panel H-046) pero NO generó texto de respuesta → la conversación quedó trabada (Luna debía seguir y no lo hizo). Causa: en `generate_with_tools` (destino_puerto_varas/services/llm/openrouter_provider.py), tras ejecutar tools, gemini-flash devolvió un mensaje VACÍO (sin content ni tool_calls) en la iteración siguiente → el loop retornaba texto vacío → el agente escalaba ("respuesta vacía del modelo") → borrador vacío. **100% Django** (provider compartido Luna + DPV). | Django | ✅ HECHO y vivo en prod: si tras ejecutar tools el texto viene vacío y quedan iteraciones, se fuerza UNA ronda más SIN tools (`force_text`) para que el modelo redacte el cierre. Cambio acotado al borde vacío (el camino normal no cambia); sin loop infinito (force_text evita reintentar más de una vez). **Iteración (force_text NO bastó en prod — gemini devolvía vacío incluso sin tools):** red final determinística en el agente (`_cierre_fallback_tras_tools` en whatsapp_agent/agent.py) — si el modelo devuelve vacío PERO las tools ya cambiaron el carrito/propuesta con éxito, NO escala: da un cierre determinístico ("Ya lo tengo en tu carrito, ¿algo más o te envío la cotización?") para que la conversación nunca se trabe. La acción no se pierde porque ya pasó. | 2026-06-29 (agente Django) |
| H-046 | **Carrito EN VIVO en la bandeja (Fase 1: solo lectura)** (pedido de Jorge 2026-06-29). La vista de la cotización llenándose en vivo resultó espectacular; Jorge quiere verla DESDE antes: que el **carrito** se muestre llenándose en vivo desde el primer ítem (no recién al generar la cotización). Modelo "una cosa viva a la vez": antes de cotizar = carrito; después = propuesta. **Fase 1 = solo lectura** (Deborah lo VE); Fase 2 = editar + "pasar a cotización". | Django (API ✅) + aremko-cli (panel front ✅) | ✅ CERRADO 2026-06-29 (Fase 1) — **Django ✅ vivo en prod:** el endpoint de conversación devuelve `carrito_en_curso` (READ-ONLY, shape de `propuesta_reserva.servicios`; null si no hay ítems / carrito >24h / ya hay propuesta vigente). **aremko-cli integrado** (commit `15d1e22`, pusheado; Vercel auto-deploy): panel "🛒 Carrito en curso" en `CotizacionCajon.tsx` — gris, SOLO LECTURA (sin botones), reusa el render de líneas/descuento/total del cajón; se muestra solo cuando NO hay propuesta y se refresca con el polling (~12s); al cotizar (carrito=null) desaparece y queda el cajón ámbar. `types.ts`: `CarritoEnCurso` + `carrito_en_curso` en ambas respuestas. WhatsApp + Instagram cableados. Front puro (passthrough, sin tocar Go). tsc 0 + next build OK. **Pendiente:** prueba viva de Jorge. **Fase 2 (no ahora):** editar + "pasar a cotización" (reusa H-042 + endpoint Django con `editable:true`). | 2026-06-29 (Django + aremko-cli) |
| H-045 | **🐛 No genera la cotización + Pausa incompleta cuando avisos de staff contaminan el hilo** (detectado por Jorge en prueba viva 2026-06-29). Pidió "tina y masaje" (Pausa); Luna ofreció el combo pero al elegir agregó **solo la tina** (no el masaje), y en "dame la cotización" dijo "te enviaré" pero **no creó la propuesta** (cajón vacío). Causa: el número de Jorge es **doble rol** (cliente de prueba + staff), así que 3 avisos H-038 "🔔 Nueva tarea · Recepción" entraron al MISMO hilo entre la oferta y la elección; `_historial_texto` los mete como `[Aremko]` en el contexto → descarrila al modelo liviano (gemini-flash). Sin crash en logs. **100% Django.** | Django | ✅ HECHO y vivo en prod: **(A)** `_historial_texto` (agent.py) excluye del contexto los avisos salientes `body__contains='Nueva tarea ·'` (siguen en la bandeja, solo no los lee Luna). **(B)** prompt: Pausa/combo tina+masaje = agregar las DOS patas (no solo la tina); y al aceptar la cotización, llamar `confirmar_reserva_carrito` EN ESE TURNO (prohibido prometer "te enviaré" sin ejecutar). | 2026-06-29 (agente Django) |
| H-044 | **🐛 Fecha "mañana + día" contradictoria mal resuelta** (detectado por Jorge en prueba viva 2026-06-28). Cliente dijo "tina mañana domingo" (hoy domingo 28 → mañana es lunes); Luna resolvió a "domingo 5 de julio" (una semana después) y lo llamó "mañana" → riesgo de reserva en fecha equivocada. Causa: en `resolver_fecha` (whatsapp_agent/availability.py) el PATRÓN de nombre de día se evaluaba ANTES que el relativo ("mañana/hoy"), así que "domingo" ganaba (próxima ocurrencia) e ignoraba "mañana". **100% Django.** | Django | ✅ HECHO y vivo en prod: el término relativo ahora MANDA y el nombre de día es chequeo de consistencia → si no calzan ("mañana domingo" pero mañana no es domingo) devuelve `ambiguo` con las 2 opciones para que Luna re-pregunte; helper `_termino_relativo` excluye "la mañana" (parte del día, ej. "el sábado por la mañana" sigue cayendo en sábado); "hoy domingo" ahora da HOY (antes próxima semana). Prompt reforzado (no llamar "mañana" a otra fecha). Probado standalone 10 casos. | 2026-06-28 (agente Django) |
| H-043 | **🐛 Servicio agregado DESPUÉS de cotizar no llegaba a la cotización** (detectado por Jorge en prueba viva 2026-06-28, post H-042). Si ya existía una `PropuestaReserva` y el cliente sumaba un SERVICIO (ej. un masaje tras la tina + jugo), Luna lo metía en el CARRITO (objeto separado) en vez de en la propuesta → el chat decía "nuevo total $118.500 con masaje" pero el cajón seguía en $78.500 SIN el masaje; si Deborah lo aprobaba, el masaje se PERDÍA (no se reservaba ni cobraba). Era el mismo bug que H-040 #1 (productos) pero del lado de los SERVICIOS — esa pata quedó sin cubrir. **100% Django** (el cajón ya sabe renderizar servicios; aremko-cli NO toca nada). | Django | ✅ HECHO y vivo en prod: `reserva_service.agregar_servicio_a_propuesta` (espejo del de productos) usa `recalcular_propuesta` → suma el servicio a la propuesta vigente y recalcula total + **descuento de pack tina+masaje** + resumen (dedup por slot servicio_id+fecha+hora); `agregar_servicio_carrito` (agent.py) la llama ANTES de caer al carrito. Sin migración. | 2026-06-28 (agente Django) |
| H-042 | **Cotización EDITABLE + botón CERRAR el cajón** (pedido de Jorge 2026-06-28). Fase supervisada (`WhatsAppAgentConfig.modo='borrador'`): Deborah debe poder **corregir** la cotización (cambiar cantidades, quitar líneas) ANTES de enviarla y **cerrar/descartar** el borrador desde la conversación para seguir la venta. Motivado por un bug de productos duplicados (cotización inflada ~$45k, ya corregido). Plan por fases de Jorge: 1) Deborah revisa/corrige; 2) cuando Luna la arme 100% bien, flipear `modo` y que pase directo al cliente. **Django ✅ VIVO EN PROD:** `recalcular_propuesta()` (FUENTE ÚNICA del cálculo, compartida crear/editar → el total que edita Deborah == el de la reserva) + `editar_propuesta()` + fix `cancelar_propuesta` (estado `'descartada'`, antes seteaba 'cancelada' inexistente) + `POST /api/luna/reservas/editar/` y `/api/luna/reservas/descartar/` + `servicio_id`/`producto_id` ahora en `_propuesta_reserva` (el cajón los necesita para editar). Sin migración. **Falta aremko-cli:** (A) líneas editables del cajón (cantidad + quitar) → `editar/`; (B) botón Cerrar → `descartar/`; Go passthrough de los 2 POST nuevos. Detalle + contrato en `docs/BRIEF_H-042_cotizacion_editable_cerrar.md`. | Django (API ✅) + aremko-cli (UI editar + cerrar ✅) | ✅ CERRADO 2026-06-28 — Django LISTO y vivo en prod (commit `0bca679` + IDs en `_propuesta_reserva`). **aremko-cli integrado** (commit `01cb716`, pusheado a main): (A) cajón editable en `CotizacionCajon.tsx` (stepper de cantidad + quitar línea + Guardar/Cancelar; NO calcula total en el front → Django recalcula y se relee la conversación vía `onRefrescar`) → `POST /api/v1/luna/editar-reserva`; (B) botón Cerrar con confirmación → `descartar-reserva`. Go: handlers `LunaEditarPropuesta`+`LunaDescartarPropuesta`, rutas `/luna/editar-reserva`+`/luna/descartar-reserva`, cliente bookings passthrough a `/api/luna/reservas/editar/`+`/descartar/` (acepta 2xx). `tsc` 0 + `next build` OK + `go build` OK. **Pendiente: Manual Deploy en Render + prueba viva de Jorge.** | 2026-06-28 (Django + aremko-cli) |
| H-041 | **Landing "Pausa junto al río" (`/pausa-junto-al-rio/`)** (pedido de Jorge 2026-06-26, plan de campañas Ritual + Pausa). Falta una landing propia para anunciar la **Pausa** en Google + Meta (experiencia de ENTRADA: **tina + masaje en pareja el mismo día, sin alojamiento**, **$110.000 dom-jue** / $130.000 vie-sáb para 2; es el pack tina+masaje "Pausa junto al río" que ya usa Luna). Reusar el molde de `/ritual-del-rio/` (`ritual_rio_landing.html`). CTA WhatsApp (`wa.me` a 56957902525) + evento GA4 `pausa_whatsapp_click` + Meta Pixel `478226496113915`; indexable. **100% Django**; aremko-cli solo entrega el brief + **copy completo método Sabri** en `docs/BRIEF_H-041_landing_pausa.md`, y arma las campañas Google/Meta de Pausa cuando la landing esté lista. | Django (landing) | ✅ HECHO (Django, commit `27bf6db`) — ruta `/pausa-junto-al-rio/` (INDEXABLE) + `pausa_landing_view` + template `pausa_landing.html` (copy del brief; reusa reseñas/fotos de `RitualRioLandingConfig`); CTA `wa.me/56957902525` prellenado; GA4 `pausa_whatsapp_click` (+UTM) + Meta Pixel Lead al click; `PausaLandingSitemap` en el sitemap. Precios $110.000 dom-jue / $130.000 vie-sáb (2 pers.). Validado manage.py check + compila + reverse. **aremko-cli:** ya puede armar las campañas Google/Meta apuntando a `/pausa-junto-al-rio/`. | 2026-06-26 (agente Django) |
| H-040 | **Luna: integrar productos a la propuesta + upsell + alinear el Ritual al flujo de cotización** (detectado por Jorge en prueba viva 2026-06-26, post H-039; pidió pasarlo "lo más claro posible"). **100% Django** (prompt/lógica de Luna; aremko-cli NO toca nada — el cajón ya renderiza bien lo que llega). Son 3 cosas: **(1) 🐛 BUG desincronización (prioridad):** agregar un producto cuando YA existe una `PropuestaReserva` (Ritual vía `confirmar_ritual`, o un pack) abre un **carrito nuevo separado** en vez de sumarlo a ESA propuesta → el cajón sigue mostrando la propuesta vieja (caso real: Ritual del Río $210k SIN la tabla de quesos) mientras el borrador de Luna habla de un carrito de $20k (solo la tabla); si el cliente confirma, se crea una **2ª propuesta que pisa la 1ª**. Debe **actualizar la propuesta existente** (recalcular total + descuento) en vez de abrir otro carrito. **(2) Alinear el Ritual al flujo de cotización nuevo (`b67e995`):** el camino del Ritual también debe preguntar "¿te envío la cotización?" y usar el mensaje "te preparo la cotización para que la revises" (hoy el Ritual cierra con el mensaje viejo "…datos para el pago"). **(3) 💡 UPSELL de productos (pedido de Jorge):** ANTES de generar la cotización, Luna ofrece proactivamente sumar productos del catálogo con stock (ejemplos que dio Jorge: tabla de quesos, jugo natural). Caso reproducible: "quiero un ritual del río para el miércoles" → sí → "¿puedo agregar una tabla de quesos?". | Django (lógica/prompt de Luna) + aremko-cli (render líneas de producto) | 🔵 INTEGRADO 2026-06-26 (falta prueba viva de Jorge). **Django ✅** — **(1)** `reserva_service.agregar_producto_a_propuesta`: si hay propuesta vigente suma el producto a ESA propuesta (payload+total+resumen); `agregar_producto_carrito` la usa antes de caer al carrito; `_propuesta_reserva` expone los productos como líneas con `es_producto:true`/`fecha/hora=null` (`b72656f`). **(2)** prompt Ritual/Refugio piden "¿te envío la cotización?" antes de confirmar + mensajes de `confirmar_ritual`/`confirmar_refugio` a "te preparo la cotización…" (`c1cd18e`). **(3)** upsell sutil de productos en el cierre antes del consentimiento (`7fc8c94`). **aremko-cli ✅** (resultó SÍ haber parte mía) commit `dd395b0`: `CotizacionCajon` tolera `fecha/hora=null` y muestra las líneas de producto limpias (nombre + ×N, sin "· null null · 2p"); el descuento `Σ positivos − total` sigue cuadrando (producto a precio lleno). `types.ts`: `fecha/hora` ahora `string\|null` + `es_producto?`. Validado `tsc`+`next build`. Falta prueba viva (Jorge). | 2026-06-26 (agente aremko-cli) |
| H-062 | **Crear página de "Términos y Condiciones" en aremko.cl** (detectado 2026-07-13 registrando una app en TikTok for Developers para leer estadísticas propias de videos — el formulario exige URL de Términos de Servicio y de Privacidad, ambas obligatorias). Ya existe `Política de Privacidad` (`/privacidad/`, bien armada, actualizada 1-jun-2026) pero **no hay página de Términos y Condiciones** — revisé rutas comunes (`/terminos/`, `/terminos-y-condiciones/`) y el footer del sitio, no aparece. Por ahora se usó la misma URL de Privacidad en ambos campos del formulario de TikTok (decisión de Jorge, solución temporal). No es urgente — backlog, sin fecha límite. | Django (página nueva) | ⏳ PENDIENTE — sin iniciar. Sugerencia de contenido mínimo: condiciones de reserva/pago (Flow), política de cancelación si existe, condiciones de uso del sitio; puede vivir en la misma app/template que `/privacidad/` para consistencia. | 2026-07-13 (agente aremko-cli) |
| H-063 | **Adaptar el contenido de TikTok en la cola de "Publicaciones"** (Jorge 2026-07-14: Aremko va a publicar activamente en TikTok, no solo Instagram — pidió analizar si conviene subir el mismo Reel tal cual o adaptarlo, y luego construirlo). **Decisión de Jorge tras el análisis:** se sube el MISMO video (mismo archivo editado, mismo guion/tomas/audio — sin reshoot ni re-edición) a cada plataforma por separado, como **2 entradas de cola distintas**; el copy/caption/hashtags que acompañan la publicación SÍ se adaptan por plataforma vía IA; la revisión queda **igual que Instagram** (sin gate nuevo — los videos tampoco se revisan hoy). Revisé `marketing_briefs/services.py` (`PIEZAS_FIJAS`, `explode_brief_to_publicaciones`) y `ventas/services/marketing_brief_generator.py` (`build_copywriter_prompt`, schema del copywriter) para esta propuesta — a tu criterio ajustar la implementación, pero el contrato de contenido es este: **(1) `PIEZAS_FIJAS`** — agregar `("reel_martes_tiktok", "TikTok", "reel", <mismo offset que reel_martes>, <mismo responsable_default>)` y `("reel_jueves_tiktok", "TikTok", "reel", <mismo offset que reel_jueves>, <mismo responsable_default>)` — mismo día que su gemelo de Instagram (Angélica ya tiene el video ese día, no hay razón para desfasar). **(2) Schema del copywriter** — agregar a `reel_martes` y `reel_jueves` un objeto anidado `"tiktok": {"caption_completo": "...", "hashtags": [...]}` (NO un objeto top-level paralelo — así el LLM no re-inventa `guion`/`tomas_sugeridas` con riesgo de divergencia; es el mismo video, el contenido audiovisual no cambia). Instrucciones para el LLM en ese campo: el caption de TikTok debe ser MÁS CORTO y directo que el de Instagram, tono nativo/conversacional (nada de narrativa larga), puede incluir una pregunta directa a la audiencia; los hashtags deben ser típicos de TikTok — mezcla de 1-2 amplios de descubrimiento + 3-4 de nicho/local (spa, Puerto Varas, turismo) — **nunca copiar los hashtags de Instagram tal cual** (ecosistemas de descubrimiento distintos); mantener intacta la voz Aremko (humor honesto, datos específicos sin superlativos vacíos, español latino sin voseo, máx 1-2 emojis) — la adaptación es de formato/tono nativo de plataforma, no un cambio de personalidad de marca. **(3) `explode_brief_to_publicaciones`** — al construir el `copy_json` de `reel_martes_tiktok`/`reel_jueves_tiktok`: copiar `guion`+`tomas_sugeridas`+`audio_sugerido` tal cual del hermano de Instagram (mismo video), pero `caption_completo`+`hashtags` desde el objeto `tiktok` anidado del hermano. Agregar una nota/recordatorio en la ficha (campo `notas` o similar) para quien publique: **subir el archivo de video original exportado limpio (sin marca de agua de Instagram) directamente a TikTok — nunca descargar el Reel ya publicado y volver a subirlo**, porque un video con watermark de otra plataforma penaliza el alcance en el destino (es la única diferencia operativa real entre "mismo video" y "cross-post"). **(4) Revisión/estado:** confirmo que NO hace falta tocar `estado`/`revision_veredicto` — son genéricos por fila de `PublicacionPlanificada`, una fila nueva con `canal='TikTok'` fluye por la misma máquina de estados sin cambios. **Nota aparte (no bloqueante):** `canal='TikTok'` asumido en esta propuesta — si lo nombras distinto avisame para ajustar `destinoDe()` en el frontend de aremko-cli (`dashboard/angelica/publicaciones/page.tsx`, hoy no mapea ningún destino para TikTok, lo agrego cuando confirmes el nombre exacto del canal). **CERRADO lado Django (commit `8ca90ab`, 2026-07-14):** el copywriter escribe un objeto anidado `tiktok` (caption+hashtags nativos — más cortos que IG, hashtags de descubrimiento distintos, misma voz Aremko) en `reel_martes`/`reel_jueves`; `explode_brief_to_publicaciones` deriva 2 filas `canal='TikTok'` desde el gemelo de Instagram — MISMO video (guion/tomas/audio copiados tal cual), mismo día y hora, con `nota_publicacion` recordando exportar el video limpio sin marca de agua. Fallback: si el copy no trae el objeto `tiktok`, cae al caption de IG y loguea warning (el video igual sale). Sin migración (`PublicacionPlanificada` ya es genérico por canal). Tests en `marketing_briefs/tests.py` (6/6 verdes). **Canal confirmado: `'TikTok'`** → aremko-cli puede mapear `destinoDe()`. Pendiente SOLO del lado aremko-cli: agregar el destino TikTok en `destinoDe()` y (opcional) mostrar en el cajón que la fila TikTok es cross-post del Reel. **✅ CERRADO aremko-cli (commit `0596dd4`, 2026-07-14):** `destinoDe()` reconoce `canal.includes('tiktok')` → botón "Subir a TikTok" (abre `tiktok.com/upload`, cuenta @aremko.spa) con el mismo recordatorio de exportar el video sin marca de agua de Instagram. `CopyDetalle` ahora también renderiza `copy_json.nota_publicacion` cuando Django la incluye. `tsc --noEmit` limpio. Deploy de Django verificado **live** (commits `8ca90ab`+`326a19b`, sin build_failed/update_failed pese al corte de energía reportado). **H-063 CERRADO — ambos lados en prod.** | Django + front aremko-cli | ✅ CERRADO | 2026-07-14 |
| H-064 | **Prompt de edición de imagen (IA) por cada foto de la cola de "Publicaciones"** (Jorge 2026-07-16: Angélica solo tiene el texto/copy hoy — cuando una pieza lleva foto, tiene que armar el prompt de edición a mano si quiere usar Higgsfield/Nano Banana/lo que use. Pidió que la IA genere ese prompt junto con el resto del copy, con una línea de estilo fija que mantenga el perfil boutique consistente entre piezas). Flujo pensado por Jorge: Angélica sube SU foto real + pega el prompt en el editor de imágenes que esté usando (Higgsfield, Nano Banana, cualquiera — el prompt debe ser lenguaje natural, no atado a la sintaxis de una herramienta puntual) → revisa el resultado → si está OK, publica. **Misma revisión de siempre, sin gate nuevo.** Revisé el schema real del copywriter (`build_copywriter_prompt` en `marketing_brief_generator.py`) para acotar el alcance — **ojo, esto corrige algo que yo mismo asumí mal en la conversación con Jorge: `email_engaged` NO tiene ningún campo de foto hoy** (asunto/preheader/cuerpo nomás), así que queda FUERA de esta pieza. Alcance real, 3 lugares: **(1) `gbp_post`** — agregar `prompt_imagen_ia` (string) como hermano de `foto_sugerida`. **(2) `carrusel_miercoles.slides[]`** — agregar `prompt_imagen_ia` a CADA slide, hermano de `imagen_sugerida` (cada slide es una foto distinta, cada una necesita su propio prompt). **(3) `stories_diarias[].historias[]`** — agregar `prompt_imagen_ia` a cada historia, PERO solo cuando el `tipo` de esa historia realmente implica una foto (detrás de escena, naturaleza, cliente real, recordatorio práctico) — dejarlo vacío/omitirlo para formatos sin foto real detrás (ej. encuesta/sticker interactivo). **Línea de estilo fija (constante, ej. `LINEA_ESTILO_VISUAL_AREMKO`, mismo lugar que las reglas de voz del copywriter) — Jorge la aprobó tal cual, va SIEMPRE incorporada en cada prompt que se genere:** *"Estética boutique íntima de Aremko Spa: paleta verde y dorado, luz cálida y natural, ambiente de spa junto al río discreto y de lujo silencioso. Edita la foto real que se sube tal cual es — no agregues personas, objetos ni elementos que no estén en la imagen original, no inventes la escena ni el lugar."* Instrucción para la IA al escribir cada `prompt_imagen_ia`: SIEMPRE incorporar esa línea + agregar el detalle propio de esa foto puntual (qué resaltar, encuadre, mood) derivado del `concepto`/`imagen_sugerida`/`foto_sugerida` de esa pieza — nunca inventar elementos que no estén en la foto original que Angélica va a subir. **(4) `explode_brief_to_publicaciones`** — copiar `prompt_imagen_ia` tal cual a cada `copy_json`: plano para `gbp_post`, dentro de cada slide para el carrusel, y OJO — para las historias tiene que sobrevivir el paso por `_segmentos_de_historias`/`_split_historias` (la función que ya parte el texto de historias en segmentos por día) — si ese helper reconstruye los objetos de historia sin pasar todos los campos, se pierde el prompt silenciosamente. **Explícitamente DEJADO PENDIENTE (decisión de Jorge, no construir todavía):** un campo opcional de "toque distintivo" (ej. agregar vapor, oscurecer para look nocturno) que se sumaría al prompt base solo cuando exista — la idea es esperar a ver cómo funciona el prompt base primero; si se retoma, buena candidata para colgarse del mismo mecanismo de anti-repetición que ya usan los ganchos de los reels (`ganchos_previos`/`extract_ganchos`), para que no se repita el mismo toque semana a semana. **Pendiente aremko-cli (lo hago yo cuando esto esté listo, no es parte de este handoff):** mostrar `prompt_imagen_ia` en `CopyDetalle` — campo plano con botón Copiar para `gbp_post`; un botón Copiar POR SLIDE dentro del render de `slides` (hoy ese bloque no tiene copiar individual, solo lista los slides); y algo similar para cada historia una vez que confirme cómo se ven los `segmentos` de stories en el copy_json real. **CERRADO lado Django (commit `10838f9`, 2026-07-16):** `prompt_imagen_ia` agregado al schema en `gbp_post`, en cada slide del carrusel y en las historias de FOTO (omitido en encuestas/stickers). El LLM escribe SOLO la instrucción de escena; la línea de estilo fija vive en la constante **`ESTILO_IMAGEN_AREMKO`** y se sella determinísticamente al final de CADA prompt — una vez y verbatim — en `_incorporar_estilo_imagen` (post-proceso de `call_llm_copywriter`), así el estilo boutique no depende de que el modelo copie bien la línea. `_segmentos_de_slides`/`_segmentos_de_historias` ahora **sí** pasan `prompt_imagen_ia` al segmento (era exactamente el riesgo que marcaste — sin eso se perdía en silencio y no llegaba al front por-slide/por-historia); `_merge_segmentos` lo refresca en re-explode sin pisar las fotos que Angélica ya subió. Sin migración. Tests 9/9 en `marketing_briefs/tests.py`. El "toque distintivo" quedó FUERA, como decidió Jorge. **Front aremko-cli pendiente (tu lado):** botón Copiar plano para `gbp_post` (`copy_json.prompt_imagen_ia`) y POR SLIDE/HISTORIA (`segmentos[i].prompt_imagen_ia`, ya viene en cada segmento). **✅ CERRADO aremko-cli (commit `d347685`, 2026-07-16):** `prompt_imagen_ia` agregado a `CopyDetalle` (ORDEN+LABELS → render plano con botón Copiar, cubre gbp_post) + chip nuevo `PromptImagenChip` por segmento en `RevisionSegmentos` (lee `segmentos[i].prompt_imagen_ia`, preview truncado, el botón copia el prompt COMPLETO, y se oculta del todo cuando viene `''` — historias sin foto, tal como pediste). Tipo `SegmentoHistoria` actualizado. `tsc --noEmit` limpio; Vercel auto-deploy. **H-064 CERRADO — ambos lados en prod.** | Django + front aremko-cli | ✅ CERRADO | 2026-07-16 |
| H-065 | **Revisión IA de VIDEOS (reels IG/TikTok) en la cola de Publicaciones** (Jorge 2026-07-16, plan aprobado — hoy `revision_service.py` solo revisa imágenes y su propio docstring dejó el video como "fase posterior (extracción de fotogramas)"; las filas `tipo='reel'` muestran "Los reels se revisan pronto" en el front). **Arquitectura aprobada: SIN ffmpeg ni infra nueva** — el video se sube a Cloudinary (patrón de video ya usado en este repo: `hero_video`/`video_recorrido` en ventas) y los fotogramas se derivan por URL con la transformación `so_<segundos>` + extensión `.jpg`; esos fotogramas entran al MISMO `_run_review` (OpenRouter + visión, mismo `MARKETING_REVISION_LLM_MODEL`) con un prompt específico de reels; mismos campos `revision_*`, mismo hilo background best-effort, mismo polling del front — Angélica no aprende nada nuevo. **(1) `publicacion_material`:** aceptar `.mp4`/`.mov` SOLO en filas `tipo='reel'` (tope sugerido **100 MB por archivo** — verificar con un reel real de ~50 MB que gunicorn/Render aguantan el body; las fotos de 16 MB ya pasan, el streaming a temp file de Django debería escalar), subir con el storage de VIDEO de Cloudinary (el `MediaCloudinaryStorage` de imágenes probablemente falla o degrada con resource_type video — usar el patrón que ya tienen en ventas), y capturar de la respuesta de Cloudinary `duration`+`width`+`height` → `material_meta` (equivalente video de `_medir_imagen`). **(2) Chequeos determinísticos EN CÓDIGO (no LLM), pasados como hechos al prompt igual que hace `_describir_formatos`:** orientación/ratio (debe ser 9:16 vertical) y duración vs objetivo (~30-35s = suma de los bloques del guion; tope duro IG 90s). **(3) Fotogramas:** offsets 0s, 1s, 3s (ventana del gancho), luego cada ~5s, y el último segundo (CTA) — tope ~10 frames por video. **(4) Prompt nuevo de reels** (hermano de `REVISION_SYSTEM_PROMPT`), checklist: (a) **gancho en los primeros 3s** — ¿los frames 0/1/3 frenan el scroll o abren con logo/genérico?; (b) **adherencia al guion** — `copy_json.guion` trae los bloques con texto EXACTO (gancho→contexto→moraleja→solución→cta): comparar contra los overlays visibles en los frames; (c) **legibilidad + zonas seguras** — que los textos no caigan bajo el caption inferior ni los botones de la derecha de IG/TikTok; (d) **marca de agua cruzada = corrección CRÍTICA** — en filas `canal='TikTok'` detectar watermark de Instagram y viceversa (la `nota_publicacion` de H-063 lo RECUERDA; esto lo VERIFICA); (e) lugar real vs stock (mismo criterio que fotos); (f) voz de marca en overlays (frases prohibidas, datos exactos); (g) CTA final presente en el cierre. Incluir en el prompt la limitación honesta: son fotogramas — NO juzgar ritmo de montaje, transiciones ni audio, y no inventar problemas sobre lo que no puede ver. **(5)** `revisar_video(publicacion)` o rama por extensión dentro de `revisar_material` — a tu criterio. **FUERA de esta fase (explícito):** audio/transcripción (los reels Aremko son B-roll sin voz a cámara; el mensaje va en overlays que SÍ se ven en los frames) e historias en video (follow-up natural con la misma plomería por segmento). **Costos Cloudinary (sensibilidad conocida de Jorge):** ~8-16 videos/mes × 20-40 MB, frames jpg ínfimos, el video NO se sirve al público (solo lo lee el revisor) — marginal; si molesta, retención corta después (patrón 90d existente). **Plan B documentado (NO construir de entrada, solo si la cadena real sufre con archivos grandes):** subida directa firmada browser→Cloudinary + mandar solo la URL a Django. **aremko-cli (mi lado):** (a) YA SUBIDO el fix del proxy Go — tope del body 64→110 MB y timeout 60→180s en `PublicacionMaterial` (por sobre el tope de Django para que el 400 claro de Django sea el que llegue al front); (b) cuando esto esté live: habilito accept de video en filas reel, preview `<video controls>`, saco el aviso "Los reels se revisan pronto" y pinto la duración desde `material_meta` — **CONFIRMAME el contrato final** (extensiones aceptadas, tope MB, y el shape exacto de `material_meta` para video). | Django (Fase 1: subida video + frames + prompt) + aremko-cli (Fase 2: front) | 🔵 INTEGRADO lado Django (2026-07-19) — subida de video (.mp4/.mov, SOLO reels, tope 100 MB, `upload_large` chunked resource_type=video; la metadata del upload alimenta `material_meta` con `tipo:'video'` + duration/width/height/ratio); fotogramas por URL `so_<seg>,w_720,q_auto` + `.jpg` (clip de toma: 0/1/mitad/final tope 5; reel completo: 0/1/3/cada 5s/último tope 10, el CTA siempre queda); `REVISION_VIDEO_SYSTEM_PROMPT` (marca de agua cruzada Y de herramienta IA = CRÍTICO; fidelidad a la toma y su `prompt_video_ia`; artefactos de IA; 9:16 con dimensiones reales; limitación honesta: no juzga ritmo/audio); ramas video en `revisar_segmento` (el ÚLTIMO video subido = versión vigente del clip) y `revisar_material` (reel completo); `_run_review` de fotos intacto (núcleo compartido `_chat_vision`). Cero migraciones. Tests 30/30. **Contrato final CONFIRMADO: `docs/CONTRATO_H-065_VIDEO_CLIP.md`** (extensiones, tope, shape de `material_meta` — lo que pediste). Falta SOLO tu front: botón subir video por clip y a nivel pieza + `<video>` con poster `so_0` + `.jpg`. **VERIFICADO EN VIVO 2026-07-19** (`diagnosticar_revision_video`, Jorge en la Shell de Render): encontramos y arreglamos un bug real (JSON truncado — `max_tokens` 2000→4000 + reintento único ante JSON inválido, commit `f682348`), y con eso el ciclo completo corrió con un video real: metadata Cloudinary → 4 fotogramas `so_<seg>` → modelo con visión → veredicto `con_observaciones` con correcciones específicas citando segundo exacto → persistido → limpieza. Sistema probado de punta a punta con llamada real al modelo, no un mock. | 2026-07-19 (agente Django) |
| H-066 | **Prompt de video (IA) POR TOMA en los reels** (Jorge 2026-07-16, al revisar la cola regenerada: "no veo el prompt para ayudar a procesar el video" — H-064 cubrió solo piezas con FOTO por diseño, los reels quedaron fuera; pero el objetivo original de Jorge era ayudar a Angélica **sobre todo con video**, vía Higgsfield). Extensión de H-064 con el mismo esqueleto, ahora para `reel_martes`/`reel_jueves`: **cada toma de `tomas_sugeridas` lleva su propio prompt de generación de video**, listo para pegar en Higgsfield/Kling/el que use — para cuando Angélica no pueda o no quiera FILMAR esa toma: parte de una FOTO REAL de esa escena (o un clip corto real) y la herramienta la anima (image-to-video). NO es generación desde cero — mismo principio de autenticidad de H-064. **(1) Shape del schema:** hoy `tomas_sugeridas` es un array de strings. Requisito: cada toma queda EMPAREJADA atómicamente con su prompt (nada de arrays paralelos frágiles por índice) — mi recomendación es subirlo a array de objetos `{"descripcion": "...", "prompt_video_ia": "..."}`, pero el shape final es a tu criterio; **ambos lados tienen que tolerar el shape viejo** (briefs archivados pre-H-066 siguen siendo strings — re-explode de semanas viejas no debe romper). CONFIRMAME el shape final para pintar el front. **(2) Contenido de cada `prompt_video_ia`** (lenguaje natural, no atado a una herramienta): punto de partida explícito ("esta es una foto/clip real de X que se anima"), UN movimiento de cámara concreto y sutil (push-in lento, dolly suave, orbit leve, o cámara fija con el movimiento natural del vapor/agua/lluvia), atmósfera/luz coherente con la descripción de la toma, y duración objetivo del clip según el bloque del guion al que pertenece (típico 4-5s). **(3) Línea de estilo:** misma mecánica de sellado determinístico de H-064 (`_incorporar_estilo_imagen`), pero con una **variante de video de la constante** (ej. `ESTILO_VIDEO_AREMKO`): la actual dice "edita la foto real que se sube tal cual es" — para video debería decir además que el movimiento permitido es DE CÁMARA y de los elementos ya presentes en la imagen (vapor, agua, lluvia, luz), y que NO se agregan personas/objetos/elementos ni se generan escenas sintéticas. **(4) `explode_brief_to_publicaciones`:** el copy_json de los reels ya copia `tomas_sugeridas` tal cual — con el shape nuevo los prompts fluyen solos a las filas IG **y a los gemelos TikTok de H-063** (mismo video → mismas tomas → mismos prompts, cero trabajo extra). Verificar que ningún consumidor intermedio asuma strings. **(5) Alcance:** SOLO reels — historias/carrusel/gbp ya quedaron cubiertos por H-064 con su prompt de FOTO. **FUERA (mismo criterio que H-064):** el "toque distintivo" opcional sigue pendiente hasta que se pruebe el prompt base. **aremko-cli (mi lado, cuando confirmes el shape):** render de `tomas_sugeridas` en `CopyDetalle` tolerando ambos shapes (strings viejos = como hoy; objetos nuevos = descripción + chip "🎬 Prompt de video (IA)" con botón Copiar por toma, mismo patrón del `PromptImagenChip` de H-064). **FASE 1 CERRADA lado Django (commit `d86d1b8`, 2026-07-16):** `tomas_sugeridas` de reel_martes/reel_jueves pasó a EXACTAMENTE 3 objetos `{descripcion, prompt_video_ia}` (los 3 momentos: GANCHO/DESARROLLO/CIERRE). El LLM escribe solo la escena + movimiento; la constante `ESTILO_VIDEO_AREMKO` se sella determinísticamente al final de cada `prompt_video_ia` en `_incorporar_estilo_video` (post-proceso del copywriter). Nuevo `_segmentos_de_tomas`: cada reel se explota en 3 SEGMENTOS (por clip) — IG y sus gemelos TikTok — así que **el front lee `segmentos[i].prompt_video_ia`** (mismo shape por-segmento que los slides/historias de H-064). Ambos lados toleran el shape viejo (tomas como strings de briefs archivados): en un segmento viejo `prompt_video_ia` viene `''` → escondé el chip. `brief_renderer` tolerante (no rompe el email del brief). Sin migración. Tests 12/12. **Faseo confirmado con Jorge:** Fase 1 = prompts (ESTO); Fase 2 = revisión IA POR CLIP (extiende H-065, cada segmento sube su micro-video → frames Cloudinary → `_run_review`); Fase 3 = unir los 3 clips (concatenación Cloudinary, sin ffmpeg); Fase 4 = loop conversacional (rechaza+comenta → el agente regenera ese prompt). **Front aremko-cli pendiente (tu lado):** chip "🎬 Prompt de video" con Copiar POR SEGMENTO en las filas de reel (`segmentos[i].prompt_video_ia`, escondido si `''`). **✅ FRONT aremko-cli HECHO (commit `b6a8a54`, 2026-07-16):** sección "Producción por clip" en las filas de reel (IG y TikTok) — chip 🎬 por segmento (`segmentos[i].prompt_video_ia`, copia el prompt completo, oculto si `''`), sin botón de subir foto en clips (la subida de video por clip llega con H-065 F2). Dos regresiones evitadas: (1) los reels con segmentos habrían perdido caption/guion/hashtags por el `omitTexto` de la rama de segmentos — ahora el copy del reel se muestra entero (en reels los segmentos son clips de producción, no duplican texto como en stories/carrusel) + LinkChip del sticker; (2) `tomas_sugeridas` en shape nuevo se normaliza a descripciones en `CopyDetalle` (sin `[object Object]`; el prompt no se duplica, vive en el chip del clip). Tolera shape viejo. `tsc`+`next build` limpios; Vercel auto-deploy. **FASE 1 COMPLETA ambos lados — quedan F2 (revisión por clip = H-065), F3 (unión de clips), F4 (loop conversacional).** | Django + front aremko-cli | 🟡 F1 CERRADA (ambos lados) · **F2 (revisión por clip) INTEGRADA lado Django 2026-07-19** (ver fila H-065 y `docs/CONTRATO_H-065_VIDEO_CLIP.md`; falta front aremko-cli) — F3 (unión de clips)/F4 (loop conversacional) pendientes | 2026-07-19 |
| H-070 | **Catálogo de Clips (M17) — Fase A: modelo + ingesta + etiquetado IA** (Jorge 2026-07-24, greenlight). Base del **M17 (Asistente de Publicaciones)**, validado local esta semana (`AREMKO/catalogo.json`, 282-291 clips + skills `/catalogar`, `/post-aremko`, `/historia-aremko`). **Repo: Aremko primero (cliente cero); diseñar port-friendly a DH multi-tenant.** **Construir (Django, solo servidor):** (1) app aislada `catalogo_clips` — modelo `Clip` (taxonomía validada: area, nombre_comercial, momento, estacion, vapor, decoracion, personas, permiso, calidad, keeper, etiquetas[], apto_para[], atributos{} para extras por vertical; imagen optimizada en Cloudinary + master NO se sube) + admin superuser (curación humana) + migración `0001` **drift-safe** (app aislada, sin deps cruzadas — ver AR-034); (2) `catalogo_clips/tagging.py` que **REUSA `marketing_briefs/revision_service._chat_vision`** (OpenRouter/`MARKETING_REVISION_LLM_MODEL`) + `TAGGING_SYSTEM_PROMPT` con la taxonomía Aremko (tinas por FORMA+hidromasaje; 5 cabañas Torre/Laurel/Acantilado/Tepa/Arrayán; domo masaje "Sala Sol"; pasarelas; decoración=servicio; vapor solo tinas calientes; personas→`revisar_derechos`); (3) 4 endpoints `X-API-KEY` (patrón `marketing_briefs/api_views`): `POST /marketing/api/catalogo/ingesta/` (imagen→Cloudinary `q_auto,f_auto,w_1440`→etiqueta→devuelve **draft SIN persistir**), `POST /marketing/api/catalogo/` (guarda confirmado, **upsert idempotente por `archivo`**), `GET /marketing/api/catalogo/` (lista con filtros area/nombre/keeper/vapor/momento/estado/q), `PATCH /…/<id>/`. **Semilla** de los 282-291 = script LOCAL push→nube (lo hace Jorge/aremko-cli, NO este agente; partir por ~89 keepers). **Contrato:** `docs/CONTRATO_H-070_CATALOGO.md` (shapes → luego pantalla de ingesta con botones + **render en NAVEGADOR** = Fase B, handoff aparte). **Fuera de alcance:** render/composición (Fase B, en navegador), **video** (local por ahora), multi-tenant real, auto-pick. Brief completo: `docs/BRIEF_H-070_catalogo_clips_fase_a.md`; plan producto: `datamatic-hospitality/docs/PLAN_M17_PRODUCCION_PIEZAS.md`. | Django (modelo + ingesta + etiquetado) → luego aremko-cli (pantalla + render navegador) | 🟡 CONSTRUIDO lado Django 2026-07-24 — app `catalogo_clips` (modelo+admin+migración 0001 drift-safe) + `tagging.py` (reusa `_chat_vision`, taxonomía §3) + 4 endpoints X-API-KEY (`/marketing/api/catalogo/…`, ingesta draft sin persistir, upsert por `archivo`) + `docs/CONTRATO_H-070_CATALOGO.md` + tests. **🟢 CERRADO 2026-07-24 — e2e confirmado por aremko-cli:** semilla corrida contra prod → **88/88 clips, 0 errores**, todos con `cloud_url` sirviendo (HTTP 200). Por área: tina 40 · cabaña 18 · entorno_region 17 · masaje 6 · entorno 4 · recepcion 2 · detalle 1. Upsert idempotente verificado (4 pasadas). Notas del cliente para Fase B (en contrato §1): HEIC se rechaza (convertir cliente-side o evaluar server-side), masters >16MB se rechazan (optimizar antes), estados fuera de ok/revisar/descartado deben mapearse. **Fase B (explorador + auto-pick + render en navegador) = handoff aparte.** | 2026-07-24 (Django) |
| H-071 | **Catálogo de Clips (M17) — Fase B1: Explorador web para el CM** (Jorge 2026-07-25, greenlight). Continúa H-070 (modelo + API + **89 keepers en prod**). **Decisión 2026-07-25:** el front del catálogo se construye como **vista Django server-rendered en ESTE repo** (NO en el Next de aremko-cli) — M17 es módulo DH/Django (se porta directo) + el render irá en el servidor (B2) → todo en el mismo lugar. **Construir (Django):** ruta protegida `GET /marketing/catalogo/` (`@staff_member_required`, **NO** superuser; idealmente grupo "Community Manager") server-rendered que consulta el **ORM directo** de `Clip`; galería con **miniaturas Cloudinary chicas** (derivar `w_400,c_fill` del `cloud_url`, NO servir la de 1440) + badges (keeper/vapor/momento/decoración); **filtros server-side** (`area, nombre_comercial, momento, estacion, vapor, decoracion, keeper, estado, q`) + paginación + contador; detalle `/<id>/` con taxonomía completa + botón **"Usar en historia →" DESHABILITADO** (gancho de B2). La API REST `/marketing/api/catalogo/` de H-070 **sigue viva** para externos/auto-pick. **Fuera de alcance:** render/composición (B2, en el **servidor** — handoff aparte), auto-pick brief→foto (B3), ingesta web (hoy `/catalogar`), edición (ya en admin + `PATCH`), video, multi-tenant real. **Acceso:** Jorge crea el user staff de Angélica al desplegar. Brief: `docs/BRIEF_H-071_catalogo_clips_fase_b1_explorador.md`; plan producto: `datamatic-hospitality/docs/PLAN_M17_PRODUCCION_PIEZAS.md`. | Django (vista + template server-rendered) | 🟡 CONSTRUIDO lado Django 2026-07-25 — `catalogo_clips/web_views.py` + `web_urls.py` montado en `/marketing/catalogo/` (`@staff_member_required`), galería con thumbs `w_400,c_fill,ar_4:5` derivados del `cloud_url` + badges (⭐💨🌙🎀 + borde REVISAR) + filtros server-side completos + paginación 48 + contador por área + detalle `/<id>/` con taxonomía completa y botón "Usar en historia →" deshabilitado (gancho B2) + link a admin si tiene permiso. Default excluye descartadas (toggle "ver todas"). 10 tests nuevos (24/24 app) + check OK. **🟢 CERRADO 2026-07-25:** live en prod (302→login verificado, API H-070 intacta). Acceso resuelto: **Angélica es SOCIA de Aremko y su usuario ya existe con permisos completos** (dato Jorge 2026-07-25) — entra directo a `/marketing/catalogo/`; como tiene permisos, en el detalle le aparece "Editar en el admin". **+ B1.5 INGESTA WEB construida 2026-07-25 (pedido directo de Jorge):** `GET/POST /marketing/catalogo/ingesta/` (staff) — Angélica sube la foto desde el navegador → reusa el MISMO motor de H-070 (`_subir_imagen_optimizada` + `etiquetar_imagen`, sin pasar por la API) → form de confirmación con el draft IA pre-llenado (todos los campos, datalist de nombres, checkboxes apto_para) → guardar = upsert por `archivo` (fuente `ingesta_web`) → redirige al detalle. Personas→fuerza `revisar_derechos`. HEIC/16MB con mensajes amables (iPhone convierte solo vía Safari). Botón "📤 Subir foto" en el explorador. 7 tests nuevos (31/31 app). **+ B2-A COMPONER HISTORIA construida 2026-07-25 (OK directo de Jorge):** botón "Usar en historia →" ACTIVO → `/marketing/catalogo/componer/<id>/` (staff): receta {texto, posición arriba/centro/abajo, preset velo/crema} → **URL Cloudinary compuesta** (c_fill 9:16 1080×1920 + l_text Montserrat con caja + sello de marca, texto doble-encodeado, zona segura IG) — el preview ES el JPG final; descarga con `fl_attachment`. Cero CPU server (patrón H-065); sin migración. `catalogo_clips/composer.py` + vista `componer` + template. 9 tests nuevos (40/40 app). **Siguiente: B2-B (enviar a la cola de publicaciones + pieza guardada) y B2-C (presets con Angélica).** | 2026-07-25 (Django) |
| H-072 | **M17 Creación de Historias — Fase 1: Manual + puente + registro de uso** (Jorge 2026-07-25, greenlight "con fe"). Continúa H-071 (explorador + ingesta + componer B2-A, LIVE). Es la **Fase 1 de 6** del roadmap de "Creación de Historias" del M17 (plan: `datamatic-hospitality/docs/PLAN_M17_PRODUCCION_PIEZAS.md` §11). **Responde tu pregunta B2-B.** **Construir (Django, server-rendered, estética boutique):** (1) pantalla `/marketing/publicaciones/` (staff) con tarjetas de `PublicacionPlanificada` (día/canal/tipo + copy de `copy_json` + estado + preview del material) + botón **"✍️ Crear historia"** (+ botones "🤖 Automática"/"⚡ las de hoy" **VISIBLES pero DESHABILITADOS** = ganchos Fase 2/3); (2) **puente**: el composer B2-A recibe el **texto de `copy_json` precargado** + `pub_id` (+`segmento` futuro); (3) **enganche por ORM directo** (NO re-subir; la historia ya es `cloud_url`): agregar a `material_urls`/`material_meta` + **guardar la receta** `{texto,posición,preset,clip_id,tipo:'historia'}` para re-editar + actualizar `estado`; el endpoint API `publicacion_material` queda intacto (no hace falta `material_desde_url`); (4) **registro de uso** = modelo `UsoClip(clip,fecha,publicacion?,canal)` drift-safe + `Clip.ultimo_uso` desnormalizado (cimiento anti-repetición Fase 2, default no-repetir ~45–60 días). **Regla de oro:** elección de foto/anti-repetición = código; el LLM solo conversa. **Fuera de alcance:** auto-pick (F2), batch (F3), carrusel UI (F4 — el modelo ya tiene `segmentos`), chat ajuste (F5), video (F6). Brief: `docs/BRIEF_H-072_m17_historias_fase1_manual_puente.md`. | Django (pantalla publicaciones + puente + enganche ORM + `UsoClip`) | 🟡 CONSTRUIDO lado Django 2026-07-25 — (1) `/marketing/publicaciones/` (`marketing_briefs/web_views.py`, staff): tarjetas por semana (navegable ← →) con copy/estado/preview, por-segmento cuando la pieza tiene historias múltiples, botones auto/batch visibles-deshabilitados; (2) puente: `catalogo_clips` explorador/detalle arrastran `pub_id`/`segmento` por querystring hasta el composer, que precarga el texto vía `texto_de_publicacion()` (segmento si se pidió, si no `copy_json`) — banner "Eligiendo foto para…" en explorador/detalle/componer; (3) `enganchar_publicacion` (POST, catalogo_clips): ORM directo, agrega a `material_urls`/`material_meta` (o al segmento si aplica) con la receta completa `{texto,posicion,preset,clip_id,tipo:'historia'}`, `estado→'lista'`, redirige a Publicaciones con aviso; maneja publicación/segmento inexistente sin romper (mensajes de error en el composer); (4) `UsoClip(clip,fecha,publicacion_id,canal)` + `Clip.ultimo_uso` — **decisión de diseño:** `publicacion_id` es referencia SUAVE (`PositiveIntegerField`, NO ForeignKey) para no romper el aislamiento drift-safe de `catalogo_clips` (H-070); se resuelve por id desde la vista, sin migración cruzada. API `publicacion_material` intacta. 17 tests nuevos (10 catalogo_clips + 4 marketing_briefs + 3 helper de texto) → 87/87 en ambas apps + `check` limpio. **🟢 CERRADO 2026-07-25:** migración aplicada por Jorge (`migrate catalogo_clips` → `0002`); e2e real confirmado por Jorge en prod (crear historia → enganchar → verificado). **Siguiente: Fase 2 del roadmap (auto-pick anti-repetición, usa `Clip.ultimo_uso`/`UsoClip.fecha`) = handoff aparte.** | 2026-07-25 (Django) |
| H-073 | **M17 Creación de Historias — Fase 2: Auto-pick (foto automática determinista)** (Jorge 2026-07-25, greenlight). Continúa H-072. **Diseño acordado Django+coordinador:** el brief emite `criterio_foto` estructurado por historia; el auto-pick es **100% determinista** (el LLM solo participa al escribir el brief, NO en la selección de foto). **Parte A (marketing_briefs):** actualizar el generador del brief para emitir `criterio_foto {area, nombre_comercial, vapor_preferido, decoracion, momento}` (enums de `Clip`, patrón `prompt_imagen_ia` de H-064) por historia; `_segmentos_de_historias` lo guarda por segmento (pieza de 1 imagen → en `copy_json`); aparece desde el próximo brief; **fallback** historia sin criterio = deshabilitar "🤖 Generar" y dejar "✍️ Manual" (NO adivinar con LLM). **Parte B (catalogo_clips):** `seleccionar_clip(criterio, dias=60, permitir_personas=False)` = `filter(area[, nombre_comercial__icontains si no vacío][, vapor__in=['sí','sí (IA)'] si vapor_preferido][, decoracion][, momento], personas=False, estado='ok').exclude(ultimo_uso__gte=hoy-60d).order_by('-keeper','ultimo_uso')` con **degradación en cascada** (sin keeper → relajar momento/decoracion → permitir repetir → permitir personas → nada; cada nivel con `aviso` auditable). **UI:** activar "🤖 Generar" por historia → auto-pick → compone (composer B2-A + texto del brief) → preview con foto/criterio/aviso; acciones ✓ Aprobar (registra `UsoClip`) / 🔄 Otra foto / ✍️ Manual. "⚡ las de hoy" sigue DESHABILITADO (F3). **Fuera de alcance:** batch (F3), carrusel (F4), chat (F5), video (F6). Brief: `docs/BRIEF_H-073_m17_historias_fase2_autopick.md`. | Django (Parte A brief + Parte B auto-pick + UI) | 🟢 CERRADO — Parte A (schema `criterio_foto` en gbp_post/slides/historias + `_sanear_criterio_foto`/`_sanear_criterios_foto` defensivos en `marketing_brief_generator.py`, que vive en `ventas/services/`, NO en `marketing_briefs`) + Parte B (`catalogo_clips/seleccionar.py::seleccionar_clip`, cascada de 6 niveles, `area`/`nombre_comercial`/`vapor_preferido` duros en toda la cascada) + UI (`auto_generar` en `catalogo_clips/web_views.py`, redirige al composer B2-A con banner nivel/aviso + 🔄 Otra foto; botón "🤖 Generar" activo por historia solo si trae `criterio_foto`) construidos y con 118/118 tests pasando (catalogo_clips+marketing_briefs) + `check` limpio. **E2E validado en prod 2026-07-25**: Jorge regeneró el brief (`generate_weekly_marketing_brief --dry-run`), `criterio_foto` apareció y "🤖 Generar" salió selectivamente (solo en historias con criterio; video/audio/reseña/quiz/personas quedaron en Manual — fallback OK). Probó en Sábado 25/07 · Historia 3 → auto-pick eligió `Tronador-Calbuco` (tina, sin personas, con vapor), banner mostró "se relajó momento/decoración" (nivel 3, transparente). La publicación #31 (26/07, ya enganchada a mano) quedó intacta — protegida por la lógica pre-existente de `_upsert` (`estado in pendiente/no_aplica`). Observación no bloqueante (copy, no auto-pick): notas de producción entre corchetes salen literales en la imagen compuesta → **P-21** en `docs/PENDIENTES.md`. | 2026-07-25 (Django) |
| H-074 | **M17 Creación de Historias — Fase 3: Batch "Generar las de hoy"** (Jorge 2026-07-25, greenlight). Continúa H-073 (auto-pick, LIVE). Reusa `seleccionar_clip(…, excluir_ids)` + composer B2-A + enganche de Fase 1. **Construir:** activar **"⚡ Generar las de hoy"** (hoy deshabilitado) → genera **EN LOTE** las historias auto-generables del día (con `criterio_foto` y sin material aún; **respeta lo ya enganchado**, p.ej. la #31); **anti-repetición dentro del lote** acumulando los `clip_id` elegidos en `excluir_ids` por iteración (+ diversidad de `nombre_comercial` en el lote si hay stock, deseable); por cada una: `seleccionar_clip` → compone → **engancha** (`estado='lista'` + miniatura + receta + `UsoClip`); **resumen** al terminar (N generadas / M con aviso de degradación / K a Manual sin criterio). **"lista" ≠ "publicada"** (nada sube solo; Angélica revisa las miniaturas y ajusta 🔄/✍️; re-generar **REEMPLAZA** el material, no acumula). **Fuera de alcance:** carrusel (F4), chat (F5), video (F6); historias sin `criterio_foto` no entran al batch (quedan solo Manual). Brief: `docs/BRIEF_H-074_m17_historias_fase3_batch.md`. | Django (batch + anti-repetición en lote + resumen) | 🟡 CONSTRUIDO — `generar_lote(pub_id)` (catalogo_clips/web_views.py, POST-only): itera segmentos, salta los que ya tienen material (respeta lo ya enganchado, incluida la #31), salta los sin `criterio_foto` (→ Manual); por cada elegible usa `_elegir_diverso` (envuelve `seleccionar_clip` con `excluir_ids` acumulado — anti-repetición dura — + intenta variar `nombre_comercial` hasta 4 veces, degradando a repetir si no hay stock) → compone (B2-A) → engancha + `UsoClip`. Resumen final vía querystring (`?lote=<id>&resumen=...`). Botón por tarjeta de historias: "⚡ Generar las de hoy" (si `pub.dia`==hoy) / "⚡ Generar todas" (otro día) — solo visible si queda algo por auto-generar; el atajo global de arriba se activa igual si hoy tiene pendientes en la semana en pantalla. Fix acompañante: `publicaciones_lista` leía `urls[0]` (primera foto) en vez de `urls[-1]` (vigente — mismo criterio que ya usa `revision_service` para fotos/video); sin este fix, "re-generar reemplaza" no se veía reflejado en la miniatura aunque el dato ya fuera correcto. 17 tests nuevos (135/135 en catalogo_clips+marketing_briefs) + `check` limpio. Pendiente: deploy + prueba en prod antes de cerrar 🟢. | 2026-07-25 (Django) |
| H-075 | **M17 Historias — corrección de tipografía de marca + fix de saltos de línea + límite de glyphs ¿/¡** (Jorge reportó en prod: "el tipo de letra que definimos en el manual no es la que se muestra en las historias"). No es una fase nueva del roadmap — es un hilo de calidad visual sobre las historias que ya generan H-072/073/074. | Django | 🟢 CERRADO — **Corrección de fuente** (2 intentos): primero probé "Cormorant Garamond" por inferencia de otras plantillas de la web (equivocado — Jorge lo frenó en vivo, interrumpiendo el deploy). Corrección real de Jorge: **Glacial Indifference, peso regular, sin negrita** (decisión ya tomada y documentada en la skill local `historia-aremko`, con el `.otf` ya incluido ahí — aprendizaje: revisar skills locales por decisiones de marca YA tomadas antes de inferir). Como no es Google Font, se subió a Cloudinary como recurso `raw`/`authenticated` (`subir_fuente_historias`, corrido por Jorge en la Shell de Render: "✅ Subida OK... bytes=54752" + render de prueba HTTP 200) y se referencia en `l_text` por su `public_id` CON extensión — gotcha nuevo: un `public_id` de fuente personalizada no admite guion_bajo. Commits `03763e9` (equivocado) → `10517ae` (correcto) → `284105f` (doc dossier). **Fix de saltos de línea** (Jorge: "cuando edito el texto y separo el párrafo en distintas líneas... al actualizar vista previa mantiene el párrafo completo"): `_cap()` colapsaba TODO el whitespace con `' '.join(texto.split())`, incluidos los `\n` intencionales, antes de que el texto llegara a `_txt()`. Fix: limpiar espacios repetidos solo DENTRO de cada línea, preservando los saltos entre líneas (`_txt()` ya sabía encodearlos — verificado empíricamente que Cloudinary respeta `%250A` como salto real, igual mecanismo que la coma/slash). Commit `fa29562`, 4 tests nuevos (142/142), **deployado y LIVE verificado** (`dep-d9j3srf41pts73au6210`). **Límite de glyphs ¿/¡** (hallazgo propio, reportado proactivamente antes de que Jorge lo notara): Glacial Indifference NO tiene ¿ (U+00BF) ni ¡ (U+00A1) — confirmado con `fontTools`. Jorge pidió buscar otra versión con esos glyphs; investigación (fontTools sobre el archivo propio + 1001fonts.com de forma independiente: mismo 2015, mismo conteo de glyphs + comentario real de una usuaria hace 8 años quejándose de otro hueco de glyphs nunca resuelto) concluyó que **es la única versión que existe — nunca se amplió**. **Decisión de Jorge: no se toca código ni fuente — quien redacte evita el signo de apertura ¿/¡** (se descartaron tanto el aviso automático en el composer como cambiar de fuente). Documentado en `composer.py` (comentario junto a `FUENTE`) + gotcha #8 del dossier M17→DH. | 2026-07-26 (Django) |
| H-076 | **M17 Historias — preset "transparente" (sin caja) + "3 líneas con tamaño"** (Jorge, 2026-07-26, a partir de comparar 2 historias reales de Aremko: una con caja crema, otra sin caja con precio grande + resto chico). Pidió una tercera opción de estilo sin caja + la posibilidad de dar tamaños distintos a hasta 3 líneas del texto. | Django | 🟢 CERRADO — **Preset `transparente`**: sin `caja` (texto directo sobre la foto) + `e_shadow:60,co_black` de legibilidad (verificado con diff de píxeles + inspección visual contra Cloudinary real que el shadow SÍ se nota, no es un no-op). **Modo "3 líneas con tamaño"**: `TAMANOS={chico:36, normal:58, grande:80}`; cada línea es su PROPIA capa `l_text` (Cloudinary no soporta tamaños mixtos dentro de una sola capa) — se apilan sin superponerse con `_offsets_abajo/_offsets_arriba/_offsets_centro`, fórmulas basadas en un multiplicador tamaño→alto (`_ALTURA_LINEA=1.35`) **verificado empíricamente** (curl + inspección visual de 3 líneas reales apiladas en cada una de las 3 posiciones, sin overlap, antes de fijar las fórmulas). UI elegida por Jorge entre 3 alternativas: 3 campos de texto separados (línea 1/2/3), cada uno con su selector Grande/Normal/Chico — no un textarea con marcadores ni un editor visual. **Retrocompatible a propósito**: `receta_normalizada(..., lineas=None)` — una receta sin `lineas` (todas las ya guardadas) renderiza EXACTAMENTE como antes (mismo código legado intacto, sin pasar por la ruta nueva); con `lineas`, `url_historia` usa esa ruta y el `texto` clásico se ignora. Persistencia: `item['receta']['lineas']` se guarda en `material_meta` (enganche manual, batch, y "Ver/Editar") para poder reabrir/editar. `marketing_briefs/web_views.py::_qs_ver_editar` centraliza la construcción del querystring de reapertura (antes se armaba a mano en 4 lugares del template; ahora soporta también `linea1..3`/`linea{N}_tamano`). 25 tests nuevos (157/157 catalogo_clips+marketing_briefs) + `check` limpio. Verificado end-to-end: URL generada por el código real (no a mano) → descargada → inspeccionada visualmente, coincide con el ejemplo real que Jorge mostró. **Fix del mismo día, encontrado en el primer uso real en prod:** Jorge notó un "signo extraño" en el sello + poco protagonismo. Causa confirmada con fontTools: `SELLO_TEXTO` usaba "·" (punto medio, U+00B7) que **tampoco existe** en Glacial Indifference (tercera grieta de esta fuente, después de ¿/¡) — a diferencia de esas, esta sale en el 100% de las historias, así que se corrigió en código (no se dejó a criterio de quien redacta): "·" → "•" (bullet, U+2022, sí existe en la fuente). Además se agregó `e_shadow:60,co_black` al sello **siempre** (en los 3 presets, a diferencia del texto principal donde solo lo lleva 'transparente') — el sello se notaba poco contra fotos de tonos cálidos. 2 tests actualizados + 1 nuevo (158/158). **Segundo fix, mismo hilo:** Jorge seguía viendo poco protagonismo tras el fix del bullet+sombra y sugirió negrita. Probé `_bold_` de Cloudinary contra el recurso personalizado (diff de píxeles: 0 — Cloudinary NO sintetiza negrita sobre fuentes propias subidas, solo sobre las que gestiona internamente como Montserrat). Se descargó el peso **Bold real** de Glacial Indifference (mismo autor/licencia SIL OFL, mismos huecos de glyphs esperables — ¿/¡/· ausentes, verificado con fontTools antes de usarlo) y se subió como SEGUNDO recurso Cloudinary (`GlacialIndifference-Bold.otf`, mismo comando `subir_fuente_historias` extendido a una lista de fuentes). Nueva constante `FUENTE_SELLO` (Bold) — el cuerpo del texto sigue en `FUENTE` (regular, decisión de Jorge 2026-07-20 sin cambios); el sello es la única pieza en negrita. Pendiente: correr `python manage.py subir_fuente_historias` de nuevo en la Shell de Render (re-sube Regular sin cambios + sube Bold por primera vez) antes de que el sello en negrita se vea en prod. | 2026-07-26 (Django) |
| H-077 | **Cotización editable con "+ Agregar ítem" (ambientaciones + productos) en el cajón de la bandeja** (Jorge 2026-07-29, plan aprobado — Fase 1 de "la cotización se modifica hasta quedar como la pide el cliente; ambientaciones las ofrece Deborah en la conversación"). Deborah puede AGREGAR líneas a una propuesta pendiente — ambientaciones (Servicio, categoría "Ambientaciones") y productos Comestibles/Bebestibles (tablas/jugos/café/torta) — además de cambiar cantidades/quitar (H-042). **Django:** endpoint nuevo `GET /api/luna/catalogo-agregables/` (X-API-Key; solo activos con precio real `>1` — excluye cortesías $0 y centinelas $1; los precios son referencia para elegir, al guardar `editar_propuesta` re-lee el catálogo como siempre). SIN migraciones; `editar_propuesta`/`recalcular_propuesta` NO cambian (ya aceptaban cualquier composición y no validan slots). **aremko-cli:** proxy Go `GET /api/v1/inbox/catalogo-agregables` + picker en `CotizacionCajon` (modo editar): buscador + grupos Ambientaciones/Productos; la ambientación hereda fecha/hora de la primera línea de servicio con fecha (tina-first, criterio decoraciones) y entra con `cantidad=1` (OJO: para servicios Django cobra `precio_base × cantidad_personas`); ítem repetido → incrementa cantidad (merge, no duplica línea). **Fase 0 verificada contra catálogo PROD** (Shell de Jorge): ambientaciones (R1 $32k/R2 $68k/decoraciones), tablas, jugos y café existen con precio; **falta Producto "Torta" standalone** — Jorge lo crea en admin cuando quiera y aparece solo en el picker. Recomendado además: desactivar ambientaciones de temporada pasada (Mama 2025, Mamá $25k, San Valentín) y corregir typo "Tabla Jamòn". **Fase 2 pendiente aparte** (Luna agrega ambientaciones sola cuando el cliente aprueba lo ofrecido — toca el core del agente, se hará con el manual ya rodando). Tests del endpoint en `ventas/tests_catalogo_agregables.py` — la suite local NO puede correr por el drift AR-033/AR-034 preexistente (crear el test DB revienta en `ventas_premio.tramos_validos`); validado con `manage.py check` limpio + smoke en Docker (auth 401/403 ✓; la query es la misma que corrió OK en prod durante Fase 0). | Django (endpoint) + aremko-cli (proxy Go + UI cajón) | 🟢 LIVE VERIFICADO E2E (2026-07-29): Django 403 sin key / sitio 200; vía backend Go en Render → 200 con 10 ambientaciones + 69 productos reales de prod (filtros calzan: 13 − 1 inactiva − 2 en $0). Falta solo la prueba visual de Jorge en la bandeja + sus 3 ítems de admin (crear "Torta", desactivar ambientaciones vencidas, typo "Jamòn"). | 2026-07-29 (ambos lados) |
| H-078 | **Luna: rollback de carrito en turnos escalados + cotizador oficial de experiencias** (Jorge 2026-07-29, diagnóstico aprobado tras caso real en su conversación de prueba: turno A agregó la Tina Hornopiren al carrito mientras "armaba" opciones y fue frenado por el guardián H-045 → tina fantasma visible en la bandeja; turno B redactó "masaje 16:45" — hora INVENTADA por el modelo, no salió de ninguna tool — y se envió al cliente; turno C intentó agregar ese 16:45 → `validar_hora_es_slot` lo rechazó ("hay 16:00") → escalar. Verificado en código que `disponibilidad()` y el validador usan la MISMA grilla `extraer_slots_para_fecha`, o sea el motor jamás habría ofrecido 16:45). **F1 — rollback:** módulo nuevo `whatsapp_agent/rollback.py` (snapshot en memoria de carrito+propuesta pendiente al inicio del turno; restauración si el turno ESCALA o revienta con excepción — un turno sin mensaje no deja residuo; deshace también propuestas CREADAS en el turno → descartadas; best-effort, nunca rompe el flujo) + `_producir_borrador` ahora es wrapper de `_producir_borrador_inner`. Sin migraciones. **F2 — cotizador:** tool nueva `alternativas_experiencia(tipo, fecha, personas)` que envuelve el MISMO motor H-061 de las olitas (`construir_alternativas`) — las opciones (horas+precios+`texto_sugerido`) las arma CÓDIGO y el modelo solo las presenta; `fecha` se resuelve con `resolver_fecha` (devuelve `dia_semana`), `personas` obligatorio (sin asumir 1), tope 8 alternativas al contexto + `total_alternativas`. Regla dura nueva en el system prompt: cotizar experiencias SOLO con esta tool, presentar tal cual, y NO agregar al carrito mientras el cliente elige (agregar recién cuando confirma). Tests en `whatsapp_agent/tests/test_h078_rollback_y_cotizador.py` (rollback con DB + tool con mocks; la suite local sigue bloqueada por el drift AR-033/AR-034 — validado con py_compile + `manage.py check` en Docker). F3 (validar horas HH:MM en el texto saliente) quedó descartada por ahora: F2 la vuelve redundante si el modelo obedece la regla, y el guardián H-045 + validador de carrito siguen de red. | Django | 🟡 CONSTRUIDO — pendiente deploy + re-test de Jorge (misma conversación de prueba: Borrar (test) → "quiero reservar pausa para el próximo miércoles") | 2026-07-29 (Django) |
| H-079 | **Ambientaciones en el catálogo vivo de Luna + carrito EN CURSO editable (H-046 Fase 2)** (Jorge 2026-07-29, "Construye los dos", tras su test en vivo: preguntó "¿y qué ambientaciones tienes?" y Luna alucinó poesía del "Río Pescado y bosque nativo" — causa confirmada: `catalogo_vivo()` solo traía `TIPOS_PRINCIPALES`, las ambientaciones tipo 'otro' no existían para el modelo; además el carrito pre-cotización era solo-lectura y no había dónde agregarlas a mano). **(1) Grounding:** `construir_catalogo_texto(..., ambientaciones=None)` + sección "AMBIENTACIONES" en `catalogo_vivo()` (activas, categoría Ambientaciones, precio>1 — mismo criterio que el picker H-077; sin filtrar `publicado_web` a propósito). Instrucción embebida: mencionarlas SOLO del catálogo con precio, no inventar qué incluyen, y NO agregarlas al carrito (el equipo las suma — el auto-agregar cuando el cliente acepta sigue siendo la Fase 2 congelada). **(2) Carrito editable:** endpoint nuevo `POST /api/luna/carrito/editar/` (X-API-Key; reemplazo TOTAL servicios+productos re-leyendo precios del catálogo + `_recalcular_totales` con descuento; listas vacías → ELIMINA el carrito) + `_carrito_en_curso.editable=True` (inbox_omnicanal). **aremko-cli:** proxy Go `POST /api/v1/luna/editar-carrito` + lápiz "Corregir carrito" en el bloque gris del cajón → REUSA el mismo editor de la cotización (cantidades/quitar/+ Agregar ítem con ambientaciones y productos, herencia de fecha de la tina) con `editTarget: 'propuesta'|'carrito'`; la cotización sigue exigiendo ≥1 servicio, el carrito puede quedar vacío (aviso "Guardar VACIARÁ el carrito"). Identidad vía props nuevos `editCanal`/`editExternalId` (WhatsApp: phone; IG/Messenger: IGSID/PSID → cubre los 3 canales). Validado: `manage.py check` limpio + tsc/next build + go build OK. | Django (grounding + endpoint) + aremko-cli (proxy Go + editor cajón) | 🟢 LIVE VERIFICADO (2026-07-29 20:38): Django d0b9abb live + sitio 200; ruta Go→Django viva (probe a external_id inexistente devolvió el carrito_not_found controlado a través de toda la cadena). Falta solo la prueba visual de Jorge: preguntar ambientaciones a Luna + lápiz del carrito. | 2026-07-29 (ambos lados) |
| H-080 | **Luna agrega ambientaciones al carrito cuando el cliente CONFIRMA** (Jorge 2026-07-29, encontrado probando H-079 en vivo: pidió "reservame la R1 de 32.000" y Luna se AUTO-escaló con "producto no encontrado en el catálogo" — el motivo no existe en el código, fue `[ESCALAR:]` del propio modelo. Callejón sin salida de diseño: H-079 le dio la LISTA pero la instrucción decía "no la agregues" y además no tenía CÓMO — el override por nombre de `agregar_servicio_carrito` solo mira `TIPOS_PRINCIPALES` y las ambientaciones son tipo 'otro'; tampoco son Producto. El rollback H-078 sí protegió el carrito). **Fix (cierra el loop que Jorge pidió el mismo día: "que el agente lo agregue automáticamente si el cliente lo aprueba"):** (1) `_resolver_ambientacion(nombre)` — resolución determinista por nombre contra la categoría Ambientaciones (activas, precio>1, mismo filtro del catálogo/picker); solo resuelve si matchea UNA (0 o 2+ → None, sin adivinar); enchufada como segundo intento del override por nombre. (2) `_es_ambientacion(id)` + **cantidad forzada a 1 EN CÓDIGO** para ambientaciones (el precio de servicio es base × cantidad_personas → con 2 personas se duplicaría a $64k; guardia independiente de lo que pase el modelo; también salta el gate falta_personas). (3) Instrucción del catálogo actualizada: agregar SOLO con confirmación explícita del cliente, `nombre_servicio` EXACTO de la lista, cantidad 1, misma fecha/hora de la tina; sin tina elegida → primero la tina. Lo que sigue congelado: que Luna decida CUÁNDO ofrecer ambientaciones proactivamente según contexto (el tema complejo original). Tests en `tests/test_h080_ambientacion_carrito.py`. **Cross-fix mismo día (2ª ronda de Jorge):** el borrador repitió "no pude encontrar la R1 en nuestro catálogo" — paráfrasis EXACTA del error `producto_no_resuelto`: el modelo trató la ambientación como PRODUCTO (`agregar_producto_carrito`; venía de usar esa tool para el jugo). Fix determinista: si el nombre no resuelve como producto pero SÍ como ambientación única → `_agregar_ambientacion_al_carrito()` (cantidad 1; fecha/hora heredadas de la primera línea de servicio con fecha — propuesta vigente primero, si no carrito; reusa fusion H-043 o CarritoService). Solo en el handler de CARRITO (el de reserva-existente H-060 quedó igual a propósito: otro flujo/gate). **Tercer fix, misma noche (3ª ronda de Jorge):** el borrador pasó a "la R1 no está disponible en este momento" — paráfrasis exacta del gate `publicado_web` de `CarritoService.agregar_servicio` (la resolución YA funcionaba; las ambientaciones están activas pero NO publicadas en la web a propósito — complementos internos). Fix: para categoría Ambientaciones basta `activo` (el gate publicado_web sigue intacto para la oferta principal). La capa Deborah (picker H-077/H-079 vía recalcular_propuesta/editar_carrito) nunca tuvo este gate. | Django | 🟢 LIVE + fix publicado_web desplegándose — re-test: "agrégame la R1" → R1 $32.000, total con descuento intacto | 2026-07-29 (Django) |
| H-081 | **Luna ofrece UNA sola opción a la vez al cotizar** (Jorge 2026-07-30: "Es abrumador ver que Luna te ofrezca 3 opciones. Se ve muy de robot" — la lista de 4 bullets venía de la instrucción H-078 "presenta las opciones tal cual" sobre una tool que devuelve hasta 8). **Decisión de Jorge (AskUserQuestion): la MÁS BARATA primero**; desempate a igual precio: horario MÁS TARDE (regla de la casa). Cambios: la tool `alternativas_experiencia` ahora ordena en CÓDIGO y devuelve `recomendada` (1 sola) + `otras_alternativas` (respaldo topeado a 8) + `total_alternativas`; instrucción y REGLA DURA del prompt reescritas: ofrecer SOLO la recomendada redactada natural en 1-2 frases (PROHIBIDO listas/asteriscos/varias opciones), cerrar SIEMPRE preguntando si acomoda o prefiere otro horario/otra tina, y ante un ajuste ofrecer UNA sola de las otras (la que mejor calce). Sin disponibilidad → recomendada:null + mensaje cálido de otra fecha. Beneficio lateral: mata la ambigüedad "la con llaima" (H-078 obs) — con 1 oferta, "sí" es inequívoco. Tests actualizados + 2 nuevos (orden barata→tarde, sin disponibilidad). | Django | 🟢 LIVE (2026-07-30 07:51, deploy 9a28a8d verde + sitio 200) — pendiente re-test de Jorge: pedir pausa → UNA propuesta redactada (la más barata, horario más tarde), sin listas; "¿con hidromasaje?" → una alternativa más | 2026-07-30 (Django) |
| H-082 | **Doctrina de variantes: preguntar el gusto, no elegirlo — y ambientaciones también de a UNA** (Jorge 2026-07-30, probando H-081 en vivo: (1) "Agrega un jugo" → Luna eligió FRAMBUESA sola en vez de preguntar el sabor; (2) "¿Tienes ambientaciones?" → listó R1 $32k o R2 $68k + chocolates $16k — la instrucción H-079 del bloque decía literalmente "menciona estas opciones", anterior a la regla H-081 que solo cubría la tool de experiencias). **Doctrina completa:** hay "mejor opción" objetiva (horarios/combos) → proponer UNA (H-081); es gusto puro (sabor/color/ocasión) → PREGUNTAR la variante inline en una frase, NUNCA elegir por el cliente. Cambios: (a) REGLA DURA nueva en el prompt (variantes de preferencia: jugos/cafés/colores/ocasiones — pregunta en una frase, sin viñetas ni precios repetidos); (b) bloque AMBIENTACIONES reescrito como PROTOCOLO DE VENTA: pregunta genérica → preguntar ocasión (romántico/cumpleaños/detalle dulce) → ofrecer UNA (la más barata de la ocasión) mencionando que existe versión superior; reconocer si ya hay una en el carrito; agregar SOLO con confirmación explícita (guardias H-080 intactas). Solo texto emitido por código (prompt+grounding), sin cambios de lógica. | Django | 🟢 LIVE (2026-07-30 08:04, deploy e6be113 verde + sitio 200) — pendiente re-test de Jorge: "agrega un jugo" → pregunta el sabor; "¿tienes ambientaciones?" → pregunta la ocasión y ofrece UNA. Pregunta abierta: cómo llegó la R1 al carrito de su captura (¿chat/lápiz o sola? si sola → cazar agregado prematuro) | 2026-07-30 (Django) |
| H-083 | **Gate anti agregado prematuro de ambientaciones (confirmación del cliente EN CÓDIGO)** (Jorge 2026-07-30: "Cuando pregunté tienes ambientaciones inmediatamente agregó al carrito la de 32000" — el modelo ejecutó una ACCIÓN ante una pregunta INFORMATIVA, saltándose la instrucción de confirmar; el turno no escaló, así que el rollback H-078 no aplicaba. Lección de la casa confirmada otra vez: lo importante se fuerza en código, no en prompt). **Fix:** `_cliente_confirmo_ambientacion(mensaje, nombre)` — función PURA: True solo si el último mensaje del cliente NOMBRA la ambientación (palabra significativa del nombre, excluyendo genéricas como "ambientación/decoración") o trae ASENTIMIENTO explícito sin negación ("sí/dale/esa/agrégala/resérvame..."); "¿tienes ambientaciones?" → False; "no, esa no" → False (negación mata asentimiento; nombrarla gana a todo). Aplicada en LOS DOS caminos de agregado (agregar_servicio_carrito y el cross-add de agregar_producto_carrito): sin confirmación → error `requiere_confirmacion` con instrucción de ofrecer primero. El closure ejecutor captura `mensaje` del turno (scope léxico de `_producir_borrador_inner`). **Validación: 14/14 casos ejecutados EN VIVO** (función extraída del archivo y corrida con python3, sin depender de la suite bloqueada) + tests en `test_h080_ambientacion_carrito.py` + check limpio. | Django | 🟢 LIVE (2026-07-30 08:12, deploy d4ebf80 verde + sitio 200) — pendiente re-test de Jorge: "¿tienes ambientaciones?" NO toca el carrito; "agrégame la R1" sí la agrega | 2026-07-30 (Django) |
| H-084 | **Gate anti "variante elegida por el modelo" en productos** (Jorge 2026-07-30, probando H-082/H-083: "Si un jugo" → el modelo agregó Jugo Natural de FRAMBUESA al carrito Y ADEMÁS redactó "¿de qué sabor te gustaría?" en el mismo turno — texto y acción contradictorios; H-082 arregló el texto, la acción solo se arregla en código, espejo del H-083 de ambientaciones). **Fix:** `_cliente_eligio_producto(mensaje, historial, producto_id, candidatos=None)` — un producto entra al carrito SOLO si el CONTEXTO del cliente lo desambigua: puntaje por palabras significativas del nombre presentes en su mensaje (con singular/plural español: jugo↔jugos, natural↔naturales, jamón↔jamones) + cobertura del nombre como desempate ("tabla de quesos" le gana a la Mixta); si el mensaje es asentimiento puro ("sí"), se usa la última línea `[Aremko]:` del historial (la oferta a la que asintió) — oferta específica pasa, oferta doble bloquea; empate entre variantes o producto distinto al pedido → error `variante_no_confirmada` con las opciones para preguntar. Candidatos inyectables → función PURA testeable sin DB. **Validación: 11/11 casos ejecutados EN VIVO** (incl. el bug real, "sí" a oferta específica vs doble, cobertura Quesos vs Mixta, y pidió-piña-quiso-frambuesa) + tests en `test_h080_ambientacion_carrito.py` + check limpio. Con esto quedan gateados EN CÓDIGO los 3 caminos de agregado conversacional: ambientación-como-servicio (H-083), ambientación-como-producto (H-083) y producto con variantes (H-084). | Django | 🟢 LIVE (2026-07-30 08:28, deploy 620b329 verde + sitio 200) — pendiente re-test de Jorge: "si un jugo" → pregunta el sabor SIN tocar el carrito; "de frambuesa" → recién ahí lo agrega | 2026-07-30 (Django) |
| H-085 | **Personas del cliente mandan en turnos de asentimiento (anti "pausa para 1")** (Jorge 2026-07-30: "Pedí pausa pero agregó un solo masaje. Primera vez que ocurre" — la oferta H-081 salió perfecta para 2 y ante el "Si" el modelo agregó tina Y masaje con cantidad_personas=1 → $65.000 sin descuento de pack; varianza pura del modelo, ~1 de 6 corridas). **Fix determinista:** `_es_asentimiento_puro(mensaje)` (asentimiento corto sin números/negación/datos nuevos) + `_personas_declaradas_por_cliente(historial)` (última declaración del cliente en líneas `[Cliente]:`: respuesta suelta "2", "N personas", "para/somos N", números en palabra). En `agregar_servicio_carrito` (camino no-ambientación): si el turno es asentimiento puro y el cliente declaró N ≠ lo que pasó el modelo → OVERRIDE a N (en un "sí" el cliente acepta lo ofrecido, no pudo haber cambiado las personas; si el mensaje trae datos nuevos —"sí, pero para 3"— NO es puro y no se fuerza nada). Bonus: la instrucción de la recomendada ahora pide mencionar para cuántas personas es. **Validación: 16/16 casos ejecutados EN VIVO** (asentimiento 9/9 + personas 7/7 + el caso real Si+"2"→override) + tests puros + check limpio. | Django | 🟢 LIVE (2026-07-30 08:49, deploy 49241ff verde + sitio 200) — pendiente re-test de Jorge: pausa para 2 → "Si" → carrito con tina y masaje en 2p + descuento de pack | 2026-07-30 (Django) |
| H-086 | **Luna responde QUÉ INCLUYE una ambientación (usa `descripcion_web` del admin)** (Jorge 2026-07-30: "¿De dónde sacaría una respuesta Luna si le pregunto qué contiene R1 o C1?" — diagnóstico: de NINGÚN lado; el bloque H-079 solo pasaba nombre+precio, a diferencia de los servicios principales que sí llevan descripcion_web. Con la instrucción de no inventar, en el mejor caso decía "confirmo con el equipo"; en el peor inventaba como ya pasó. Jorge confirmó que el campo `descripcion_web` de Servicio ya EXISTE y ya tiene contenido redactado para las ambientaciones —incluido el "Incluye:"—, así que se reusa en vez de crear campo nuevo). **Fix:** `catalogo_vivo()` ahora trae `descripcion_web` + `informacion_adicional` (fallback) de cada ambientación, pasadas por `normalizar_detalle_ambientacion()` (nueva, pura): esas descripciones están redactadas para la WEB (párrafo + "Incluye:" + viñetas `*` y saltos de línea) y entrar crudas tenía 2 problemas — las viñetas con asterisco se le pegan al modelo, que tiene PROHIBIDO usarlas (H-081/H-082), y el límite de 140 de los principales cortaba justo antes del "Incluye:" que es lo que el cliente pregunta. La normalización aplana a una línea, convierte viñetas en " · " y usa límite 400. Instrucción del bloque actualizada: responder QUÉ INCLUYE con ese detalle en PROSA; si una ambientación no trae detalle, decir con calidez que se confirma con el equipo (nunca inventar ni deducir del nombre). **Bug de import cazado en el camino:** `grounding.py` no importaba `re` (habría reventado el armado del prompt en el primer turno). **Validado en vivo** con el texto REAL del admin de Jorge: 0 asteriscos, conserva "Incluye" + los 4 ítems; bloque completo inspeccionado; ambientación sin descripción simplemente sale con nombre+precio. **Hallazgo aparte pendiente de Jorge: "C1" NO existe en el catálogo** (las de cumpleaños se llaman "Decoración Cumpleaños con Torta · Azul/Rosado" y "Decoración Simple · Azul/Rosado") — si el equipo usa códigos internos R1/R2/C1, deben ir en el NOMBRE o la descripción para que la resolución por nombre (H-080) y el gate (H-083) los reconozcan. Jorge además ya hizo la limpieza de admin: quedan 7 ambientaciones activas (se fueron Mama 2025, Mamá $25k, San Valentín). | Django | 🟢 LIVE (2026-07-30 09:55, deploy ffa6939 verde + sitio 200) — pendiente re-test de Jorge ("¿qué incluye la R1?") + que llene descripcion_web de las ambientaciones que aún no la tengan + decidir el mapeo de códigos internos (C1) | 2026-07-30 (Django) |
| M17→DH | **Migración de M17 a Datamatic Hospitality — arranque** (Jorge greenlight 2026-07-25/26; `datamatic-hospitality/docs/MIGRACION_M17_A_DH.md`). **Modelo de trabajo nuevo desde acá:** Django ↔ agente DH (sesión "DH") coordinan DIRECTO por handoffs; el coordinador (aremko-cli) queda en producto/estrategia. Entregado: `docs/DOSSIER_TRASPASO_M17_A_DH.md` (commit `9fd7891`) — modelos exactos (`Clip`/`UsoClip`/`PublicacionPlanificada`), qué es portable tal cual (`composer.py`, `seleccionar_clip`, `_elegir_diverso`, `_sanear_criterio_foto`) vs. qué necesita adaptación (taxonomía por tenant, FK `tenant`, `UsoClip.publicacion_id` → FK real en vez de referencia suave — la única decisión que se invierte a propósito), 5 gotchas empíricos (sintaxis Cloudinary, `nulls_first`, "último=vigente"), contratos de datos, historial H-070…H-074. DH respondió con `datamatic-hospitality/docs/PLAN_M17_M1.md` (commit `35ff14c`): 3 chunks (M-1a catálogo → M-1b motor del brief → M-1c composición, Fases 2-3 completas dentro de M-1), taxonomía derivada de datos del propio tenant (`Cabana`/`ExperienciaEntorno`/etc., el cliente no la tipea), `UsoClip` → FK real + tenant. Revisión técnica cruzada (3 rondas): confirmé las 4 decisiones + reforcé que el filtro por `tenant` en `seleccionar_clip` debe ser tan duro como `estado='ok'` (nunca relajarse en la cascada — evita que el auto-pick de un cliente elija la foto de otro) + precisé la semántica exacta de `_sanear_criterio_foto` (área inválida = descarta todo; momento/decoración inválidos = degradan a default, NO descartan la pieza) + maticé el umbral de cobertura del catálogo (por-área, no total global — DH lo ajustó a "≥8 `ok` por área" en vez de "20 total"). **Jorge dio el OK (2026-07-26) — M-1a YA CONSTRUIDO por DH** (commit `15a4b88`): `Clip`+`UsoClip` multi-tenant, explorador en admin Unfold, feature flag `marketing.catalogo_fotos`, semáforo con cobertura por área. DH ajustó tipografía a **configurable por tenant** (perfil de marca + default), en vez de fija en código — correcto, dato de marca no de producto. Ronda de revisión adicional (Django→DH, mismo día): señalé 2 grietas reales del gate `estado='ok'` en la implementación de Aremko que conviene decidir a propósito en el multi-tenant (la IA puede proponer `'ok'` directo con un tag incorrecto pero schema-válido; el gate es hoy una convención de "quien tiene la API key ya tiene visto bueno humano", no algo que el código imponga) + corregí un supuesto de DH (`personas` en Aremko NO fuerza `estado`, fuerza el campo informativo `permiso`; el filtro duro real es `personas` mismo). DH sigue a M-1b (motor del brief). | DH (construcción) + Django (dossier + revisión técnica) | 🟢 M-1a CONSTRUIDO (DH) — M-1b en curso. | 2026-07-26 (Django + DH) |
| H-069 | **Cablear los 4 heros del blog que hoy salen en placeholder (degradado)** — vienen del nuevo skill `post-aremko` de aremko-cli (elige fotos reales del banco de clips de Aremko según el tema del post; memoria `project_aremko_banco_clips`). **Posts sin hero:** `refugio-aremko-puerto-varas`, `pausa-junto-al-rio-puerto-varas`, `ritual-del-rio-puerto-varas`, `termas-puerto-varas` (los otros 3 —escapada-romantica / masajes / tinas-calientes— ya tienen hero en Cloudinary `aremko/blog/`, ej. `laurel_2-foto`, `masaje_tui-na`, `aremko_acuarela_tina`). **Qué hacer (Django, dueño del modelo del blog):** subir cada imagen a Cloudinary (`aremko/blog/…`, mismo patrón que los existentes) y setear el campo hero del post correspondiente. **Imágenes listas (16:9, 1600×900) en la Mac de Jorge, carpeta `~/Desktop/heros_blog/`:** (1) `hero_refugio.jpg` → *Refugio Aremko* = foto REAL de cabaña (sala con ventanal al bosque); (2) `hero_pausa.jpg` → *Pausa junto al Río* = tina Puyehue **con vapor**, noche, con ambientación; (3) `hero_ritual.jpg` → *Ritual del Río* = tina Osorno **con vapor**, noche, con ambientación; (4) `hero_termas.jpg` → *¿Termas en Puerto Varas?* = tina Puyehue con el río al lado, día, sin ambientación. **⚠️ Honestidad de marca:** `hero_pausa` y `hero_ritual` son **variantes editadas con IA (ChatGPT/GPT-Image)** que agregan vapor a fotos reales de esas tinas — Jorge las revisó y aprobó (representan tinas reales; el vapor realza el agua caliente real, no es escena sintética). `hero_refugio` y `hero_termas` son fotos reales sin editar. **aremko-cli solo entrega la selección + los `.jpg` recortados; NO puede subir a Cloudinary ni tocar el modelo del blog.** Si prefieren otro canal para recibir los archivos, coordinar con Jorge. **Cómo se resolvió (2026-07-20):** se intentaron 2 rutas automatizadas y ambas quedaron descartadas por diseño — `render ssh` rechaza modo no-interactivo, y la subida de archivo del navegador solo acepta rutas compartidas explícitamente con la sesión (no rutas arbitrarias del disco). En vez de eso, Jorge subió los 4 directo por Django Admin (ya con sesión iniciada) usando los links y el mapeo archivo→post que se le armó. **Verificado en vivo:** las 4 URLs públicas (`/blog/<slug>/`) cargan imagen real de Cloudinary confirmada por red (ej. `aremko/blog/hero_termas_nmmtwi`, HTTP 200), no el placeholder. | Django (Cloudinary + campo hero de cada post) | ✅ CERRADO (2026-07-20) | 2026-07-20 (Jorge + agente Django) |
| H-068 | *(renumerado desde H-067 — colisión con el encargo Fase 3 del agente Datamatic, que llegó 1 min antes)* **🐛 Luna saluda con "nombres" basura de WhatsApp** (caso real en prod, Jorge 2026-07-18, bandeja): cliente +56995985619 con pushname **"L@mirn@dri@@🥰😍"** → Luna abrió con "¡Hola, L@mirn@dri@@🥰😍! 🌿 Te saluda tu asistente…" — trató el pushname (emojis+@) como nombre real. Se ve poco serio y pasa seguido: mucha gente tiene pushnames decorativos. **Criterio de Jorge:** si el nombre incluye signos extraños a un nombre real, NO considerarlo — el saludo debió ser solo "Hola, te saluda tu asistente…". **Propuesta (determinismo en CÓDIGO, no en el prompt — mismo criterio de siempre):** helper único `nombre_presentable(nombre) -> str|None` (o similar): devuelve el nombre solo si parece nombre humano — únicamente letras unicode (tildes/ñ incluidas), espacios y `.'-`, con al menos 2 letras; si trae emojis, `@`, dígitos u otros símbolos → None (tratar como SIN nombre). Aplicarlo en (como mínimo): **(a) donde Luna arma el contexto/saludo** con el nombre del cliente — esto arregla también los clientes YA guardados con nombre basura (como este), porque filtra al LEER, no solo al crear; **(b) el auto-relleno de `Cliente.nombre` desde el pushname de WhatsApp** al crear/actualizar cliente — guardar `''` si no es presentable, así el lápiz de la bandeja (H-004, `if nombre and not cliente.nombre`) puede corregirlo a mano después sin pelear con la basura. **(c) Sugerido no bloqueante:** el mismo helper como gate de `{nombre_cliente}` en plantillas/campañas de email — el mismo pushname basura saldría hoy en un email masivo. Opcional a tu criterio: command de limpieza one-off para los `Cliente.nombre` existentes no-presentables (auditar cuántos hay antes). OJO: no sobre-filtrar — nombres legítimos con tilde/ñ/apóstrofe ("María-José", "O'Higgins") deben pasar. **CERRADO lado Django (2026-07-18):** helper único `nombre_presentable()` en `whatsapp_agent/prompt.py` (solo letras unicode + espacios + `.'-`, ≥2 letras; basura → `''`, placeholders rechazados) y `saneo_nombre` endurecido para usarlo como gate — se acabó el rescate de letras (`'Jorgito🔥'` antes → `'Jorgito'`, ahora `''`). Aplicado al LEER: tool `verificar_cliente` (nombre solo si presentable + entra a `faltan` → Luna pide el nombre real), contexto inicial del agente, y el saludo (vía saneo). Aplicado al ESCRIBIR: `crear_reserva` (get_or_create + update solo-si-presentable), `ClienteService` (create + update conserva existente ante basura) y destinatario de giftcards. Arregla también los clientes YA guardados con basura (filtro al leer). Tests 39/39 (`test_nombre_presentable` + `test_saneo_nombre` actualizado con el caso real). **Quedan como opcionales NO hechos:** gate en `{nombre_cliente}` de campañas email + command de auditoría/limpieza de nombres existentes. Sin cambios aremko-cli (confirmado por ustedes). | Django | ✅ CERRADO lado Django (opcionales pendientes) | 2026-07-18 |
| H-067 | **Fase 3 del Asistente de Publicaciones: métricas POR PUBLICACIÓN → el ciclo de aprendizaje** (Jorge 2026-07-16; contexto completo en `datamatic-hospitality/docs/PLAN_M17.md` §9 y memoria `project_asistente_publicaciones_producto`). Hoy el ciclo se corta en "publicada + published_url": el campo `PublicacionPlanificada.metricas` existe pero NADIE lo llena (no hay cron/command, y `publicacion_actualizar` solo acepta estado/published_url/notas). Sin resultados por pieza no hay lecciones que destilar. **Qué construir (lado Django): (1) Cosecha semanal** — command `sync_metricas_publicaciones` + cron (sugerido viernes AM, para que el brief del lunes ya las vea): para cada publicación `publicada` con `published_url` de las últimas ~4 semanas, obtener métricas vía Graph API de Instagram (media insights: reach, saves, shares, comments, likes, y para reels plays/avg_watch_time si disponible; el media-id se resuelve desde la URL o, mejor, guardarlo al publicar si el flujo lo tiene) y guardarlas en `metricas` (JSON con `fetched_at`, historial simple por semana, no solo el último valor). TikTok/GBP/Email: fuera de alcance F3-a (dejar `{"fuente": "manual"}` como opción para que Angélica pegue números a mano si quiere). **(2) Jerarquía de valor** (regla de Jorge): consultas/ventas > guardados/compartidos > alcance > likes — al calcular un "score" por publicación, ponderar en ese orden; los likes NUNCA como métrica principal. **(3) Sección en el brief siguiente** — pasada 1 recibe un resumen compacto (NO crudo) de los resultados de las últimas 2-4 semanas: top 3 publicaciones y bottom 3 con su porqué aparente, para que el análisis abra con «Decisiones de la semana: persistir con X / no insistir con Y / re-enfocar Z». Esa taxonomía (ganadora✅/descartada❌/en_prueba🔁) es la semilla de la futura capa `Leccion` (memoria de la empresa curada por humano — diseño en PLAN_M17 §9; NO construirla aún, solo dejar el resumen listo para alimentarla). **(4) Salvaguardas**: fuentes tolerantes a fallo (patrón `*_safe` existente — si Meta no responde, el brief sale igual); cuidado con el rate limit de Graph API (batch/sleep); no comparar métricas de semanas distintas sin normalizar por alcance base. **Meta-métrica a registrar desde ya**: % de borradores que Angélica publica sin editar (la tasa de corrección debe BAJAR con las semanas = prueba de que el sistema aprende). aremko-cli (UI): mostrar las métricas en la card de cada publicación cerrada + badge del score; a tu criterio el diseño **🟧 EN PROGRESO (aremko-cli, 2026-07-18, Jorge aprobó arrancar en paralelo a F2 video). AJUSTE DE REPARTO propuesto por aremko-cli (motivo: los clientes de IG orgánico y sus tokens viven en el Go de aremko-cli — regla "credenciales donde corre el código"; Django NO tiene token de IG Graph hoy, solo CAPI que es otro producto):** **(a) aremko-cli COSECHA** — extiende su `instagram_client.go` (lista media propia con `permalink` → matchea contra `published_url` normalizada, más robusto que parsear la URL; insights por media según media_type, tolerante a métrica no disponible) + endpoint protegido por token para el cron (mismo patrón cron-job.org del reporte diario, viernes AM) + merge del historial semanal client-side (lee `metricas` actual vía `GET /publicaciones/semana`, agrega snapshot, manda el objeto completo). **(b) Django RECIBE Y GUARDA** — extender `publicacion_actualizar` (o endpoint nuevo, a su criterio) para aceptar `metricas` (JSON completo, replace — el merge ya viene hecho); **(c) Django BRIEF** — sección "Decisiones de la semana" leyendo `metricas` de las últimas 2-4 semanas (top/bottom por jerarquía de Jorge) + cálculo del % publicado-sin-editar comparando `caption_publicado` (viene en el payload de la cosecha, sacado del media de Graph) vs el caption generado en `copy_json`. Payload propuesto de `metricas`: `{fuente:"instagram_graph", media_id, permalink, media_type, caption_publicado, snapshots:[{fetched_at, reach, saves, shares, comments, likes, views}], tasas:{valiosa:(saves+shares)/reach, interaccion:(likes+comments)/reach}}` — sin score compuesto en Go: el ranking top/bottom lo hace el brief con la jerarquía (consultas/ventas quedan para cuando exista atribución; NO inventar un proxy). TikTok/GBP/email fuera de F3-a como pedía la fila (el payload deja `fuente` para "manual" futuro). **✅ COSECHA aremko-cli LIVE (commits 2ec1aed+a3b0548, 2026-07-18):** endpoint `/api/v1/cron/publicaciones-metricas` (token cron, dry-run default, apply=1 escribe con guardia de persistencia), matching por shortcode, insights con fallback, historial tope 12, payload contrato v1 (`docs/CONTRATO_H-067_METRICAS.md`, validado por Datamatic). **Probado por Jorge en prod: HTTP 200, candidatas:0** (correcto — aún no hay piezas marcadas "publicada" con link de IG). **✅ DJANGO LISTO Y LIVE (commit 14c594e, 2026-07-18, confirmación directa de Jorge):** `publicacion_actualizar` acepta `metricas` (replace, 400 si no es objeto; `_serialize` la devuelve → la guardia del apply funciona), «Decisiones de la semana» en pasada 1 (`marketing_briefs/decisiones.py`, rankea por tasas.valiosa/reach de últimas 4 semanas con `metricas.v`, nunca lanza — sin datos el brief sale igual), % sin-editar tolerante (SequenceMatcher ≥0.95, no-evaluables fuera; TikTok compara contra su caption nativo). Tests 19/19, sin migración. **PARA CERRAR LA FILA:** (1) retro-marcar piezas "publicada"+link (Jorge/Angélica, operativo); (2) primer `apply=1` real verificado contra el endpoint de Django; (3) agendar cron viernes AM en cron-job.org; (4) UI métricas+badge en la card (aremko-cli, con el primer dato real). **✅ PRIMER APPLY REAL OK (2026-07-18, corrido por Jorge en prod):** candidatas:1 → matcheadas:1 (reel Ritual del Río del martes 14-jul, URL /p/ matcheada por shortcode, media_id 17977928622109871) → persistidas:1 — Django guardó `metricas` y la guardia lo verificó en la respuesta. Ciclo completo funcionando end-to-end con datos reales. **✅ CRON AGENDADO por Jorge** (cron-job.org, viernes 09:00 America/Santiago, `0 9 * * 5`, con apply=1; primera corrida automática: 2026-07-24; notificación de fallo activada). **✅ UI LIVE y VERIFICADA por Jorge en prod (commit `9fb5673`):** badge 💎 de tasa valiosa en la fila colapsada + caja "📈 Métricas del post" al expandir (último snapshot + delta vs cosecha anterior) — el reel del Ritual ya muestra 💎 0.4% con datos reales. Higiene pendiente (bajo riesgo, sin urgencia): rotar `REPORTE_CRON_TOKEN` (quedó visible en una captura durante el setup) y actualizar los 3 cronjobs. **H-067 CERRADO — el ciclo de aprendizaje está completo y vivo: pieza → link pegado → cosecha del viernes → metricas → «Decisiones de la semana» en el brief del lunes.** Retro-marcar más piezas publicadas queda como hábito operativo (más datos = mejor ranking); el "toque distintivo" y la capa Leccion (PLAN_M17 §9) siguen explícitamente FUERA hasta nueva orden. | aremko-cli (✅) + Django (✅) | ✅ CERRADO | 2026-07-16 (agente Datamatic) |
| H-039 | **Cotización boutique + Aprobar (Reserva-cliente-digital, Fase 3)** — la cotización deja de ser texto plano y pasa a una página boutique (misma identidad que la Ficha de Fase 1) con botón **Aprobar** que crea la reserva automáticamente. Flujo: Luna pregunta "¿te envío la cotización?" → sí → `PropuestaReserva` → borrador con link en el cajón → Deborah envía → cliente abre y toca **Aprobar** → `crear_reserva` (idempotente) crea la `VentaReserva` → redirige a la Ficha. **Django:** refactor seguro de la creación inline de `crear_reserva` a `crear_reserva_desde_propuesta(propuesta)`; página de cotización (token firmado sobre `propuesta_id`, reusa template de la Ficha en `modo='cotizacion'`, solo Servicios + Aprobar); endpoint `aprobar_cotizacion`; OpenGraph para preview en WhatsApp; pregunta de consentimiento en el prompt de Luna; retirar el cotizador de texto viejo. **aremko-cli:** cajón muestra borrador con link de cotización (en vez del banner "Crear reserva"); tras el Aprobar, banner → "Revisar y enviar Ficha" (= Fase 2); pulir `30000p` de la línea de descuento. Detalle en `docs/BRIEF_H-039_cotizacion_ficha_aprobar.md`. | Django (cotización + Aprobar) ✅ + aremko-cli (cajón + banner) ⬜ | ✅ CERRADO 2026-06-26 (Django + aremko-cli, validado en prod por Jorge end-to-end) — **Django LISTO Y VALIDADO EN PROD** (cotización `propuesta/<token>/` + Aprobar reusa crear_reserva; descuento de pack consistente vía `PackDescuentoService.construir_cart` en propuesta/cotización/reserva; "Pausa junto al río"; #3 no filtra propuesta_id ni "confirmada"; admin de PropuestaReserva con link "🌙 Cotización"). **Falta aremko-cli (B1–B3):** cajón con borrador del link de cotización + banner "Revisar y enviar Ficha" + línea de descuento explícita. Pendiente menor Django: pregunta de consentimiento de Luna + retirar cotizador viejo. **(detalle viejo abajo:)** **Django cotización + Aprobar** (página `propuesta/<token>/` reusa template Ficha en `es_cotizacion`; Aprobar REUSA `crear_reserva` vía APIRequestFactory, sin tocar el camino diario; OpenGraph). Validado manage.py check + render + plomería en Docker; falta **prueba viva** (propuesta real → Aprobar → reserva → Ficha). NO se hizo el refactor de extracción (se reusó crear_reserva tal cual = menor riesgo). Pendiente Django: pregunta de consentimiento en prompt de Luna + retirar cotizador viejo. Pendiente **aremko-cli**: cajón con link de cotización + banner. Fase 1 (Ficha) en prod `64b2207`. **⚠️ GAP DE CONTRATO detectado por aremko-cli (2026-06-26):** el cajón lee `GET /api/inbox/conversation/` → `inbox_omnicanal.views._propuesta_reserva`, que HOY devuelve solo `{propuesta_id, resumen, total, servicios[]}`. Para B1/B2 le faltan 2 cosas que aremko-cli NO puede generar (token firmado con SECRET_KEY de Django): (1) **`url_cotizacion`** en la propuesta pendiente (`from ventas.views.ficha_reserva_view import url_cotizacion; url_cotizacion(propuesta.propuesta_id)`) → para mostrar el borrador con el link que envía Deborah; (2) un bloque **`reserva_creada`** en el payload de la conversación cuando la última `PropuestaReserva` de ese `(canal, external_id)` está en `estado='creada'` (tras el Aprobar del cliente), con `{reserva_id, numero, total, resumen, url_ficha}` (`url_ficha_reserva(reserva_id)`) → para el banner "Revisar y enviar Ficha". **Solicitado a Django por mensaje cruzado. ✅ RESUELTO por Django (commit `1bff8e3`):** `_propuesta_reserva` ahora incluye `url_cotizacion`, y se agregó el bloque `reserva_creada` (`{reserva_id, numero, total, resumen, url_ficha}`, solo cuando la ÚLTIMA propuesta está `creada` y es <48h) en los 3 canales de `/api/inbox/conversation/` + el gemelo `/api/whatsapp/conversation/`. Aditivo. aremko-cli desbloqueado para B1/B2. B3 (línea de descuento explícita) aremko-cli la hace 100% en frontend (`Σ subtotales − total`, y limpia la línea cruda `Descuento_Servicios · 30000p` → "Descuento · −$X") **sin tocar Django**. **✅ FRONTEND aremko-cli HECHO + VALIDADO (commit `560230d`, `tsc` 0 + `next build` OK):** componente `CotizacionCajon.tsx` (reemplaza `BotonCrearReserva`, borrado) cableado en WhatsApp + Instagram/Messenger — B1 botón "Poner cotización en el mensaje" (borrador con `url_cotizacion` que Deborah envía), B2 banner verde "Reserva creada Nº X — revisa y envía la Ficha" (consume `reserva_creada.url_ficha`), B3 línea "Descuento −$X". Sin cambios en Go (los endpoints de conversación son passthrough crudo). Decisión Jorge: reemplazar del todo el botón "Crear reserva" (sin fallback). Push coordinado tras el deploy de Django (`1bff8e3`). **✅ VALIDADO EN PROD END-TO-END por Jorge 2026-06-26:** propuesta con Luna ("Pausa junto al río" tina+masaje, $130k→$110k) → cajón ámbar "Cotización lista" con línea **Descuento −$20.000** + botón "Poner cotización en el mensaje" → borrador con `url_cotizacion` enviado → página de cotización boutique abierta → Aprobar → **Reserva Nº 6118** creada + Ficha "Pendiente de pago" → cajón pasó solo a banner verde "Reserva creada Nº 6118" con "Poner Ficha en el mensaje". Reserva de prueba 6118 ELIMINADA por Jorge. **Iteración post-validación (commit aremko-cli `ff7bd2c`):** botón **✕ para cerrar el banner verde** (el `reserva_creada` reaparecía hasta 48h en cada apertura) → cierre persistido por `reserva_id` en localStorage (por estación), no reaparece al Actualizar. Commits aremko-cli: `560230d` (cajón B1-B3) + `ff7bd2c` (✕). | 2026-06-26 (agente aremko-cli) |
| H-038 | **Luna Interna · Fase 2: avisos de operación al recepcionista por WhatsApp** (pedido de Jorge 2026-06-22; Jorge cubre recepción "hasta nuevo aviso"). control_gestion YA crea tareas automáticas desde las reservas (check-in → tarea Recepción + Operación por servicio con checklist + hora límite). Esta fase = el PUENTE a WhatsApp: tarea de Recepción/Operación creada → aviso al recepcionista de turno. **Ya hecho (Django, commit `0029459`):** `PersonalOperativo.recibe_avisos_operacion` (marca al de turno) + signal post_save sobre `control_gestion.Task` (swimlanes RX/OPS, sin tocar control_gestion) → encola en **`NotificacionStaff`** (cola saliente durable, idempotente por dedup_key) + endpoints X-API-Key `GET /api/staff/notificaciones` (pendientes) y `POST /api/staff/notificaciones/marcar` (enviada\|fallida) + admin monitor. **Falta aremko-cli:** drenar la cola (poll) → enviar el `texto` por WhatsApp al `telefono` (path /whatsapp/reply) → marcar. Ojo ventana 24h (proactivo): en turno activo la ventana suele estar abierta; si cerrada, marcar fallida (plantilla = v2). "Todas las tareas al comienzo (RX+OPS), luego filtramos". Detalle en `docs/BRIEF_H-038_avisos_operacion_staff.md`. | Django (cola + signal + endpoints ✅) + aremko-cli (drenar + enviar) | 🟧 EN PROGRESO — Django LISTO (commit `0029459`); pendiente aremko-cli: drenar/enviar el canal saliente. | 2026-06-22 (agente Django) |

## Estados

`SOLICITADO` → `EN PROGRESO (lado X)` → `IMPLEMENTADO (esperando al otro lado)` → `INTEGRADO` → `CERRADO`

- **SOLICITADO**: existe el `BRIEF_H-xxx_*.md`, nadie lo tomó aún.
- **EN PROGRESO (lado X)**: el agente X está trabajando. ⚠️ Marcar ANTES de empezar.
- **IMPLEMENTADO**: un lado terminó y publicó su `RESPUESTA_H-xxx_*.md`; falta el otro.
- **INTEGRADO**: ambos lados desplegados, falta validación de Jorge en producción.
- **CERRADO**: validado funcionando.

## Reglas del protocolo (para ambos agentes)

1. **Un ID por encargo cruzado**: serie `H-001`, `H-002`, … (los AR-### siguen
   siendo para bugs/issues internos del booking system). Los documentos llevan
   el ID: `docs/BRIEF_H-002_<tema>.md` y `docs/RESPUESTA_H-002_<tema>.md`.
2. **Commit + push del cambio de estado ANTES de empezar a trabajar** (fila →
   `EN PROGRESO (Django)` o `EN PROGRESO (aremko-cli)`), y de nuevo al terminar.
   El estado vive en git, no en la memoria de la sesión.
3. **Este archivo vive solo en el repo Django** (aquí ya viven los briefs).
   El agente aremko-cli lo lee y edita directamente en
   `~/dev/booking-system-aremko/docs/HANDOFFS.md` y commitea SOLO este archivo
   (y briefs/respuestas) en este repo — nunca código del otro lado.
4. **Tras un corte** (energía, sesión, contexto): leer esta tabla; si hay un
   `EN PROGRESO`, revisar `git log --oneline -10` y `git status` del repo que
   implementa para ver qué alcanzó a commitearse. No re-implementar sin
   verificar primero.
5. **Mensajes de commit de la bitácora**: `docs(handoffs): H-xxx → <estado>`.
6. **Histórico previo al protocolo** (referencia): brief de la bandeja masajes
   (`BRIEF_BANDEJA_MASAJES_AREMKO_CLI.md`), respuesta geo
   (`RESPUESTA_OUTBOX_GEO_AREMKO_CLI.md`) — anteriores a la numeración, no se renombran.
